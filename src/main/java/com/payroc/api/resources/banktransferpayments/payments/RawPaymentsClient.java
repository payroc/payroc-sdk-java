/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.banktransferpayments.payments;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.PayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.banktransferpayments.payments.requests.BankTransferPaymentRequest;
import com.payroc.api.resources.banktransferpayments.payments.requests.ListPaymentsRequest;
import com.payroc.api.resources.banktransferpayments.payments.requests.Representment;
import com.payroc.api.resources.banktransferpayments.payments.requests.RetrievePaymentsRequest;
import com.payroc.api.types.BankTransferPayment;
import com.payroc.api.types.BankTransferPaymentPaginatedList;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawPaymentsClient {
    protected final ClientOptions clientOptions;

    public RawPaymentsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payments.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment and you have its paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve Payment</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a date range, or a settlement state.</p>
     * <p>Our gateway returns the following information about each payment in the list:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer's details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<BankTransferPayment>> list(ListPaymentsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payments.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment and you have its paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve Payment</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a date range, or a settlement state.</p>
     * <p>Our gateway returns the following information about each payment in the list:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer's details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<BankTransferPayment>> list(
            ListPaymentsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("bank-transfer-payments");
        QueryStringMapper.addQueryParameter(httpUrl, "processingTerminalId", request.getProcessingTerminalId(), false);
        if (request.getOrderId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "orderId", request.getOrderId().get(), false);
        }
        if (request.getNameOnAccount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "nameOnAccount", request.getNameOnAccount().get(), false);
        }
        if (request.getLast4().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last4", request.getLast4().get(), false);
        }
        if (request.getDateFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateFrom", request.getDateFrom().get(), false);
        }
        if (request.getDateTo().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateTo", request.getDateTo().get(), false);
        }
        if (request.getSettlementState().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementState", request.getSettlementState().get(), false);
        }
        if (request.getSettlementDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementDate", request.getSettlementDate().get(), false);
        }
        if (request.getPaymentLinkId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "paymentLinkId", request.getPaymentLinkId().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "type", request.getType().get(), true);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                BankTransferPaymentPaginatedList parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BankTransferPaymentPaginatedList.class);
                return new PayrocApiHttpResponse<>(
                        PayrocPager.create(parsedResponse, clientOptions, requestOptions), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to run a sale with a customer's bank account details.
     * <p>In the response, our gateway returns information about the bank transfer payment and a paymentId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve payment</a> - View the details of the bank transfer payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/reverse-payment">Reverse payment</a> - Cancel the bank transfer payment if it's an open batch.</li>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/refund">Refund payment</a> - Run a referenced refund to return funds to the customer's bank account.</li>
     * </ul>
     * <p><strong>Payment methods</strong></p>
     * <p>Our gateway accepts the following payment methods:</p>
     * <ul>
     * <li>Automated clearing house (ACH) details</li>
     * <li>Pre-authorized debit (PAD) details</li>
     * </ul>
     * <p>You can also use <a href="https://docs.payroc.com/api/schema/payments/secure-tokens/overview">secure tokens</a> and <a href="https://docs.payroc.com/api/schema/tokenization/single-use-tokens/create">single-use tokens</a> that you created from ACH details or PAD details.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> create(BankTransferPaymentRequest request) {
        return create(request, null);
    }

    /**
     * Use this method to run a sale with a customer's bank account details.
     * <p>In the response, our gateway returns information about the bank transfer payment and a paymentId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve payment</a> - View the details of the bank transfer payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/reverse-payment">Reverse payment</a> - Cancel the bank transfer payment if it's an open batch.</li>
     * <li><a href="https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/refund">Refund payment</a> - Run a referenced refund to return funds to the customer's bank account.</li>
     * </ul>
     * <p><strong>Payment methods</strong></p>
     * <p>Our gateway accepts the following payment methods:</p>
     * <ul>
     * <li>Automated clearing house (ACH) details</li>
     * <li>Pre-authorized debit (PAD) details</li>
     * </ul>
     * <p>You can also use <a href="https://docs.payroc.com/api/schema/payments/secure-tokens/overview">secure tokens</a> and <a href="https://docs.payroc.com/api/schema/tokenization/single-use-tokens/create">single-use tokens</a> that you created from ACH details or PAD details.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> create(
            BankTransferPaymentRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("bank-transfer-payments");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BankTransferPayment.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to retrieve information about a bank transfer payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/create">Create Payment</a> method.</p>
     * <p>Note: If you don’t have the paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer’s details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>If the merchant saved the customer’s bank account details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> retrieve(String paymentId) {
        return retrieve(paymentId, RetrievePaymentsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a bank transfer payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/create">Create Payment</a> method.</p>
     * <p>Note: If you don’t have the paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer’s details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>If the merchant saved the customer’s bank account details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> retrieve(String paymentId, RequestOptions requestOptions) {
        return retrieve(paymentId, RetrievePaymentsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a bank transfer payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/create">Create Payment</a> method.</p>
     * <p>Note: If you don’t have the paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer’s details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>If the merchant saved the customer’s bank account details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> retrieve(String paymentId, RetrievePaymentsRequest request) {
        return retrieve(paymentId, request, null);
    }

    /**
     * Use this method to retrieve information about a bank transfer payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/create">Create Payment</a> method.</p>
     * <p>Note: If you don’t have the paymentId, use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Bank account details, including the customer’s name and account number.</li>
     * <li>Customer’s details, including the customer’s phone number.</li>
     * <li>Transaction details, including any refunds or re-presentments.</li>
     * </ul>
     * <p>If the merchant saved the customer’s bank account details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> retrieve(
            String paymentId, RetrievePaymentsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("bank-transfer-payments")
                .addPathSegment(paymentId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BankTransferPayment.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to re-present an ACH payment.
     * <p>To re-present a payment, you need the paymentId of the return. To get the paymentId of the return, complete the following steps:</p>
     * <ol>
     * <li>Use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve Payment</a> method  to view the details of the original payment.</li>
     * <li>From the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve#response.body.returns">returns object</a> in the response, get the paymentId of the return.</li>
     * </ol>
     * <p>Our gateway uses the bank account details from the original payment. If you want to update the customer's bank account details, send the new bank account details in the request.</p>
     * <p>If your request is successful, our gateway re-presents the payment.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> represent(String paymentId, Representment request) {
        return represent(paymentId, request, null);
    }

    /**
     * Use this method to re-present an ACH payment.
     * <p>To re-present a payment, you need the paymentId of the return. To get the paymentId of the return, complete the following steps:</p>
     * <ol>
     * <li>Use our <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve">Retrieve Payment</a> method  to view the details of the original payment.</li>
     * <li>From the <a href="https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve#response.body.returns">returns object</a> in the response, get the paymentId of the return.</li>
     * </ol>
     * <p>Our gateway uses the bank account details from the original payment. If you want to update the customer's bank account details, send the new bank account details in the request.</p>
     * <p>If your request is successful, our gateway re-presents the payment.</p>
     */
    public PayrocApiHttpResponse<BankTransferPayment> represent(
            String paymentId, Representment request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("bank-transfer-payments")
                .addPathSegment(paymentId)
                .addPathSegments("represent");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BankTransferPayment.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
