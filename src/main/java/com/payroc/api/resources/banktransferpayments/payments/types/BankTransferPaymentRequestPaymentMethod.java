/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.banktransferpayments.payments.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.AchPayload;
import com.payroc.api.types.PadPayload;
import com.payroc.api.types.SecureTokenPayload;
import com.payroc.api.types.SingleUseTokenPayload;
import java.util.Objects;
import java.util.Optional;

public final class BankTransferPaymentRequestPaymentMethod {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private BankTransferPaymentRequestPaymentMethod(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static BankTransferPaymentRequestPaymentMethod ach(AchPayload value) {
        return new BankTransferPaymentRequestPaymentMethod(new AchValue(value));
    }

    public static BankTransferPaymentRequestPaymentMethod pad(PadPayload value) {
        return new BankTransferPaymentRequestPaymentMethod(new PadValue(value));
    }

    public static BankTransferPaymentRequestPaymentMethod secureToken(SecureTokenPayload value) {
        return new BankTransferPaymentRequestPaymentMethod(new SecureTokenValue(value));
    }

    public static BankTransferPaymentRequestPaymentMethod singleUseToken(SingleUseTokenPayload value) {
        return new BankTransferPaymentRequestPaymentMethod(new SingleUseTokenValue(value));
    }

    public boolean isAch() {
        return value instanceof AchValue;
    }

    public boolean isPad() {
        return value instanceof PadValue;
    }

    public boolean isSecureToken() {
        return value instanceof SecureTokenValue;
    }

    public boolean isSingleUseToken() {
        return value instanceof SingleUseTokenValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AchPayload> getAch() {
        if (isAch()) {
            return Optional.of(((AchValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PadPayload> getPad() {
        if (isPad()) {
            return Optional.of(((PadValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SecureTokenPayload> getSecureToken() {
        if (isSecureToken()) {
            return Optional.of(((SecureTokenValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SingleUseTokenPayload> getSingleUseToken() {
        if (isSingleUseToken()) {
            return Optional.of(((SingleUseTokenValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAch(AchPayload ach);

        T visitPad(PadPayload pad);

        T visitSecureToken(SecureTokenPayload secureToken);

        T visitSingleUseToken(SingleUseTokenPayload singleUseToken);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(AchValue.class),
        @JsonSubTypes.Type(PadValue.class),
        @JsonSubTypes.Type(SecureTokenValue.class),
        @JsonSubTypes.Type(SingleUseTokenValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("ach")
    @JsonIgnoreProperties("type")
    private static final class AchValue implements Value {
        @JsonUnwrapped
        private AchPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AchValue() {}

        private AchValue(AchPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAch(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AchValue && equalTo((AchValue) other);
        }

        private boolean equalTo(AchValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BankTransferPaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("pad")
    @JsonIgnoreProperties("type")
    private static final class PadValue implements Value {
        @JsonUnwrapped
        private PadPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PadValue() {}

        private PadValue(PadPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPad(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PadValue && equalTo((PadValue) other);
        }

        private boolean equalTo(PadValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BankTransferPaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("secureToken")
    @JsonIgnoreProperties("type")
    private static final class SecureTokenValue implements Value {
        @JsonUnwrapped
        private SecureTokenPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SecureTokenValue() {}

        private SecureTokenValue(SecureTokenPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSecureToken(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SecureTokenValue && equalTo((SecureTokenValue) other);
        }

        private boolean equalTo(SecureTokenValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BankTransferPaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("singleUseToken")
    @JsonIgnoreProperties("type")
    private static final class SingleUseTokenValue implements Value {
        @JsonUnwrapped
        private SingleUseTokenPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SingleUseTokenValue() {}

        private SingleUseTokenValue(SingleUseTokenPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSingleUseToken(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SingleUseTokenValue && equalTo((SingleUseTokenValue) other);
        }

        private boolean equalTo(SingleUseTokenValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BankTransferPaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BankTransferPaymentRequestPaymentMethod{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
