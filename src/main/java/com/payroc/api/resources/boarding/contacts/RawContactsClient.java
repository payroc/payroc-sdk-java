/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.boarding.contacts;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.boarding.contacts.requests.DeleteContactsRequest;
import com.payroc.api.resources.boarding.contacts.requests.RetrieveContactsRequest;
import com.payroc.api.resources.boarding.contacts.requests.UpdateContactsRequest;
import com.payroc.api.types.Contact;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawContactsClient {
    protected final ClientOptions clientOptions;

    public RawContactsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to retrieve details about a contact.
     * <p>To retrieve a contact, you need its contactId. Our gateway returned the contactId in the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the contactId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     * <p>Our gateway returns the following information about a contact:</p>
     * <ul>
     * <li>Name and contact method, including their phone number or mobile number.</li>
     * <li>Role within the business, for example, if they are a manager.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Contact> retrieve(int contactId) {
        return retrieve(contactId, RetrieveContactsRequest.builder().build());
    }

    /**
     * Use this method to retrieve details about a contact.
     * <p>To retrieve a contact, you need its contactId. Our gateway returned the contactId in the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the contactId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     * <p>Our gateway returns the following information about a contact:</p>
     * <ul>
     * <li>Name and contact method, including their phone number or mobile number.</li>
     * <li>Role within the business, for example, if they are a manager.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Contact> retrieve(int contactId, RetrieveContactsRequest request) {
        return retrieve(contactId, request, null);
    }

    /**
     * Use this method to retrieve details about a contact.
     * <p>To retrieve a contact, you need its contactId. Our gateway returned the contactId in the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the contactId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     * <p>Our gateway returns the following information about a contact:</p>
     * <ul>
     * <li>Name and contact method, including their phone number or mobile number.</li>
     * <li>Role within the business, for example, if they are a manager.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Contact> retrieve(
            int contactId, RetrieveContactsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("contacts")
                .addPathSegment(Integer.toString(contactId))
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Contact.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to update a contact of a processing account.
     * <p>To update a contact, you need its contactId. Our gateway returned the contactId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the contactId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     * <p>You can update the following details about a contact:</p>
     * <ul>
     * <li>First name and last name.</li>
     * <li>Contact details, including their phone number or mobile number.</li>
     * <li>Identification details, including their identification type and number.</li>
     * <li>Role within the business, for example, if they are a manager.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Void> update(int contactId, UpdateContactsRequest request) {
        return update(contactId, request, null);
    }

    /**
     * Use this method to update a contact of a processing account.
     * <p>To update a contact, you need its contactId. Our gateway returned the contactId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the contactId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     * <p>You can update the following details about a contact:</p>
     * <ul>
     * <li>First name and last name.</li>
     * <li>Contact details, including their phone number or mobile number.</li>
     * <li>Identification details, including their identification type and number.</li>
     * <li>Role within the business, for example, if they are a manager.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Void> update(
            int contactId, UpdateContactsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("contacts")
                .addPathSegment(Integer.toString(contactId))
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to delete a contact associated with a processing account.
     * <p>To delete a contact, you need their contactId. Our gateway returned the contactId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the contactId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method or our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     */
    public PayrocApiHttpResponse<Void> delete(int contactId) {
        return delete(contactId, DeleteContactsRequest.builder().build());
    }

    /**
     * Use this method to delete a contact associated with a processing account.
     * <p>To delete a contact, you need their contactId. Our gateway returned the contactId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the contactId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method or our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     */
    public PayrocApiHttpResponse<Void> delete(int contactId, DeleteContactsRequest request) {
        return delete(contactId, request, null);
    }

    /**
     * Use this method to delete a contact associated with a processing account.
     * <p>To delete a contact, you need their contactId. Our gateway returned the contactId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the contactId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method or our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts">List Contacts</a> method to search for the contact.</p>
     */
    public PayrocApiHttpResponse<Void> delete(
            int contactId, DeleteContactsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("contacts")
                .addPathSegment(Integer.toString(contactId))
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
