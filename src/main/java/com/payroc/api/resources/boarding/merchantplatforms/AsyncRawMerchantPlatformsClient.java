/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.boarding.merchantplatforms;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.boarding.merchantplatforms.requests.CreateMerchantAccount;
import com.payroc.api.resources.boarding.merchantplatforms.requests.CreateProcessingAccountMerchantPlatformsRequest;
import com.payroc.api.resources.boarding.merchantplatforms.requests.ListBoardingMerchantPlatformProcessingAccountsRequest;
import com.payroc.api.resources.boarding.merchantplatforms.requests.ListMerchantPlatformsRequest;
import com.payroc.api.resources.boarding.merchantplatforms.requests.RetrieveMerchantPlatformsRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.MerchantPlatform;
import com.payroc.api.types.PaginatedMerchants;
import com.payroc.api.types.PaginatedProcessingAccounts;
import com.payroc.api.types.ProcessingAccount;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawMerchantPlatformsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawMerchantPlatformsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of merchant platforms that are linked to your ISV account.
     * <p><strong>Note</strong>: If you want to view the details of a specific merchant platform and you have its merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/retrieve">Retrieve Merchant Platform</a> method.</p>
     * <p>Our gateway returns the following information about each merchant platform in the list:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing  account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     * <p>For each merchant platform, we also return its merchantPlatformId and its linked processingAccountIds, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<MerchantPlatform>>>> list() {
        return list(ListMerchantPlatformsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of merchant platforms that are linked to your ISV account.
     * <p><strong>Note</strong>: If you want to view the details of a specific merchant platform and you have its merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/retrieve">Retrieve Merchant Platform</a> method.</p>
     * <p>Our gateway returns the following information about each merchant platform in the list:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing  account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     * <p>For each merchant platform, we also return its merchantPlatformId and its linked processingAccountIds, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<MerchantPlatform>>>> list(
            ListMerchantPlatformsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of merchant platforms that are linked to your ISV account.
     * <p><strong>Note</strong>: If you want to view the details of a specific merchant platform and you have its merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/retrieve">Retrieve Merchant Platform</a> method.</p>
     * <p>Our gateway returns the following information about each merchant platform in the list:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing  account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     * <p>For each merchant platform, we also return its merchantPlatformId and its linked processingAccountIds, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<MerchantPlatform>>>> list(
            ListMerchantPlatformsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("merchant-platforms");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<MerchantPlatform>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaginatedMerchants parsedResponse =
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaginatedMerchants.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to board a merchant with Payroc.
     * <p><strong>Note</strong>: This method is part of our Boarding solution. To help you understand how this method works with other Boarding methods, go to <a href="https://docs.payroc.com/guides/integrate/boarding">Board a Merchant</a>.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing account information, including the pricing model, owners, and contacts for the processing account.</li>
     * </ul>
     * <p>When you send a successful request, we review the merchant's information. After we complete our review and approve the merchant, we assign:</p>
     * <ul>
     * <li><strong>merchantPlatformId</strong> - Unique identifier for the merchant platform.</li>
     * <li><strong>processingAccountId</strong> - Unique identifier for each processing account linked to the merchant platform.</li>
     * </ul>
     * <p>You need to keep these to perform follow-on actions, for example, you need the processingAccountId to <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/create-terminal-order">order terminals</a> for the processing account.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> create(CreateMerchantAccount request) {
        return create(request, null);
    }

    /**
     * Use this method to board a merchant with Payroc.
     * <p><strong>Note</strong>: This method is part of our Boarding solution. To help you understand how this method works with other Boarding methods, go to <a href="https://docs.payroc.com/guides/integrate/boarding">Board a Merchant</a>.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing account information, including the pricing model, owners, and contacts for the processing account.</li>
     * </ul>
     * <p>When you send a successful request, we review the merchant's information. After we complete our review and approve the merchant, we assign:</p>
     * <ul>
     * <li><strong>merchantPlatformId</strong> - Unique identifier for the merchant platform.</li>
     * <li><strong>processingAccountId</strong> - Unique identifier for each processing account linked to the merchant platform.</li>
     * </ul>
     * <p>You need to keep these to perform follow-on actions, for example, you need the processingAccountId to <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/create-terminal-order">order terminals</a> for the processing account.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> create(
            CreateMerchantAccount request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("merchant-platforms")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MerchantPlatform.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a merchant platform.
     * <p>To retrieve a merchant platform, you need its merchantPlatformId. Our gateway returned the merchantPlatformId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create">Create Merchant Platform</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about the merchant platform:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> retrieve(String merchantPlatformId) {
        return retrieve(
                merchantPlatformId, RetrieveMerchantPlatformsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a merchant platform.
     * <p>To retrieve a merchant platform, you need its merchantPlatformId. Our gateway returned the merchantPlatformId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create">Create Merchant Platform</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about the merchant platform:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> retrieve(
            String merchantPlatformId, RetrieveMerchantPlatformsRequest request) {
        return retrieve(merchantPlatformId, request, null);
    }

    /**
     * Use this method to retrieve information about a merchant platform.
     * <p>To retrieve a merchant platform, you need its merchantPlatformId. Our gateway returned the merchantPlatformId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create">Create Merchant Platform</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about the merchant platform:</p>
     * <ul>
     * <li>Legal information, including its legal name and address.</li>
     * <li>Contact information, including the email address for the business.</li>
     * <li>Processing account information, including the processingAccountId and status of each processing account that's linked to the merchant platform.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> retrieve(
            String merchantPlatformId, RetrieveMerchantPlatformsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("merchant-platforms")
                .addPathSegment(merchantPlatformId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<MerchantPlatform>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MerchantPlatform.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of processing accounts linked to a merchant platform.
     * <p><strong>Note</strong>: If you want to view the details of a specific processing account and you have its processingAccountId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method.</p>
     * <p>Use the query parameters to filter the list of results that we return, for example, to search for only closed processing accounts.</p>
     * <p>To list the processing accounts for a merchant platform, you need its merchantPlatformId. If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about eahc processing account in the list:</p>
     * <ul>
     * <li>Business details, including its status, time zone, and address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding, pricing, and processing information, including its pricing model and funding accounts.</li>
     * </ul>
     * <p>For each processing account, we also return its processingAccountId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ProcessingAccount>>>>
            listProcessingAccounts(String merchantPlatformId) {
        return listProcessingAccounts(
                merchantPlatformId,
                ListBoardingMerchantPlatformProcessingAccountsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of processing accounts linked to a merchant platform.
     * <p><strong>Note</strong>: If you want to view the details of a specific processing account and you have its processingAccountId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method.</p>
     * <p>Use the query parameters to filter the list of results that we return, for example, to search for only closed processing accounts.</p>
     * <p>To list the processing accounts for a merchant platform, you need its merchantPlatformId. If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about eahc processing account in the list:</p>
     * <ul>
     * <li>Business details, including its status, time zone, and address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding, pricing, and processing information, including its pricing model and funding accounts.</li>
     * </ul>
     * <p>For each processing account, we also return its processingAccountId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ProcessingAccount>>>>
            listProcessingAccounts(
                    String merchantPlatformId, ListBoardingMerchantPlatformProcessingAccountsRequest request) {
        return listProcessingAccounts(merchantPlatformId, request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of processing accounts linked to a merchant platform.
     * <p><strong>Note</strong>: If you want to view the details of a specific processing account and you have its processingAccountId, use our <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method.</p>
     * <p>Use the query parameters to filter the list of results that we return, for example, to search for only closed processing accounts.</p>
     * <p>To list the processing accounts for a merchant platform, you need its merchantPlatformId. If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>Our gateway returns the following information about eahc processing account in the list:</p>
     * <ul>
     * <li>Business details, including its status, time zone, and address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding, pricing, and processing information, including its pricing model and funding accounts.</li>
     * </ul>
     * <p>For each processing account, we also return its processingAccountId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ProcessingAccount>>>>
            listProcessingAccounts(
                    String merchantPlatformId,
                    ListBoardingMerchantPlatformProcessingAccountsRequest request,
                    RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("merchant-platforms")
                .addPathSegment(merchantPlatformId)
                .addPathSegments("processing-accounts");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getIncludeClosed().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "includeClosed", request.getIncludeClosed().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ProcessingAccount>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaginatedProcessingAccounts parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, PaginatedProcessingAccounts.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to add an additional processing account to a merchant platform.
     * <p>To add a processing account to a merchant platform, you need the merchantPlatformId. Our gateway returned the merchantPlatformId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create">Create Merchant Platform</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Business details, including its business type, time zone, and address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding, pricing, and processing information, including its pricing model and funding accounts.</li>
     * </ul>
     * <p>When you send a successful request, we review the information about the processing account. After we complete our review and approve the processing account, we assign a processingAccountId, which you need to perform follow-on actions.</p>
     * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we update the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events List</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<ProcessingAccount>> createProcessingAccount(
            String merchantPlatformId, CreateProcessingAccountMerchantPlatformsRequest request) {
        return createProcessingAccount(merchantPlatformId, request, null);
    }

    /**
     * Use this method to add an additional processing account to a merchant platform.
     * <p>To add a processing account to a merchant platform, you need the merchantPlatformId. Our gateway returned the merchantPlatformId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create">Create Merchant Platform</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the merchantPlatformId, use our <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/list">List Merchant Platforms</a> method to search for the merchant platform.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Business details, including its business type, time zone, and address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding, pricing, and processing information, including its pricing model and funding accounts.</li>
     * </ul>
     * <p>When you send a successful request, we review the information about the processing account. After we complete our review and approve the processing account, we assign a processingAccountId, which you need to perform follow-on actions.</p>
     * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we update the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events List</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<ProcessingAccount>> createProcessingAccount(
            String merchantPlatformId,
            CreateProcessingAccountMerchantPlatformsRequest request,
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("merchant-platforms")
                .addPathSegment(merchantPlatformId)
                .addPathSegments("processing-accounts")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<ProcessingAccount>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ProcessingAccount.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
