/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.boarding.owners;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.boarding.owners.requests.DeleteOwnersRequest;
import com.payroc.api.resources.boarding.owners.requests.RetrieveOwnersRequest;
import com.payroc.api.resources.boarding.owners.requests.UpdateOwnersRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.Owner;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawOwnersClient {
    protected final ClientOptions clientOptions;

    public AsyncRawOwnersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to retrieve details about an owner of a processing account or an owner associated with a funding recipient.
     * <p>To retrieve an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method if you are searching for a processing account owner, or use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method if you are searching for a funding recipient owner.</p>
     * <p>Our gateway returns the following information about an owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong or authorized signatory, and their equity stake in the business.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> retrieve(int ownerId) {
        return retrieve(ownerId, RetrieveOwnersRequest.builder().build());
    }

    /**
     * Use this method to retrieve details about an owner of a processing account or an owner associated with a funding recipient.
     * <p>To retrieve an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method if you are searching for a processing account owner, or use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method if you are searching for a funding recipient owner.</p>
     * <p>Our gateway returns the following information about an owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong or authorized signatory, and their equity stake in the business.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> retrieve(int ownerId, RequestOptions requestOptions) {
        return retrieve(ownerId, RetrieveOwnersRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve details about an owner of a processing account or an owner associated with a funding recipient.
     * <p>To retrieve an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method if you are searching for a processing account owner, or use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method if you are searching for a funding recipient owner.</p>
     * <p>Our gateway returns the following information about an owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong or authorized signatory, and their equity stake in the business.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> retrieve(int ownerId, RetrieveOwnersRequest request) {
        return retrieve(ownerId, request, null);
    }

    /**
     * Use this method to retrieve details about an owner of a processing account or an owner associated with a funding recipient.
     * <p>To retrieve an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account">Create Processing Account</a> method or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve">Retrieve Processing Account</a> method if you are searching for a processing account owner, or use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method if you are searching for a funding recipient owner.</p>
     * <p>Our gateway returns the following information about an owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong or authorized signatory, and their equity stake in the business.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> retrieve(
            int ownerId, RetrieveOwnersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("owners")
                .addPathSegment(Integer.toString(ownerId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Owner>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Owner.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't update the details of an owner of a processing account.
     * </blockquote>
     * <p>Use this method to update the details of an owner associated with a funding recipient.</p>
     * <p>To update an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     * <p>You can update the following details about an owner:</p>
     * <ul>
     * <li>Personal details, including their name, date of birth, and address.</li>
     * <li>Identification details, including their identification type and number.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(int ownerId, UpdateOwnersRequest request) {
        return update(ownerId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't update the details of an owner of a processing account.
     * </blockquote>
     * <p>Use this method to update the details of an owner associated with a funding recipient.</p>
     * <p>To update an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     * <p>You can update the following details about an owner:</p>
     * <ul>
     * <li>Personal details, including their name, date of birth, and address.</li>
     * <li>Identification details, including their identification type and number.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the business, including whether they are a control prong.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int ownerId, UpdateOwnersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("owners")
                .addPathSegment(Integer.toString(ownerId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete an owner of a processing account.
     * </blockquote>
     * <p>Use this method to delete an owner associated with a funding recipient. You can delete an owner only if the funding recipient has more than one owner.</p>
     * <p>To delete an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int ownerId) {
        return delete(ownerId, DeleteOwnersRequest.builder().build());
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete an owner of a processing account.
     * </blockquote>
     * <p>Use this method to delete an owner associated with a funding recipient. You can delete an owner only if the funding recipient has more than one owner.</p>
     * <p>To delete an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int ownerId, RequestOptions requestOptions) {
        return delete(ownerId, DeleteOwnersRequest.builder().build(), requestOptions);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete an owner of a processing account.
     * </blockquote>
     * <p>Use this method to delete an owner associated with a funding recipient. You can delete an owner only if the funding recipient has more than one owner.</p>
     * <p>To delete an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int ownerId, DeleteOwnersRequest request) {
        return delete(ownerId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete an owner of a processing account.
     * </blockquote>
     * <p>Use this method to delete an owner associated with a funding recipient. You can delete an owner only if the funding recipient has more than one owner.</p>
     * <p>To delete an owner, you need their ownerId. Our gateway returned the ownerId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method and the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create-owner">Create Funding Recipient Owner</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the ownerId, use the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list-owners">List Funding Recipient Owners</a> method, the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method, or the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient owner.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int ownerId, DeleteOwnersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("owners")
                .addPathSegment(Integer.toString(ownerId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
