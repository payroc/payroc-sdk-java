/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.cardpayments.payments;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.cardpayments.payments.requests.ListPaymentsRequest;
import com.payroc.api.resources.cardpayments.payments.requests.PaymentAdjustment;
import com.payroc.api.resources.cardpayments.payments.requests.PaymentCapture;
import com.payroc.api.resources.cardpayments.payments.requests.PaymentRequest;
import com.payroc.api.resources.cardpayments.payments.requests.RetrievePaymentsRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.Payment;
import com.payroc.api.types.PaymentPaginatedListForRead;
import com.payroc.api.types.RetrievedPayment;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawPaymentsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawPaymentsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payments.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment and you have its paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/retrieve">Retrieve Payment</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a tip mode, or a date range.</p>
     * <p>Our gateway returns the following information about each payment in the list:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<RetrievedPayment>>>> list() {
        return list(ListPaymentsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payments.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment and you have its paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/retrieve">Retrieve Payment</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a tip mode, or a date range.</p>
     * <p>Our gateway returns the following information about each payment in the list:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<RetrievedPayment>>>> list(
            ListPaymentsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payments.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment and you have its paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/retrieve">Retrieve Payment</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a tip mode, or a date range.</p>
     * <p>Our gateway returns the following information about each payment in the list:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<RetrievedPayment>>>> list(
            ListPaymentsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments");
        if (request.getProcessingTerminalId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "processingTerminalId",
                    request.getProcessingTerminalId().get(),
                    false);
        }
        if (request.getOrderId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "orderId", request.getOrderId().get(), false);
        }
        if (request.getOperator().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "operator", request.getOperator().get(), false);
        }
        if (request.getCardholderName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cardholderName", request.getCardholderName().get(), false);
        }
        if (request.getFirst6().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first6", request.getFirst6().get(), false);
        }
        if (request.getLast4().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last4", request.getLast4().get(), false);
        }
        if (request.getTender().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tender", request.getTender().get(), false);
        }
        if (request.getDateFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateFrom", request.getDateFrom().get(), false);
        }
        if (request.getDateTo().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateTo", request.getDateTo().get(), false);
        }
        if (request.getSettlementState().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementState", request.getSettlementState().get(), false);
        }
        if (request.getSettlementDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementDate", request.getSettlementDate().get(), false);
        }
        if (request.getPaymentLinkId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "paymentLinkId", request.getPaymentLinkId().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getTipMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tipMode", request.getTipMode().get(), true);
        }
        if (request.getType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "type", request.getType().get(), true);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), true);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<RetrievedPayment>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaymentPaginatedListForRead parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, PaymentPaginatedListForRead.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to run a sale or a pre-authorization with a customer's payment card.
     * <p>In the response, our gateway returns information about the card payment and a paymentId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/retrieve">Retrieve payment</a> - View the details of the card payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/adjust">Adjust payment</a> - Update the details of the card payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/capture">Capture payment</a>  - Capture the pre-authorization.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/reverse">Reverse payment</a>  - Cancel the card payment if it's in an open batch.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund payment</a>  - Run a referenced refund to return funds to the payment card.</li>
     * </ul>
     * <p><strong>Payment methods</strong></p>
     * <ul>
     * <li><strong>Cards</strong> - Credit, debit, and EBT</li>
     * <li><strong>Digital wallets</strong> - <a href="https://docs.payroc.com/guides/integrate/apple-pay">Apple Pay®</a> and <a href="https://docs.payroc.com/guides/integrate/google-pay">Google Pay®</a></li>
     * <li><strong>Tokens</strong> - Secure tokens and single-use tokens</li>
     * </ul>
     * <p><strong>Features</strong></p>
     * <p>Our Create Payment method also supports the following features:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/guides/integrate/repeat-payments/use-your-own-software">Repeat payments</a> - Run multiple payments as part of a payment schedule that you manage with your own software.</li>
     * <li><strong>Offline sales</strong> - Run a sale or a pre-authorization if the terminal loses its connection to our gateway.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/save-payment-details">Tokenization</a> - Save card details to use in future transactions.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/3-d-secure">3-D Secure</a> - Verify the identity of the cardholder.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/add-custom-fields">Custom fields</a> - Add your own data to a payment.</li>
     * <li><strong>Tips</strong> - Add tips to the card payment.</li>
     * <li><strong>Taxes</strong> - Add local taxes to the card payment.</li>
     * <li><strong>Surcharging</strong> - Add a surcharge to the card payment.</li>
     * <li><strong>Dual pricing</strong> - Offer different prices based on payment method, for example, if you use our RewardPay Choice pricing program.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> create(PaymentRequest request) {
        return create(request, null);
    }

    /**
     * Use this method to run a sale or a pre-authorization with a customer's payment card.
     * <p>In the response, our gateway returns information about the card payment and a paymentId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/retrieve">Retrieve payment</a> - View the details of the card payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/adjust">Adjust payment</a> - Update the details of the card payment.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/payments/capture">Capture payment</a>  - Capture the pre-authorization.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/reverse">Reverse payment</a>  - Cancel the card payment if it's in an open batch.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund payment</a>  - Run a referenced refund to return funds to the payment card.</li>
     * </ul>
     * <p><strong>Payment methods</strong></p>
     * <ul>
     * <li><strong>Cards</strong> - Credit, debit, and EBT</li>
     * <li><strong>Digital wallets</strong> - <a href="https://docs.payroc.com/guides/integrate/apple-pay">Apple Pay®</a> and <a href="https://docs.payroc.com/guides/integrate/google-pay">Google Pay®</a></li>
     * <li><strong>Tokens</strong> - Secure tokens and single-use tokens</li>
     * </ul>
     * <p><strong>Features</strong></p>
     * <p>Our Create Payment method also supports the following features:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/guides/integrate/repeat-payments/use-your-own-software">Repeat payments</a> - Run multiple payments as part of a payment schedule that you manage with your own software.</li>
     * <li><strong>Offline sales</strong> - Run a sale or a pre-authorization if the terminal loses its connection to our gateway.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/save-payment-details">Tokenization</a> - Save card details to use in future transactions.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/3-d-secure">3-D Secure</a> - Verify the identity of the cardholder.</li>
     * <li><a href="https://docs.payroc.com/guides/integrate/add-custom-fields">Custom fields</a> - Add your own data to a payment.</li>
     * <li><strong>Tips</strong> - Add tips to the card payment.</li>
     * <li><strong>Taxes</strong> - Add local taxes to the card payment.</li>
     * <li><strong>Surcharging</strong> - Add a surcharge to the card payment.</li>
     * <li><strong>Dual pricing</strong> - Offer different prices based on payment method, for example, if you use our RewardPay Choice pricing program.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> create(
            PaymentRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Payment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Payment.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a card payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>If the merchant saved the customer's card details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<RetrievedPayment>> retrieve(String paymentId) {
        return retrieve(paymentId, RetrievePaymentsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a card payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>If the merchant saved the customer's card details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<RetrievedPayment>> retrieve(
            String paymentId, RetrievePaymentsRequest request) {
        return retrieve(paymentId, request, null);
    }

    /**
     * Use this method to retrieve information about a card payment.
     * <p>To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Our gateway returns the following information about the payment:</p>
     * <ul>
     * <li>Order details, including the transaction amount and when it was processed.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * <li>Payment details, including the payment type, status, and response.</li>
     * </ul>
     * <p>If the merchant saved the customer's card details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<RetrievedPayment>> retrieve(
            String paymentId, RetrievePaymentsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .addPathSegment(paymentId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<RetrievedPayment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievedPayment.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to adjust a payment in an open batch.
     * <p>To adjust a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>You can adjust the following details of the payment:</p>
     * <ul>
     * <li>Sale amount and tip amount</li>
     * <li>Payment status</li>
     * <li>Cardholder shipping address and contact information</li>
     * <li>Cardholder signature data</li>
     * </ul>
     * <p>Our gateway returns information about the adjusted payment, including information about the payment card and the cardholder.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> adjust(String paymentId, PaymentAdjustment request) {
        return adjust(paymentId, request, null);
    }

    /**
     * Use this method to adjust a payment in an open batch.
     * <p>To adjust a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>You can adjust the following details of the payment:</p>
     * <ul>
     * <li>Sale amount and tip amount</li>
     * <li>Payment status</li>
     * <li>Cardholder shipping address and contact information</li>
     * <li>Cardholder signature data</li>
     * </ul>
     * <p>Our gateway returns information about the adjusted payment, including information about the payment card and the cardholder.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> adjust(
            String paymentId, PaymentAdjustment request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .addPathSegment(paymentId)
                .addPathSegments("adjust")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Payment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Payment.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to capture a pre-authorization.
     * <p>To capture a pre-authorization, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Depending on the amount you want to capture, complete the following:</p>
     * <ul>
     * <li><strong>Capture the full amount of the pre-authorization</strong> - Don't send a value for the amount parameter in your request.</li>
     * <li><strong>Capture less than the amount of the pre-authorization</strong> - Send a value for the amount parameter in your request.</li>
     * <li><strong>Capture more than the amount of the pre-authorization</strong> - Adjust the pre-authorization before you capture it. For more information about adjusting a pre-authorization, go to <a href="https://docs.payroc.com/api/schema/card-payments/payments/adjust">Adjust Payment</a>.</li>
     * </ul>
     * <p>If your request is successful, our gateway takes the amount from the payment card.</p>
     * <p><strong>Note:</strong> For more information about pre-authorizations and captures, go to <a href="https://docs.payroc.com/guides/integrate/run-a-pre-authorization">Run a pre-authorization</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> capture(String paymentId, PaymentCapture request) {
        return capture(paymentId, request, null);
    }

    /**
     * Use this method to capture a pre-authorization.
     * <p>To capture a pre-authorization, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>Depending on the amount you want to capture, complete the following:</p>
     * <ul>
     * <li><strong>Capture the full amount of the pre-authorization</strong> - Don't send a value for the amount parameter in your request.</li>
     * <li><strong>Capture less than the amount of the pre-authorization</strong> - Send a value for the amount parameter in your request.</li>
     * <li><strong>Capture more than the amount of the pre-authorization</strong> - Adjust the pre-authorization before you capture it. For more information about adjusting a pre-authorization, go to <a href="https://docs.payroc.com/api/schema/card-payments/payments/adjust">Adjust Payment</a>.</li>
     * </ul>
     * <p>If your request is successful, our gateway takes the amount from the payment card.</p>
     * <p><strong>Note:</strong> For more information about pre-authorizations and captures, go to <a href="https://docs.payroc.com/guides/integrate/run-a-pre-authorization">Run a pre-authorization</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Payment>> capture(
            String paymentId, PaymentCapture request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .addPathSegment(paymentId)
                .addPathSegments("capture")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Payment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Payment.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
