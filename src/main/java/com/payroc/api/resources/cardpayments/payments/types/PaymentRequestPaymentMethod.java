/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.cardpayments.payments.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.CardPayload;
import com.payroc.api.types.DigitalWalletPayload;
import com.payroc.api.types.SecureTokenPayload;
import com.payroc.api.types.SingleUseTokenPayload;
import java.util.Objects;
import java.util.Optional;

public final class PaymentRequestPaymentMethod {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentRequestPaymentMethod(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentRequestPaymentMethod card(CardPayload value) {
        return new PaymentRequestPaymentMethod(new CardValue(value));
    }

    public static PaymentRequestPaymentMethod secureToken(SecureTokenPayload value) {
        return new PaymentRequestPaymentMethod(new SecureTokenValue(value));
    }

    public static PaymentRequestPaymentMethod digitalWallet(DigitalWalletPayload value) {
        return new PaymentRequestPaymentMethod(new DigitalWalletValue(value));
    }

    public static PaymentRequestPaymentMethod singleUseToken(SingleUseTokenPayload value) {
        return new PaymentRequestPaymentMethod(new SingleUseTokenValue(value));
    }

    public boolean isCard() {
        return value instanceof CardValue;
    }

    public boolean isSecureToken() {
        return value instanceof SecureTokenValue;
    }

    public boolean isDigitalWallet() {
        return value instanceof DigitalWalletValue;
    }

    public boolean isSingleUseToken() {
        return value instanceof SingleUseTokenValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CardPayload> getCard() {
        if (isCard()) {
            return Optional.of(((CardValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SecureTokenPayload> getSecureToken() {
        if (isSecureToken()) {
            return Optional.of(((SecureTokenValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<DigitalWalletPayload> getDigitalWallet() {
        if (isDigitalWallet()) {
            return Optional.of(((DigitalWalletValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SingleUseTokenPayload> getSingleUseToken() {
        if (isSingleUseToken()) {
            return Optional.of(((SingleUseTokenValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCard(CardPayload card);

        T visitSecureToken(SecureTokenPayload secureToken);

        T visitDigitalWallet(DigitalWalletPayload digitalWallet);

        T visitSingleUseToken(SingleUseTokenPayload singleUseToken);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(CardValue.class),
        @JsonSubTypes.Type(SecureTokenValue.class),
        @JsonSubTypes.Type(DigitalWalletValue.class),
        @JsonSubTypes.Type(SingleUseTokenValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("card")
    @JsonIgnoreProperties("type")
    private static final class CardValue implements Value {
        @JsonUnwrapped
        private CardPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CardValue() {}

        private CardValue(CardPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCard(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CardValue && equalTo((CardValue) other);
        }

        private boolean equalTo(CardValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("secureToken")
    @JsonIgnoreProperties("type")
    private static final class SecureTokenValue implements Value {
        @JsonUnwrapped
        private SecureTokenPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SecureTokenValue() {}

        private SecureTokenValue(SecureTokenPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSecureToken(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SecureTokenValue && equalTo((SecureTokenValue) other);
        }

        private boolean equalTo(SecureTokenValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("digitalWallet")
    @JsonIgnoreProperties("type")
    private static final class DigitalWalletValue implements Value {
        @JsonUnwrapped
        private DigitalWalletPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DigitalWalletValue() {}

        private DigitalWalletValue(DigitalWalletPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDigitalWallet(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DigitalWalletValue && equalTo((DigitalWalletValue) other);
        }

        private boolean equalTo(DigitalWalletValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("singleUseToken")
    @JsonIgnoreProperties("type")
    private static final class SingleUseTokenValue implements Value {
        @JsonUnwrapped
        private SingleUseTokenPayload value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SingleUseTokenValue() {}

        private SingleUseTokenValue(SingleUseTokenPayload value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSingleUseToken(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SingleUseTokenValue && equalTo((SingleUseTokenValue) other);
        }

        private boolean equalTo(SingleUseTokenValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestPaymentMethod{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestPaymentMethod{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
