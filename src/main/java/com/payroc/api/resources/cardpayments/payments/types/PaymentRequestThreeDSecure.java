/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.cardpayments.payments.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.GatewayThreeDSecure;
import com.payroc.api.types.ThirdPartyThreeDSecure;
import java.util.Objects;
import java.util.Optional;

public final class PaymentRequestThreeDSecure {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentRequestThreeDSecure(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentRequestThreeDSecure gateway(GatewayThreeDSecure value) {
        return new PaymentRequestThreeDSecure(new GatewayValue(value));
    }

    public static PaymentRequestThreeDSecure thirdParty(ThirdPartyThreeDSecure value) {
        return new PaymentRequestThreeDSecure(new ThirdPartyValue(value));
    }

    public boolean isGateway() {
        return value instanceof GatewayValue;
    }

    public boolean isThirdParty() {
        return value instanceof ThirdPartyValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<GatewayThreeDSecure> getGateway() {
        if (isGateway()) {
            return Optional.of(((GatewayValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ThirdPartyThreeDSecure> getThirdParty() {
        if (isThirdParty()) {
            return Optional.of(((ThirdPartyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitGateway(GatewayThreeDSecure gateway);

        T visitThirdParty(ThirdPartyThreeDSecure thirdParty);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "serviceProvider",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(GatewayValue.class), @JsonSubTypes.Type(ThirdPartyValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("gateway")
    @JsonIgnoreProperties("serviceProvider")
    private static final class GatewayValue implements Value {
        @JsonUnwrapped
        private GatewayThreeDSecure value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private GatewayValue() {}

        private GatewayValue(GatewayThreeDSecure value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitGateway(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof GatewayValue && equalTo((GatewayValue) other);
        }

        private boolean equalTo(GatewayValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestThreeDSecure{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("thirdParty")
    @JsonIgnoreProperties("serviceProvider")
    private static final class ThirdPartyValue implements Value {
        @JsonUnwrapped
        private ThirdPartyThreeDSecure value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ThirdPartyValue() {}

        private ThirdPartyValue(ThirdPartyThreeDSecure value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitThirdParty(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ThirdPartyValue && equalTo((ThirdPartyValue) other);
        }

        private boolean equalTo(ThirdPartyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestThreeDSecure{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("serviceProvider")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentRequestThreeDSecure{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
