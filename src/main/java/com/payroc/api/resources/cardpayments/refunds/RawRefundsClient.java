/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.cardpayments.refunds;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.PayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.cardpayments.refunds.requests.ListRefundsRequest;
import com.payroc.api.resources.cardpayments.refunds.requests.PaymentReversal;
import com.payroc.api.resources.cardpayments.refunds.requests.ReferencedRefund;
import com.payroc.api.resources.cardpayments.refunds.requests.RefundAdjustment;
import com.payroc.api.resources.cardpayments.refunds.requests.RetrieveRefundsRequest;
import com.payroc.api.resources.cardpayments.refunds.requests.ReverseRefundRefundsRequest;
import com.payroc.api.resources.cardpayments.refunds.requests.UnreferencedRefund;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.Payment;
import com.payroc.api.types.RefundPaginatedList;
import com.payroc.api.types.RetrievedRefund;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawRefundsClient {
    protected final ClientOptions clientOptions;

    public RawRefundsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to cancel or to partially cancel a payment in an open batch. This is also known as voiding a payment.
     * <p>To cancel a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>If your request is successful, our gateway removes the payment from the merchant's open batch and no funds are taken from the cardholder's account.</p>
     */
    public PayrocApiHttpResponse<Payment> reverse(String paymentId, PaymentReversal request) {
        return reverse(paymentId, request, null);
    }

    /**
     * Use this method to cancel or to partially cancel a payment in an open batch. This is also known as voiding a payment.
     * <p>To cancel a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>If your request is successful, our gateway removes the payment from the merchant's open batch and no funds are taken from the cardholder's account.</p>
     */
    public PayrocApiHttpResponse<Payment> reverse(
            String paymentId, PaymentReversal request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .addPathSegment(paymentId)
                .addPathSegments("reverse");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Payment.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to refund a payment that is in a closed batch.
     * <p>To refund a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>If your refund is successful, our gateway returns the payment amount to the cardholder's account.</p>
     * <p><strong>Things to consider</strong></p>
     * <ul>
     * <li>If the merchant refunds a payment that is in an open batch, our gateway reverses the payment.</li>
     * <li>Some merchants can run unreferenced refunds, which means that they don't need a paymentId to return an amount to a customer. For more information about how to run an unreferenced refund, go to <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a>.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Payment> createReferencedRefund(String paymentId, ReferencedRefund request) {
        return createReferencedRefund(paymentId, request, null);
    }

    /**
     * Use this method to refund a payment that is in a closed batch.
     * <p>To refund a payment, you need its paymentId. Our gateway returned the paymentId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">Create Payment</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentId, use our <a href="https://docs.payroc.com/api/schema/card-payments/payments/list">List Payments</a> method to search for the payment.</p>
     * <p>If your refund is successful, our gateway returns the payment amount to the cardholder's account.</p>
     * <p><strong>Things to consider</strong></p>
     * <ul>
     * <li>If the merchant refunds a payment that is in an open batch, our gateway reverses the payment.</li>
     * <li>Some merchants can run unreferenced refunds, which means that they don't need a paymentId to return an amount to a customer. For more information about how to run an unreferenced refund, go to <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a>.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Payment> createReferencedRefund(
            String paymentId, ReferencedRefund request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payments")
                .addPathSegment(paymentId)
                .addPathSegments("refund");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Payment.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of refunds.
     * <p><strong>Note:</strong> If you want to view the details of a specific refund and you have its refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve Refund</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for refunds for a customer, a tender type, or a date range.
     * Our gateway returns the following information about each refund in the list:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>For referenced refunds, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<RetrievedRefund>> list() {
        return list(ListRefundsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of refunds.
     * <p><strong>Note:</strong> If you want to view the details of a specific refund and you have its refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve Refund</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for refunds for a customer, a tender type, or a date range.
     * Our gateway returns the following information about each refund in the list:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>For referenced refunds, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<RetrievedRefund>> list(RequestOptions requestOptions) {
        return list(ListRefundsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of refunds.
     * <p><strong>Note:</strong> If you want to view the details of a specific refund and you have its refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve Refund</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for refunds for a customer, a tender type, or a date range.
     * Our gateway returns the following information about each refund in the list:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>For referenced refunds, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<RetrievedRefund>> list(ListRefundsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of refunds.
     * <p><strong>Note:</strong> If you want to view the details of a specific refund and you have its refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve Refund</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for refunds for a customer, a tender type, or a date range.
     * Our gateway returns the following information about each refund in the list:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>For referenced refunds, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<RetrievedRefund>> list(
            ListRefundsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("refunds");
        if (request.getProcessingTerminalId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "processingTerminalId",
                    request.getProcessingTerminalId().get(),
                    false);
        }
        if (request.getOrderId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "orderId", request.getOrderId().get(), false);
        }
        if (request.getOperator().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "operator", request.getOperator().get(), false);
        }
        if (request.getCardholderName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cardholderName", request.getCardholderName().get(), false);
        }
        if (request.getFirst6().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first6", request.getFirst6().get(), false);
        }
        if (request.getLast4().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last4", request.getLast4().get(), false);
        }
        if (request.getTender().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "tender", request.getTender().get(), false);
        }
        if (request.getDateFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateFrom", request.getDateFrom().get(), false);
        }
        if (request.getDateTo().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dateTo", request.getDateTo().get(), false);
        }
        if (request.getSettlementState().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementState", request.getSettlementState().get(), false);
        }
        if (request.getSettlementDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "settlementDate", request.getSettlementDate().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                RefundPaginatedList parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RefundPaginatedList.class);
                return new PayrocApiHttpResponse<>(
                        PayrocPager.create(parsedResponse, clientOptions, requestOptions), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to create an unreferenced refund. An unreferenced refund is a refund that isn't linked to a payment.
     * <p><strong>Note:</strong> If you have the paymentId of the payment you want to refund, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method. If you use our Refund Payment method, our gateway sends the refund amount to the customer's original payment method and links the refund to the payment.</p>
     * <p>In the request, you must provide the customer's payment details and the refund amount.</p>
     * <p>In the response, our gateway returns information about the refund and a refundId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve refund</a> - View the details of the refund.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/adjust">Adjust refund</a> - Update the details of the refund.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/reverse-refund">Reverse refund</a> - Cancel the refund if it's in an open batch.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<RetrievedRefund> createUnreferencedRefund(UnreferencedRefund request) {
        return createUnreferencedRefund(request, null);
    }

    /**
     * Use this method to create an unreferenced refund. An unreferenced refund is a refund that isn't linked to a payment.
     * <p><strong>Note:</strong> If you have the paymentId of the payment you want to refund, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method. If you use our Refund Payment method, our gateway sends the refund amount to the customer's original payment method and links the refund to the payment.</p>
     * <p>In the request, you must provide the customer's payment details and the refund amount.</p>
     * <p>In the response, our gateway returns information about the refund and a refundId, which you need for the following methods:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/retrieve">Retrieve refund</a> - View the details of the refund.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/adjust">Adjust refund</a> - Update the details of the refund.</li>
     * <li><a href="https://docs.payroc.com/api/schema/card-payments/refunds/reverse-refund">Reverse refund</a> - Cancel the refund if it's in an open batch.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<RetrievedRefund> createUnreferencedRefund(
            UnreferencedRefund request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("refunds");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievedRefund.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to retrieve information about a refund.
     * <p>To retrieve a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>Our gateway returns the following information about the refund:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> retrieve(String refundId) {
        return retrieve(refundId, RetrieveRefundsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a refund.
     * <p>To retrieve a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>Our gateway returns the following information about the refund:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> retrieve(String refundId, RequestOptions requestOptions) {
        return retrieve(refundId, RetrieveRefundsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a refund.
     * <p>To retrieve a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>Our gateway returns the following information about the refund:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> retrieve(String refundId, RetrieveRefundsRequest request) {
        return retrieve(refundId, request, null);
    }

    /**
     * Use this method to retrieve information about a refund.
     * <p>To retrieve a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>Our gateway returns the following information about the refund:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> retrieve(
            String refundId, RetrieveRefundsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("refunds")
                .addPathSegment(refundId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievedRefund.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to adjust a refund in an open batch.
     * <p>To adjust a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>You can adjust the following details of the refund:</p>
     * <ul>
     * <li>Customer details, including shipping address and contact information.</li>
     * <li>Status of the refund.</li>
     * </ul>
     * <p>Our gateway returns information about the adjusted refund, including:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> adjust(String refundId, RefundAdjustment request) {
        return adjust(refundId, request, null);
    }

    /**
     * Use this method to adjust a refund in an open batch.
     * <p>To adjust a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> method or the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>You can adjust the following details of the refund:</p>
     * <ul>
     * <li>Customer details, including shipping address and contact information.</li>
     * <li>Status of the refund.</li>
     * </ul>
     * <p>Our gateway returns information about the adjusted refund, including:</p>
     * <ul>
     * <li>Order details, including the refund amount and when we processed the refund.</li>
     * <li>Payment card details, including the masked card number, expiry date, and payment method.</li>
     * <li>Cardholder details, including their contact information and shipping address.</li>
     * </ul>
     * <p>If the refund is a referenced refund, our gateway also returns details about the payment that the refund is linked to.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> adjust(
            String refundId, RefundAdjustment request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("refunds")
                .addPathSegment(refundId)
                .addPathSegments("adjust");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievedRefund.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to cancel a refund in an open batch.
     * <p>To cancel a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> or <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>If your request is successful, the gateway removes the refund from the merchant’s open batch and no funds are returned to the cardholder’s account.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> reverseRefund(String refundId, ReverseRefundRefundsRequest request) {
        return reverseRefund(refundId, request, null);
    }

    /**
     * Use this method to cancel a refund in an open batch.
     * <p>To cancel a refund, you need its refundId. Our gateway returned the refundId in the response of the <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-referenced-refund">Refund Payment</a> or <a href="https://docs.payroc.com/api/schema/card-payments/refunds/create-unreferenced-refund">Create Refund</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the refundId, use our <a href="https://docs.payroc.com/api/schema/card-payments/refunds/list">List Refunds</a> method to search for the refund.</p>
     * <p>If your request is successful, the gateway removes the refund from the merchant’s open batch and no funds are returned to the cardholder’s account.</p>
     */
    public PayrocApiHttpResponse<RetrievedRefund> reverseRefund(
            String refundId, ReverseRefundRefundsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("refunds")
                .addPathSegment(refundId)
                .addPathSegments("reverse");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievedRefund.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
