/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.cardpayments.refunds.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.CustomerAdjustment;
import com.payroc.api.types.StatusAdjustment;
import java.util.Objects;
import java.util.Optional;

public final class RefundAdjustmentAdjustmentsItem {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private RefundAdjustmentAdjustmentsItem(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static RefundAdjustmentAdjustmentsItem status(StatusAdjustment value) {
        return new RefundAdjustmentAdjustmentsItem(new StatusValue(value));
    }

    public static RefundAdjustmentAdjustmentsItem customer(CustomerAdjustment value) {
        return new RefundAdjustmentAdjustmentsItem(new CustomerValue(value));
    }

    public boolean isStatus() {
        return value instanceof StatusValue;
    }

    public boolean isCustomer() {
        return value instanceof CustomerValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<StatusAdjustment> getStatus() {
        if (isStatus()) {
            return Optional.of(((StatusValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CustomerAdjustment> getCustomer() {
        if (isCustomer()) {
            return Optional.of(((CustomerValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitStatus(StatusAdjustment status);

        T visitCustomer(CustomerAdjustment customer);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(StatusValue.class), @JsonSubTypes.Type(CustomerValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("status")
    @JsonIgnoreProperties("type")
    private static final class StatusValue implements Value {
        @JsonUnwrapped
        private StatusAdjustment value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StatusValue() {}

        private StatusValue(StatusAdjustment value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStatus(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StatusValue && equalTo((StatusValue) other);
        }

        private boolean equalTo(StatusValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RefundAdjustmentAdjustmentsItem{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("customer")
    @JsonIgnoreProperties("type")
    private static final class CustomerValue implements Value {
        @JsonUnwrapped
        private CustomerAdjustment value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CustomerValue() {}

        private CustomerValue(CustomerAdjustment value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCustomer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CustomerValue && equalTo((CustomerValue) other);
        }

        private boolean equalTo(CustomerValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RefundAdjustmentAdjustmentsItem{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RefundAdjustmentAdjustmentsItem{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
