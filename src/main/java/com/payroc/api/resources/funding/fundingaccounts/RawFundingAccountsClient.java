/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.funding.fundingaccounts;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.PayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.funding.fundingaccounts.requests.DeleteFundingAccountsRequest;
import com.payroc.api.resources.funding.fundingaccounts.requests.ListFundingAccountsRequest;
import com.payroc.api.resources.funding.fundingaccounts.requests.RetrieveFundingAccountsRequest;
import com.payroc.api.resources.funding.fundingaccounts.requests.UpdateFundingAccountsRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.FundingAccount;
import com.payroc.api.types.ListFundingAccounts;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawFundingAccountsClient {
    protected final ClientOptions clientOptions;

    public RawFundingAccountsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding accounts associated with your account.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>Our gateway returns the following information about each funding account in the list:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     * <p>For each funding account, we also return the fundingAccountId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<FundingAccount>> list() {
        return list(ListFundingAccountsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding accounts associated with your account.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>Our gateway returns the following information about each funding account in the list:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     * <p>For each funding account, we also return the fundingAccountId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<FundingAccount>> list(RequestOptions requestOptions) {
        return list(ListFundingAccountsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding accounts associated with your account.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>Our gateway returns the following information about each funding account in the list:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     * <p>For each funding account, we also return the fundingAccountId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<FundingAccount>> list(ListFundingAccountsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding accounts associated with your account.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>Our gateway returns the following information about each funding account in the list:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     * <p>For each funding account, we also return the fundingAccountId, which you can use to perform follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<FundingAccount>> list(
            ListFundingAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-accounts");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListFundingAccounts parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListFundingAccounts.class);
                return new PayrocApiHttpResponse<>(
                        PayrocPager.create(parsedResponse, clientOptions, requestOptions), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to retrieve information about a funding account.
     * <p>To retrieve a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the account.</p>
     * <p>Our gateway returns the following information about the funding account:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<FundingAccount> retrieve(int fundingAccountId) {
        return retrieve(
                fundingAccountId, RetrieveFundingAccountsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a funding account.
     * <p>To retrieve a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the account.</p>
     * <p>Our gateway returns the following information about the funding account:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<FundingAccount> retrieve(int fundingAccountId, RequestOptions requestOptions) {
        return retrieve(
                fundingAccountId, RetrieveFundingAccountsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a funding account.
     * <p>To retrieve a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the account.</p>
     * <p>Our gateway returns the following information about the funding account:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<FundingAccount> retrieve(
            int fundingAccountId, RetrieveFundingAccountsRequest request) {
        return retrieve(fundingAccountId, request, null);
    }

    /**
     * Use this method to retrieve information about a funding account.
     * <p>To retrieve a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the account.</p>
     * <p>Our gateway returns the following information about the funding account:</p>
     * <ul>
     * <li>Name of the account holder and ACH details for the account.</li>
     * <li>Status of the account.</li>
     * <li>Whether we send funds to the account, withdraw funds from the account, or both.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<FundingAccount> retrieve(
            int fundingAccountId, RetrieveFundingAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-accounts")
                .addPathSegment(Integer.toString(fundingAccountId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FundingAccount.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't update the details of a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to update the details of a funding account that is associated with a funding recipient.</p>
     * <p>To update a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don’t have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     * <p>You can update the following details about the funding account:</p>
     * <ul>
     * <li>Account type.</li>
     * <li>Account holder's name.</li>
     * <li>ACH information for the account.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Void> update(int fundingAccountId, UpdateFundingAccountsRequest request) {
        return update(fundingAccountId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't update the details of a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to update the details of a funding account that is associated with a funding recipient.</p>
     * <p>To update a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don’t have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     * <p>You can update the following details about the funding account:</p>
     * <ul>
     * <li>Account type.</li>
     * <li>Account holder's name.</li>
     * <li>ACH information for the account.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<Void> update(
            int fundingAccountId, UpdateFundingAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-accounts")
                .addPathSegment(Integer.toString(fundingAccountId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to delete a funding account that is associated with a funding recipient.</p>
     * <p>To delete a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     */
    public PayrocApiHttpResponse<Void> delete(int fundingAccountId) {
        return delete(fundingAccountId, DeleteFundingAccountsRequest.builder().build());
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to delete a funding account that is associated with a funding recipient.</p>
     * <p>To delete a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     */
    public PayrocApiHttpResponse<Void> delete(int fundingAccountId, RequestOptions requestOptions) {
        return delete(fundingAccountId, DeleteFundingAccountsRequest.builder().build(), requestOptions);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to delete a funding account that is associated with a funding recipient.</p>
     * <p>To delete a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     */
    public PayrocApiHttpResponse<Void> delete(int fundingAccountId, DeleteFundingAccountsRequest request) {
        return delete(fundingAccountId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can't delete a funding account that is associated with a processing account.
     * </blockquote>
     * <p>Use this method to delete a funding account that is associated with a funding recipient.</p>
     * <p>To delete a funding account, you need its fundingAccountId. Our gateway returned the fundingAccountId when you created the funding account.</p>
     * <p><strong>Note:</strong> If you don't have the fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/list">List Funding Accounts</a> method to search for the funding account.</p>
     */
    public PayrocApiHttpResponse<Void> delete(
            int fundingAccountId, DeleteFundingAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-accounts")
                .addPathSegment(Integer.toString(fundingAccountId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
