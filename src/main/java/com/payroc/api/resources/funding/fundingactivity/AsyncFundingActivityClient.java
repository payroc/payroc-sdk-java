/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.funding.fundingactivity;

import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.resources.funding.fundingactivity.requests.ListFundingActivityRequest;
import com.payroc.api.resources.funding.fundingactivity.requests.RetrieveBalanceFundingActivityRequest;
import com.payroc.api.resources.funding.fundingactivity.types.RetrieveBalanceFundingActivityResponse;
import com.payroc.api.types.ActivityRecord;
import java.util.concurrent.CompletableFuture;

public class AsyncFundingActivityClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawFundingActivityClient rawClient;

    public AsyncFundingActivityClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawFundingActivityClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawFundingActivityClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding balances available for each merchant linked to your account.
     * <p>Use query parameters to filter the list of results we return, for example, to search for the funding balance for a specific merchant.</p>
     * <p>Our gateway returns the following information about each merchant in the list:</p>
     * <ul>
     * <li>Total funds for the merchant.</li>
     * <li>Available funds that you can use for funding instructions.</li>
     * <li>Pending funds that we have not yet sent to funding accounts.</li>
     * </ul>
     */
    public CompletableFuture<RetrieveBalanceFundingActivityResponse> retrieveBalance() {
        return this.rawClient.retrieveBalance().thenApply(response -> response.body());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding balances available for each merchant linked to your account.
     * <p>Use query parameters to filter the list of results we return, for example, to search for the funding balance for a specific merchant.</p>
     * <p>Our gateway returns the following information about each merchant in the list:</p>
     * <ul>
     * <li>Total funds for the merchant.</li>
     * <li>Available funds that you can use for funding instructions.</li>
     * <li>Pending funds that we have not yet sent to funding accounts.</li>
     * </ul>
     */
    public CompletableFuture<RetrieveBalanceFundingActivityResponse> retrieveBalance(RequestOptions requestOptions) {
        return this.rawClient.retrieveBalance(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding balances available for each merchant linked to your account.
     * <p>Use query parameters to filter the list of results we return, for example, to search for the funding balance for a specific merchant.</p>
     * <p>Our gateway returns the following information about each merchant in the list:</p>
     * <ul>
     * <li>Total funds for the merchant.</li>
     * <li>Available funds that you can use for funding instructions.</li>
     * <li>Pending funds that we have not yet sent to funding accounts.</li>
     * </ul>
     */
    public CompletableFuture<RetrieveBalanceFundingActivityResponse> retrieveBalance(
            RetrieveBalanceFundingActivityRequest request) {
        return this.rawClient.retrieveBalance(request).thenApply(response -> response.body());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding balances available for each merchant linked to your account.
     * <p>Use query parameters to filter the list of results we return, for example, to search for the funding balance for a specific merchant.</p>
     * <p>Our gateway returns the following information about each merchant in the list:</p>
     * <ul>
     * <li>Total funds for the merchant.</li>
     * <li>Available funds that you can use for funding instructions.</li>
     * <li>Pending funds that we have not yet sent to funding accounts.</li>
     * </ul>
     */
    public CompletableFuture<RetrieveBalanceFundingActivityResponse> retrieveBalance(
            RetrieveBalanceFundingActivityRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieveBalance(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of activity associated with your merchants' funding balances within a specific date range.
     * <p>Use query parameters to filter the list of results we return, for example, to view the activity for a specific merchant's funding balance.</p>
     * <p>Our gateway returns the following information about each activity in the list:</p>
     * <ul>
     * <li>Name of the merchant who owns the funding balance.</li>
     * <li>Amount of funds added or removed from the funding balance.</li>
     * <li>Funding account that received funds from the funding balance.</li>
     * </ul>
     */
    public CompletableFuture<CompletableFuture<AsyncPayrocPager<ActivityRecord>>> list(
            ListFundingActivityRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of activity associated with your merchants' funding balances within a specific date range.
     * <p>Use query parameters to filter the list of results we return, for example, to view the activity for a specific merchant's funding balance.</p>
     * <p>Our gateway returns the following information about each activity in the list:</p>
     * <ul>
     * <li>Name of the merchant who owns the funding balance.</li>
     * <li>Amount of funds added or removed from the funding balance.</li>
     * <li>Funding account that received funds from the funding balance.</li>
     * </ul>
     */
    public CompletableFuture<CompletableFuture<AsyncPayrocPager<ActivityRecord>>> list(
            ListFundingActivityRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }
}
