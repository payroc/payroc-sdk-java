/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.funding.fundinginstructions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.funding.fundinginstructions.requests.CreateFundingInstructionsRequest;
import com.payroc.api.resources.funding.fundinginstructions.requests.DeleteFundingInstructionsRequest;
import com.payroc.api.resources.funding.fundinginstructions.requests.ListFundingInstructionsRequest;
import com.payroc.api.resources.funding.fundinginstructions.requests.RetrieveFundingInstructionsRequest;
import com.payroc.api.resources.funding.fundinginstructions.requests.UpdateFundingInstructionsRequest;
import com.payroc.api.resources.funding.fundinginstructions.types.ListFundingInstructionsResponse;
import com.payroc.api.resources.funding.fundinginstructions.types.ListFundingInstructionsResponseDataItem;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.Instruction;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawFundingInstructionsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawFundingInstructionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * <blockquote>
     * Important: You can return a list of funding instructions from only the previous two years. If you want to view a funding instruction from more than two years ago and you have its instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding instructions within a specific date range.</p>
     * <p><strong>Note:</strong> If you want to view the details of a specific funding instruction and you have its instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.</p>
     * <p>Our gateway returns the following information for each instruction in the list:</p>
     * <ul>
     * <li>Status of the funding instruction.</li>
     * <li>Funding information, including which merchant's funding balance we distribute and the funding account that we send the balance to.</li>
     * </ul>
     * <p>For each funding instruction, we also return the instructionId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<
                    PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ListFundingInstructionsResponseDataItem>>>>
            list(ListFundingInstructionsRequest request) {
        return list(request, null);
    }

    /**
     * <blockquote>
     * Important: You can return a list of funding instructions from only the previous two years. If you want to view a funding instruction from more than two years ago and you have its instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding instructions within a specific date range.</p>
     * <p><strong>Note:</strong> If you want to view the details of a specific funding instruction and you have its instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.</p>
     * <p>Our gateway returns the following information for each instruction in the list:</p>
     * <ul>
     * <li>Status of the funding instruction.</li>
     * <li>Funding information, including which merchant's funding balance we distribute and the funding account that we send the balance to.</li>
     * </ul>
     * <p>For each funding instruction, we also return the instructionId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<
                    PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<ListFundingInstructionsResponseDataItem>>>>
            list(ListFundingInstructionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-instructions");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo(), false);
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<
                        PayrocApiHttpResponse<
                                CompletableFuture<AsyncPayrocPager<ListFundingInstructionsResponseDataItem>>>>
                future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListFundingInstructionsResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListFundingInstructionsResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create a funding instruction that tells us how to distribute the funds from your merchants' transactions.
     * <p><strong>Note:</strong> Before you create a funding instruction, you can use our <a href="https://docs.payroc.com/api/schema/funding/funding-activity/retrieve-balance">List Funding Balances</a> method to view the amount of available funds that a merchant has.</p>
     * <p>In your request, include an array of merchantInstruction objects. Each merchantInstruction object contains the following:</p>
     * <ul>
     * <li>Merchant ID (MID) of the merchant whose funding balance you want to distribute.</li>
     * <li>Funding account that you want to send funds to.</li>
     * <li>Amount that you want to send to the funding account.</li>
     * </ul>
     * <p>Our gateway returns the instructionId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Instruction>> create(CreateFundingInstructionsRequest request) {
        return create(request, null);
    }

    /**
     * Use this method to create a funding instruction that tells us how to distribute the funds from your merchants' transactions.
     * <p><strong>Note:</strong> Before you create a funding instruction, you can use our <a href="https://docs.payroc.com/api/schema/funding/funding-activity/retrieve-balance">List Funding Balances</a> method to view the amount of available funds that a merchant has.</p>
     * <p>In your request, include an array of merchantInstruction objects. Each merchantInstruction object contains the following:</p>
     * <ul>
     * <li>Merchant ID (MID) of the merchant whose funding balance you want to distribute.</li>
     * <li>Funding account that you want to send funds to.</li>
     * <li>Amount that you want to send to the funding account.</li>
     * </ul>
     * <p>Our gateway returns the instructionId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Instruction>> create(
            CreateFundingInstructionsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-instructions")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Instruction>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Instruction.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a funding instruction.
     * <p>To retrieve a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     * <p>Our gateway returns the following information about the funding instruction:</p>
     * <ul>
     * <li>Status of the funding instruction.</li>
     * <li>Funding information, including which merchant's funding balance we distribute and the funding account that we send the balance to.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Instruction>> retrieve(int instructionId) {
        return retrieve(
                instructionId, RetrieveFundingInstructionsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a funding instruction.
     * <p>To retrieve a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     * <p>Our gateway returns the following information about the funding instruction:</p>
     * <ul>
     * <li>Status of the funding instruction.</li>
     * <li>Funding information, including which merchant's funding balance we distribute and the funding account that we send the balance to.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Instruction>> retrieve(
            int instructionId, RetrieveFundingInstructionsRequest request) {
        return retrieve(instructionId, request, null);
    }

    /**
     * Use this method to retrieve information about a funding instruction.
     * <p>To retrieve a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     * <p>Our gateway returns the following information about the funding instruction:</p>
     * <ul>
     * <li>Status of the funding instruction.</li>
     * <li>Funding information, including which merchant's funding balance we distribute and the funding account that we send the balance to.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Instruction>> retrieve(
            int instructionId, RetrieveFundingInstructionsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-instructions")
                .addPathSegment(Integer.toString(instructionId))
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Instruction>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Instruction.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can update a funding instruction only if its status is <code>accepted</code>. To view the status of a funding instruction, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to update the details of a funding instruction.</p>
     * <p>To update a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the fundingInstructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     * <p>You can modify the following information for the funding instruction:</p>
     * <ul>
     * <li>Merchant ID (MID) of the merchant whose funding balance you want to distribute.</li>
     * <li>Funding account that you want to send funds to.</li>
     * <li>Amount that you want to send to the funding account.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int instructionId, UpdateFundingInstructionsRequest request) {
        return update(instructionId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can update a funding instruction only if its status is <code>accepted</code>. To view the status of a funding instruction, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to update the details of a funding instruction.</p>
     * <p>To update a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the fundingInstructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     * <p>You can modify the following information for the funding instruction:</p>
     * <ul>
     * <li>Merchant ID (MID) of the merchant whose funding balance you want to distribute.</li>
     * <li>Funding account that you want to send funds to.</li>
     * <li>Amount that you want to send to the funding account.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int instructionId, UpdateFundingInstructionsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-instructions")
                .addPathSegment(Integer.toString(instructionId))
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can delete a funding instruction only if its status is <code>accepted</code>. To view the status of a funding instruction, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to delete a funding instruction.</p>
     * <p>To delete a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int instructionId) {
        return delete(instructionId, DeleteFundingInstructionsRequest.builder().build());
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can delete a funding instruction only if its status is <code>accepted</code>. To view the status of a funding instruction, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to delete a funding instruction.</p>
     * <p>To delete a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int instructionId, DeleteFundingInstructionsRequest request) {
        return delete(instructionId, request, null);
    }

    /**
     * <blockquote>
     * <strong>Important:</strong> You can delete a funding instruction only if its status is <code>accepted</code>. To view the status of a funding instruction, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/retrieve">Retrieve Funding Instruction</a> method.
     * </blockquote>
     * <p>Use this method to delete a funding instruction.</p>
     * <p>To delete a funding instruction, you need its instructionId. Our gateway returned the instructionId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/create">Create Funding Instruction</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the instructionId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-instructions/list">List Funding Instructions</a> method to search for the funding instruction.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int instructionId, DeleteFundingInstructionsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-instructions")
                .addPathSegment(Integer.toString(instructionId))
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
