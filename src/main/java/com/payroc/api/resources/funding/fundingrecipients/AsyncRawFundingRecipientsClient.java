/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.funding.fundingrecipients;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.funding.fundingrecipients.requests.CreateAccountFundingRecipientsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.CreateFundingRecipient;
import com.payroc.api.resources.funding.fundingrecipients.requests.CreateOwnerFundingRecipientsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.DeleteFundingRecipientsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.ListFundingRecipientFundingAccountsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.ListFundingRecipientOwnersRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.ListFundingRecipientsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.RetrieveFundingRecipientsRequest;
import com.payroc.api.resources.funding.fundingrecipients.requests.UpdateFundingRecipientsRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.FundingAccount;
import com.payroc.api.types.FundingRecipient;
import com.payroc.api.types.Owner;
import com.payroc.api.types.PaginatedFundRecipients;
import java.io.IOException;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawFundingRecipientsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawFundingRecipientsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding recipients linked to your account.
     * <p>Note: If you want to view the details of a specific funding recipient and you have its recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method.</p>
     * <p>Our gateway returns the following information about each funding recipient in the list:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     * <p>For each funding recipient, we also return the recipientId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<FundingRecipient>>>> list() {
        return list(ListFundingRecipientsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding recipients linked to your account.
     * <p>Note: If you want to view the details of a specific funding recipient and you have its recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method.</p>
     * <p>Our gateway returns the following information about each funding recipient in the list:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     * <p>For each funding recipient, we also return the recipientId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<FundingRecipient>>>> list(
            RequestOptions requestOptions) {
        return list(ListFundingRecipientsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding recipients linked to your account.
     * <p>Note: If you want to view the details of a specific funding recipient and you have its recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method.</p>
     * <p>Our gateway returns the following information about each funding recipient in the list:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     * <p>For each funding recipient, we also return the recipientId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<FundingRecipient>>>> list(
            ListFundingRecipientsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of funding recipients linked to your account.
     * <p>Note: If you want to view the details of a specific funding recipient and you have its recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/retrieve">Retrieve Funding Recipient</a> method.</p>
     * <p>Our gateway returns the following information about each funding recipient in the list:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     * <p>For each funding recipient, we also return the recipientId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<FundingRecipient>>>> list(
            ListFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<FundingRecipient>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaginatedFundRecipients parsedResponse =
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaginatedFundRecipients.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create a funding recipient.
     * <p>A funding recipient is a business or organization that can receive funds but can't run transactions, for example, a charity.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Legal information, including its tax ID, Doing Business As (DBA) name, and address.</li>
     * <li>Contact information, including the email address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding account details.</li>
     * </ul>
     * <p>Our gateway returns the recipientId of the funding recipient, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> create(CreateFundingRecipient request) {
        return create(request, null);
    }

    /**
     * Use this method to create a funding recipient.
     * <p>A funding recipient is a business or organization that can receive funds but can't run transactions, for example, a charity.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Legal information, including its tax ID, Doing Business As (DBA) name, and address.</li>
     * <li>Contact information, including the email address.</li>
     * <li>Owners' details, including their contact details.</li>
     * <li>Funding account details.</li>
     * </ul>
     * <p>Our gateway returns the recipientId of the funding recipient, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> create(
            CreateFundingRecipient request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FundingRecipient.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a funding recipient.
     * <p>To retrieve a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about the funding recipient:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> retrieve(int recipientId) {
        return retrieve(recipientId, RetrieveFundingRecipientsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a funding recipient.
     * <p>To retrieve a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about the funding recipient:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> retrieve(
            int recipientId, RequestOptions requestOptions) {
        return retrieve(recipientId, RetrieveFundingRecipientsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a funding recipient.
     * <p>To retrieve a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about the funding recipient:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> retrieve(
            int recipientId, RetrieveFundingRecipientsRequest request) {
        return retrieve(recipientId, request, null);
    }

    /**
     * Use this method to retrieve information about a funding recipient.
     * <p>To retrieve a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about the funding recipient:</p>
     * <ul>
     * <li>Tax ID and Doing Business As (DBA) name.</li>
     * <li>Address and contact details.</li>
     * <li>Funding accounts linked to the funding recipient.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> retrieve(
            int recipientId, RetrieveFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<FundingRecipient>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FundingRecipient.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to update the details of a funding recipient. If a request contains significant changes, we might need to re-approve the funding recipient.
     * <p>To update a funding recipient, you need it's recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>You can update the following details of a funding recipient:</p>
     * <ul>
     * <li>Doing Business As (DBA) name</li>
     * <li>Tax ID and charity ID</li>
     * <li>Address and contact methods</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int recipientId, UpdateFundingRecipientsRequest request) {
        return update(recipientId, request, null);
    }

    /**
     * Use this method to update the details of a funding recipient. If a request contains significant changes, we might need to re-approve the funding recipient.
     * <p>To update a funding recipient, you need it's recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>You can update the following details of a funding recipient:</p>
     * <ul>
     * <li>Doing Business As (DBA) name</li>
     * <li>Tax ID and charity ID</li>
     * <li>Address and contact methods</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int recipientId, UpdateFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to delete a funding recipient, including its funding accounts and owners.
     * <p>To delete a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int recipientId) {
        return delete(recipientId, DeleteFundingRecipientsRequest.builder().build());
    }

    /**
     * Use this method to delete a funding recipient, including its funding accounts and owners.
     * <p>To delete a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int recipientId, RequestOptions requestOptions) {
        return delete(recipientId, DeleteFundingRecipientsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to delete a funding recipient, including its funding accounts and owners.
     * <p>To delete a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int recipientId, DeleteFundingRecipientsRequest request) {
        return delete(recipientId, request, null);
    }

    /**
     * Use this method to delete a funding recipient, including its funding accounts and owners.
     * <p>To delete a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note</strong>: If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int recipientId, DeleteFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use  this method to return a list of funding accounts associated with a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>To retrieve the funding accounts associated with a funding recipient, you need the recipientId. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each funding account:</p>
     * <ul>
     * <li>Name of the account holder.</li>
     * <li>ACH details for the account.</li>
     * <li>Status of the account.</li>
     * </ul>
     * <p>Our gateway also returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<FundingAccount>>> listAccounts(int recipientId) {
        return listAccounts(
                recipientId,
                ListFundingRecipientFundingAccountsRequest.builder().build());
    }

    /**
     * Use  this method to return a list of funding accounts associated with a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>To retrieve the funding accounts associated with a funding recipient, you need the recipientId. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each funding account:</p>
     * <ul>
     * <li>Name of the account holder.</li>
     * <li>ACH details for the account.</li>
     * <li>Status of the account.</li>
     * </ul>
     * <p>Our gateway also returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<FundingAccount>>> listAccounts(
            int recipientId, RequestOptions requestOptions) {
        return listAccounts(
                recipientId,
                ListFundingRecipientFundingAccountsRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use  this method to return a list of funding accounts associated with a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>To retrieve the funding accounts associated with a funding recipient, you need the recipientId. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each funding account:</p>
     * <ul>
     * <li>Name of the account holder.</li>
     * <li>ACH details for the account.</li>
     * <li>Status of the account.</li>
     * </ul>
     * <p>Our gateway also returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<FundingAccount>>> listAccounts(
            int recipientId, ListFundingRecipientFundingAccountsRequest request) {
        return listAccounts(recipientId, request, null);
    }

    /**
     * Use  this method to return a list of funding accounts associated with a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific funding account and you have its fundingAccountId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-accounts/retrieve">Retrieve Funding Account</a> method.</p>
     * <p>To retrieve the funding accounts associated with a funding recipient, you need the recipientId. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each funding account:</p>
     * <ul>
     * <li>Name of the account holder.</li>
     * <li>ACH details for the account.</li>
     * <li>Status of the account.</li>
     * </ul>
     * <p>Our gateway also returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<FundingAccount>>> listAccounts(
            int recipientId, ListFundingRecipientFundingAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId))
                .addPathSegments("funding-accounts");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<List<FundingAccount>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<FundingAccount>>() {}),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create a funding account and add it to a funding recipient.
     * <p>To add a funding account to a funding recipient, you need the recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Account type, for example, if the account is a checking or savings account.</li>
     * <li>Account holder's name.</li>
     * <li>ACH information, including the routing number and account number of the account.</li>
     * </ul>
     * <p>Our gateway returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingAccount>> createAccount(
            int recipientId, CreateAccountFundingRecipientsRequest request) {
        return createAccount(recipientId, request, null);
    }

    /**
     * Use this method to create a funding account and add it to a funding recipient.
     * <p>To add a funding account to a funding recipient, you need the recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>In the request, include the following information:</p>
     * <ul>
     * <li>Account type, for example, if the account is a checking or savings account.</li>
     * <li>Account holder's name.</li>
     * <li>ACH information, including the routing number and account number of the account.</li>
     * </ul>
     * <p>Our gateway returns the fundingAccountId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<FundingAccount>> createAccount(
            int recipientId, CreateAccountFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId))
                .addPathSegments("funding-accounts");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<FundingAccount>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FundingAccount.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return a list of owners of a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific owner and you have their ownerId, use our <a href="https://docs.payroc.com/api/schema/boarding/owners/retrieve">Retrieve Owner</a> method.</p>
     * <p>To list the owners of a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each owner in the list:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient.</li>
     * </ul>
     * <p>Our gateway also returns the ownerId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<Owner>>> listOwners(int recipientId) {
        return listOwners(
                recipientId, ListFundingRecipientOwnersRequest.builder().build());
    }

    /**
     * Use this method to return a list of owners of a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific owner and you have their ownerId, use our <a href="https://docs.payroc.com/api/schema/boarding/owners/retrieve">Retrieve Owner</a> method.</p>
     * <p>To list the owners of a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each owner in the list:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient.</li>
     * </ul>
     * <p>Our gateway also returns the ownerId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<Owner>>> listOwners(
            int recipientId, RequestOptions requestOptions) {
        return listOwners(
                recipientId, ListFundingRecipientOwnersRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a list of owners of a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific owner and you have their ownerId, use our <a href="https://docs.payroc.com/api/schema/boarding/owners/retrieve">Retrieve Owner</a> method.</p>
     * <p>To list the owners of a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each owner in the list:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient.</li>
     * </ul>
     * <p>Our gateway also returns the ownerId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<Owner>>> listOwners(
            int recipientId, ListFundingRecipientOwnersRequest request) {
        return listOwners(recipientId, request, null);
    }

    /**
     * Use this method to return a list of owners of a funding recipient.
     * <p><strong>Note:</strong> If you want to view the details of a specific owner and you have their ownerId, use our <a href="https://docs.payroc.com/api/schema/boarding/owners/retrieve">Retrieve Owner</a> method.</p>
     * <p>To list the owners of a funding recipient, you need its recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method. If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>Our gateway returns the following information about each owner in the list:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient.</li>
     * </ul>
     * <p>Our gateway also returns the ownerId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<Owner>>> listOwners(
            int recipientId, ListFundingRecipientOwnersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId))
                .addPathSegments("owners");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<List<Owner>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<Owner>>() {}),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to add an additional owner to a funding recipient.
     * <p>To add an owner to a funding recipient, you need the recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>In the request, include the following information about the owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient, including whether they are a control prong.</li>
     * </ul>
     * <p>In the response, our gateway returns the ownerId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> createOwner(
            int recipientId, CreateOwnerFundingRecipientsRequest request) {
        return createOwner(recipientId, request, null);
    }

    /**
     * Use this method to add an additional owner to a funding recipient.
     * <p>To add an owner to a funding recipient, you need the recipientId. Our gateway returned the recipientId in the response of the <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/create">Create Funding Recipient</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the recipientId, use our <a href="https://docs.payroc.com/api/schema/funding/funding-recipients/list">List Funding Recipients</a> method to search for the funding recipient.</p>
     * <p>In the request, include the following information about the owner:</p>
     * <ul>
     * <li>Name, date of birth, and address.</li>
     * <li>Contact details, including their email address.</li>
     * <li>Relationship to the funding recipient, including whether they are a control prong.</li>
     * </ul>
     * <p>In the response, our gateway returns the ownerId, which you can use to run follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Owner>> createOwner(
            int recipientId, CreateOwnerFundingRecipientsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("funding-recipients")
                .addPathSegment(Integer.toString(recipientId))
                .addPathSegments("owners");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Owner>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Owner.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
