/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.notifications.eventsubscriptions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.CreateEventSubscriptionsRequest;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.DeleteEventSubscriptionsRequest;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.ListEventSubscriptionsRequest;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.PartiallyUpdateEventSubscriptionsRequest;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.RetrieveEventSubscriptionsRequest;
import com.payroc.api.resources.notifications.eventsubscriptions.requests.UpdateEventSubscriptionsRequest;
import com.payroc.api.types.EventSubscription;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.PaginatedEventSubscriptions;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawEventSubscriptionsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawEventSubscriptionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of event subscriptions that are linked to your ISV account.
     * <p><strong>Note:</strong> If you want to view the details of a specific event subscription and you have its id, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/retrieve">Retrieve Event Subscription</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for subscriptions with a specific status or an event type.</p>
     * <p>Our gateway returns the following information about each subscription in the list:</p>
     * <ul>
     * <li>Event types that you have subscribed to.</li>
     * <li>Whether you have enabled notifications for the subscription.</li>
     * <li>How we contact you when an event occurs, including the endpoint that send notifications to.</li>
     * <li>If there are any issues when we try to send you a notification, for example, if we can't contact your endpoint.</li>
     * </ul>
     * <p>For each event subscription, we also return its id, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<EventSubscription>>>> list() {
        return list(ListEventSubscriptionsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of event subscriptions that are linked to your ISV account.
     * <p><strong>Note:</strong> If you want to view the details of a specific event subscription and you have its id, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/retrieve">Retrieve Event Subscription</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for subscriptions with a specific status or an event type.</p>
     * <p>Our gateway returns the following information about each subscription in the list:</p>
     * <ul>
     * <li>Event types that you have subscribed to.</li>
     * <li>Whether you have enabled notifications for the subscription.</li>
     * <li>How we contact you when an event occurs, including the endpoint that send notifications to.</li>
     * <li>If there are any issues when we try to send you a notification, for example, if we can't contact your endpoint.</li>
     * </ul>
     * <p>For each event subscription, we also return its id, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<EventSubscription>>>> list(
            RequestOptions requestOptions) {
        return list(ListEventSubscriptionsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of event subscriptions that are linked to your ISV account.
     * <p><strong>Note:</strong> If you want to view the details of a specific event subscription and you have its id, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/retrieve">Retrieve Event Subscription</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for subscriptions with a specific status or an event type.</p>
     * <p>Our gateway returns the following information about each subscription in the list:</p>
     * <ul>
     * <li>Event types that you have subscribed to.</li>
     * <li>Whether you have enabled notifications for the subscription.</li>
     * <li>How we contact you when an event occurs, including the endpoint that send notifications to.</li>
     * <li>If there are any issues when we try to send you a notification, for example, if we can't contact your endpoint.</li>
     * </ul>
     * <p>For each event subscription, we also return its id, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<EventSubscription>>>> list(
            ListEventSubscriptionsRequest request) {
        return list(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of event subscriptions that are linked to your ISV account.
     * <p><strong>Note:</strong> If you want to view the details of a specific event subscription and you have its id, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/retrieve">Retrieve Event Subscription</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for subscriptions with a specific status or an event type.</p>
     * <p>Our gateway returns the following information about each subscription in the list:</p>
     * <ul>
     * <li>Event types that you have subscribed to.</li>
     * <li>Whether you have enabled notifications for the subscription.</li>
     * <li>How we contact you when an event occurs, including the endpoint that send notifications to.</li>
     * <li>If there are any issues when we try to send you a notification, for example, if we can't contact your endpoint.</li>
     * </ul>
     * <p>For each event subscription, we also return its id, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<EventSubscription>>>> list(
            ListEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions");
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getEvent().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event", request.getEvent().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<EventSubscription>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaginatedEventSubscriptions parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, PaginatedEventSubscriptions.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create an event subscription that we use to notify you when an event occurs, for example, when we change the status of a processing account.
     * <p>In the request, include the events that you want to subscribe to and the public endpoint that we send event notifications to. For a complete list of events that you can subscribe to, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events List</a>.</p>
     * <p>In the response, our gateway returns the id of the event subscription, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> create(CreateEventSubscriptionsRequest request) {
        return create(request, null);
    }

    /**
     * Use this method to create an event subscription that we use to notify you when an event occurs, for example, when we change the status of a processing account.
     * <p>In the request, include the events that you want to subscribe to and the public endpoint that we send event notifications to. For a complete list of events that you can subscribe to, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events List</a>.</p>
     * <p>In the response, our gateway returns the id of the event subscription, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> create(
            CreateEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<EventSubscription>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EventSubscription.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve the details of an event subscription.
     * <p>In your request, include the subscriptionId that we sent to you when we created the event subscription.</p>
     * <p><strong>Note:</strong> If you don't know the subscriptionId of the event subscription, go to <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List event subscriptions</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> retrieve(int subscriptionId) {
        return retrieve(
                subscriptionId, RetrieveEventSubscriptionsRequest.builder().build());
    }

    /**
     * Use this method to retrieve the details of an event subscription.
     * <p>In your request, include the subscriptionId that we sent to you when we created the event subscription.</p>
     * <p><strong>Note:</strong> If you don't know the subscriptionId of the event subscription, go to <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List event subscriptions</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> retrieve(
            int subscriptionId, RequestOptions requestOptions) {
        return retrieve(
                subscriptionId, RetrieveEventSubscriptionsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve the details of an event subscription.
     * <p>In your request, include the subscriptionId that we sent to you when we created the event subscription.</p>
     * <p><strong>Note:</strong> If you don't know the subscriptionId of the event subscription, go to <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List event subscriptions</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> retrieve(
            int subscriptionId, RetrieveEventSubscriptionsRequest request) {
        return retrieve(subscriptionId, request, null);
    }

    /**
     * Use this method to retrieve the details of an event subscription.
     * <p>In your request, include the subscriptionId that we sent to you when we created the event subscription.</p>
     * <p><strong>Note:</strong> If you don't know the subscriptionId of the event subscription, go to <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List event subscriptions</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> retrieve(
            int subscriptionId, RetrieveEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions")
                .addPathSegment(Integer.toString(subscriptionId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<EventSubscription>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EventSubscription.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to update the details of an event subscription.
     * <p>To update an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the subscriptionId, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List Event Subscriptions</a> method to search for the event subscription.</p>
     * <p>You can update the following details about an event subscription:</p>
     * <ul>
     * <li>Status of the event subscription.</li>
     * <li>Events that you have subscribed to. For a list of events that you can subscribe to, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events list</a>.</li>
     * <li>Information about how we contact you when an event occurs.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int subscriptionId, UpdateEventSubscriptionsRequest request) {
        return update(subscriptionId, request, null);
    }

    /**
     * Use this method to update the details of an event subscription.
     * <p>To update an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the subscriptionId, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List Event Subscriptions</a> method to search for the event subscription.</p>
     * <p>You can update the following details about an event subscription:</p>
     * <ul>
     * <li>Status of the event subscription.</li>
     * <li>Events that you have subscribed to. For a list of events that you can subscribe to, go to <a href="https://docs.payroc.com/knowledge/events/events-list">Events list</a>.</li>
     * <li>Information about how we contact you when an event occurs.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> update(
            int subscriptionId, UpdateEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions")
                .addPathSegment(Integer.toString(subscriptionId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayrocApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to delete an event subscription.
     * <blockquote>
     * <p><strong>Important:</strong> After you delete an event subscription, you can’t recover it. You won't receive event notifications from the event subscription.</p>
     * </blockquote>
     * <p>To delete an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p>If you want to stop receiving event notifications but don't want to delete the event subscription, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/update">Update Event Subscription</a> method to deactivate it.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int subscriptionId) {
        return delete(subscriptionId, DeleteEventSubscriptionsRequest.builder().build());
    }

    /**
     * Use this method to delete an event subscription.
     * <blockquote>
     * <p><strong>Important:</strong> After you delete an event subscription, you can’t recover it. You won't receive event notifications from the event subscription.</p>
     * </blockquote>
     * <p>To delete an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p>If you want to stop receiving event notifications but don't want to delete the event subscription, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/update">Update Event Subscription</a> method to deactivate it.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(int subscriptionId, RequestOptions requestOptions) {
        return delete(subscriptionId, DeleteEventSubscriptionsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to delete an event subscription.
     * <blockquote>
     * <p><strong>Important:</strong> After you delete an event subscription, you can’t recover it. You won't receive event notifications from the event subscription.</p>
     * </blockquote>
     * <p>To delete an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p>If you want to stop receiving event notifications but don't want to delete the event subscription, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/update">Update Event Subscription</a> method to deactivate it.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int subscriptionId, DeleteEventSubscriptionsRequest request) {
        return delete(subscriptionId, request, null);
    }

    /**
     * Use this method to delete an event subscription.
     * <blockquote>
     * <p><strong>Important:</strong> After you delete an event subscription, you can’t recover it. You won't receive event notifications from the event subscription.</p>
     * </blockquote>
     * <p>To delete an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p>If you want to stop receiving event notifications but don't want to delete the event subscription, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/update">Update Event Subscription</a> method to deactivate it.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            int subscriptionId, DeleteEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions")
                .addPathSegment(Integer.toString(subscriptionId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to partially update an event subscription. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the id field in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the subscriptionId, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List Event Subscriptions</a> method to search for the subscription.</p>
     * <p>You can update the following properties of an event subscription:</p>
     * <ul>
     * <li><strong>eventTypes</strong> - Subscribe to new events or remove events that you are subscribed to.</li>
     * <li><strong>notifications</strong> - Information about your endpoint and who we email if we can't contact your endpoint.</li>
     * <li><strong>enabled</strong> - Turn on or turn off notifications for the subscription.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> partiallyUpdate(
            int subscriptionId, PartiallyUpdateEventSubscriptionsRequest request) {
        return partiallyUpdate(subscriptionId, request, null);
    }

    /**
     * Use this method to partially update an event subscription. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update an event subscription, you need its subscriptionId. Our gateway returned the subscriptionId in the id field in the response of the <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/create">Create Event Subscription</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the subscriptionId, use our <a href="https://docs.payroc.com/api/schema/notifications/event-subscriptions/list">List Event Subscriptions</a> method to search for the subscription.</p>
     * <p>You can update the following properties of an event subscription:</p>
     * <ul>
     * <li><strong>eventTypes</strong> - Subscribe to new events or remove events that you are subscribed to.</li>
     * <li><strong>notifications</strong> - Information about your endpoint and who we email if we can't contact your endpoint.</li>
     * <li><strong>enabled</strong> - Turn on or turn off notifications for the subscription.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<EventSubscription>> partiallyUpdate(
            int subscriptionId, PartiallyUpdateEventSubscriptionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("event-subscriptions")
                .addPathSegment(Integer.toString(subscriptionId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<EventSubscription>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EventSubscription.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
