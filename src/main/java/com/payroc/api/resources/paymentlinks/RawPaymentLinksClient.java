/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.paymentlinks;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.PayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.paymentlinks.requests.CreatePaymentLinksRequest;
import com.payroc.api.resources.paymentlinks.requests.DeactivatePaymentLinksRequest;
import com.payroc.api.resources.paymentlinks.requests.ListPaymentLinksRequest;
import com.payroc.api.resources.paymentlinks.requests.PartiallyUpdatePaymentLinksRequest;
import com.payroc.api.resources.paymentlinks.requests.RetrievePaymentLinksRequest;
import com.payroc.api.resources.paymentlinks.types.CreatePaymentLinksResponse;
import com.payroc.api.resources.paymentlinks.types.DeactivatePaymentLinksResponse;
import com.payroc.api.resources.paymentlinks.types.PartiallyUpdatePaymentLinksResponse;
import com.payroc.api.resources.paymentlinks.types.RetrievePaymentLinksResponse;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.PaymentLinkPaginatedList;
import com.payroc.api.types.PaymentLinkPaginatedListDataItem;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawPaymentLinksClient {
    protected final ClientOptions clientOptions;

    public RawPaymentLinksClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment links linked to a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment link and you have its paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/retrieve">Retrieve Payment Link</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for only active links or multi-use links.</p>
     * <p>Our gateway returns the following information about each payment link in the list:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     * <p>For each payment link, we also return a paymentLinkId, which you can use for follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkPaginatedListDataItem>> list(String processingTerminalId) {
        return list(processingTerminalId, ListPaymentLinksRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment links linked to a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment link and you have its paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/retrieve">Retrieve Payment Link</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for only active links or multi-use links.</p>
     * <p>Our gateway returns the following information about each payment link in the list:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     * <p>For each payment link, we also return a paymentLinkId, which you can use for follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkPaginatedListDataItem>> list(
            String processingTerminalId, ListPaymentLinksRequest request) {
        return list(processingTerminalId, request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment links linked to a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment link and you have its paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/retrieve">Retrieve Payment Link</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for only active links or multi-use links.</p>
     * <p>Our gateway returns the following information about each payment link in the list:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     * <p>For each payment link, we also return a paymentLinkId, which you can use for follow-on actions.</p>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkPaginatedListDataItem>> list(
            String processingTerminalId, ListPaymentLinksRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-links");
        if (request.getMerchantReference().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantReference", request.getMerchantReference().get(), false);
        }
        if (request.getLinkType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "linkType", request.getLinkType().get(), false);
        }
        if (request.getChargeType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "chargeType", request.getChargeType().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getRecipientName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "recipientName", request.getRecipientName().get(), false);
        }
        if (request.getRecipientEmail().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "recipientEmail", request.getRecipientEmail().get(), false);
        }
        if (request.getCreatedOn().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "createdOn", request.getCreatedOn().get(), false);
        }
        if (request.getExpiresOn().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "expiresOn", request.getExpiresOn().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                PaymentLinkPaginatedList parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentLinkPaginatedList.class);
                return new PayrocApiHttpResponse<>(
                        PayrocPager.create(parsedResponse, clientOptions, requestOptions), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to create a payment link that a customer can use to make a payment for goods or services.
     * <p>The request includes the following settings:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethod</strong> - Indicates the payment methods that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * </ul>
     * <p>If your request is successful, our gateway returns a paymentLinkId, which you can use to perform follow-on actions.</p>
     * <p><strong>Note:</strong> To share the payment link with a customer, use our <a href="https://docs.payroc.com/api/schema/payment-links/sharing-events/share">Share Payment Link</a> method.</p>
     */
    public PayrocApiHttpResponse<CreatePaymentLinksResponse> create(
            String processingTerminalId, CreatePaymentLinksRequest request) {
        return create(processingTerminalId, request, null);
    }

    /**
     * Use this method to create a payment link that a customer can use to make a payment for goods or services.
     * <p>The request includes the following settings:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethod</strong> - Indicates the payment methods that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * </ul>
     * <p>If your request is successful, our gateway returns a paymentLinkId, which you can use to perform follow-on actions.</p>
     * <p><strong>Note:</strong> To share the payment link with a customer, use our <a href="https://docs.payroc.com/api/schema/payment-links/sharing-events/share">Share Payment Link</a> method.</p>
     */
    public PayrocApiHttpResponse<CreatePaymentLinksResponse> create(
            String processingTerminalId, CreatePaymentLinksRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-links")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreatePaymentLinksResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to retrieve information about a payment link.
     * <p>To retrieve a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Our gateway returns the following information about the payment link:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<RetrievePaymentLinksResponse> retrieve(String paymentLinkId) {
        return retrieve(paymentLinkId, RetrievePaymentLinksRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a payment link.
     * <p>To retrieve a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Our gateway returns the following information about the payment link:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<RetrievePaymentLinksResponse> retrieve(
            String paymentLinkId, RetrievePaymentLinksRequest request) {
        return retrieve(paymentLinkId, request, null);
    }

    /**
     * Use this method to retrieve information about a payment link.
     * <p>To retrieve a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Our gateway returns the following information about the payment link:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates whether the link can be used only once or if it can be used multiple times.</li>
     * <li><strong>authType</strong> - Indicates whether the transaction is a sale or a pre-authorization.</li>
     * <li><strong>paymentMethods</strong> - Indicates the payment method that the merchant accepts.</li>
     * <li><strong>charge</strong> - Indicates whether the merchant or the customer enters the amount for the transaction.</li>
     * <li><strong>status</strong> - Indicates if the payment link is active.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<RetrievePaymentLinksResponse> retrieve(
            String paymentLinkId, RetrievePaymentLinksRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payment-links")
                .addPathSegment(paymentLinkId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievePaymentLinksResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to partially update a payment link. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a payment link, you need its paymentLinkId, which we sent you in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>You can update the following properties of a multi-use link:</p>
     * <ul>
     * <li><strong>expiresOn parameter</strong> - Expiration date of the link.</li>
     * <li><strong>customLabels object</strong> - Label for the payment button.</li>
     * <li><strong>credentialOnFile object</strong> - Settings for saving the customer's payment details.</li>
     * </ul>
     * <p>You can update the following properties of a single-use link:</p>
     * <ul>
     * <li><strong>expiresOn parameter</strong> - Expiration date of the link.</li>
     * <li><strong>authType parameter</strong> - Transaction type of the payment link.</li>
     * <li><strong>amount parameter</strong> - Total amount of the transaction.</li>
     * <li><strong>currency parameter</strong> - Currency of the transaction.</li>
     * <li><strong>description parameter</strong> - Brief description of the transaction.</li>
     * <li><strong>customLabels object</strong> - Label for the payment button.</li>
     * <li><strong>credentialOnFile object</strong> - Settings for saving the customer's payment details.</li>
     * </ul>
     * <p><strong>Note:</strong> When a merchant updates a single-use link, we update the payment URL and HTML code in the assets object. The customer can't use the original link to make a payment.</p>
     */
    public PayrocApiHttpResponse<PartiallyUpdatePaymentLinksResponse> partiallyUpdate(
            String paymentLinkId, PartiallyUpdatePaymentLinksRequest request) {
        return partiallyUpdate(paymentLinkId, request, null);
    }

    /**
     * Use this method to partially update a payment link. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a payment link, you need its paymentLinkId, which we sent you in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>You can update the following properties of a multi-use link:</p>
     * <ul>
     * <li><strong>expiresOn parameter</strong> - Expiration date of the link.</li>
     * <li><strong>customLabels object</strong> - Label for the payment button.</li>
     * <li><strong>credentialOnFile object</strong> - Settings for saving the customer's payment details.</li>
     * </ul>
     * <p>You can update the following properties of a single-use link:</p>
     * <ul>
     * <li><strong>expiresOn parameter</strong> - Expiration date of the link.</li>
     * <li><strong>authType parameter</strong> - Transaction type of the payment link.</li>
     * <li><strong>amount parameter</strong> - Total amount of the transaction.</li>
     * <li><strong>currency parameter</strong> - Currency of the transaction.</li>
     * <li><strong>description parameter</strong> - Brief description of the transaction.</li>
     * <li><strong>customLabels object</strong> - Label for the payment button.</li>
     * <li><strong>credentialOnFile object</strong> - Settings for saving the customer's payment details.</li>
     * </ul>
     * <p><strong>Note:</strong> When a merchant updates a single-use link, we update the payment URL and HTML code in the assets object. The customer can't use the original link to make a payment.</p>
     */
    public PayrocApiHttpResponse<PartiallyUpdatePaymentLinksResponse> partiallyUpdate(
            String paymentLinkId, PartiallyUpdatePaymentLinksRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payment-links")
                .addPathSegment(paymentLinkId)
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, PartiallyUpdatePaymentLinksResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to deactivate a payment link.
     * <p>To deactivate a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>If your request is successful, our gateway deactivates the payment link. The customer can't use the link to make a payment, and you can't reactivate the payment link.</p>
     */
    public PayrocApiHttpResponse<DeactivatePaymentLinksResponse> deactivate(String paymentLinkId) {
        return deactivate(paymentLinkId, DeactivatePaymentLinksRequest.builder().build());
    }

    /**
     * Use this method to deactivate a payment link.
     * <p>To deactivate a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>If your request is successful, our gateway deactivates the payment link. The customer can't use the link to make a payment, and you can't reactivate the payment link.</p>
     */
    public PayrocApiHttpResponse<DeactivatePaymentLinksResponse> deactivate(
            String paymentLinkId, DeactivatePaymentLinksRequest request) {
        return deactivate(paymentLinkId, request, null);
    }

    /**
     * Use this method to deactivate a payment link.
     * <p>To deactivate a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>If your request is successful, our gateway deactivates the payment link. The customer can't use the link to make a payment, and you can't reactivate the payment link.</p>
     */
    public PayrocApiHttpResponse<DeactivatePaymentLinksResponse> deactivate(
            String paymentLinkId, DeactivatePaymentLinksRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payment-links")
                .addPathSegment(paymentLinkId)
                .addPathSegments("deactivate")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeactivatePaymentLinksResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
