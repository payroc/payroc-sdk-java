/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.paymentlinks.sharingevents;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.PayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.paymentlinks.sharingevents.requests.ListSharingEventsRequest;
import com.payroc.api.resources.paymentlinks.sharingevents.requests.ShareSharingEventsRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.PaymentLinkEmailShareEvent;
import com.payroc.api.types.SharingEventPaginatedList;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSharingEventsClient {
    protected final ClientOptions clientOptions;

    public RawSharingEventsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of sharing events for a payment link. A sharing event occurs when a merchant shares a payment link with a customer.
     * <p>To list the sharing events for a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for links sent to a specific customer.</p>
     * <p>Our gateway returns the following information for each sharing event in the list:</p>
     * <ul>
     * <li>Customer that the merchant sent the link to.</li>
     * <li>Date that the merchant sent the link.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkEmailShareEvent>> list(String paymentLinkId) {
        return list(paymentLinkId, ListSharingEventsRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of sharing events for a payment link. A sharing event occurs when a merchant shares a payment link with a customer.
     * <p>To list the sharing events for a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for links sent to a specific customer.</p>
     * <p>Our gateway returns the following information for each sharing event in the list:</p>
     * <ul>
     * <li>Customer that the merchant sent the link to.</li>
     * <li>Date that the merchant sent the link.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkEmailShareEvent>> list(
            String paymentLinkId, RequestOptions requestOptions) {
        return list(paymentLinkId, ListSharingEventsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of sharing events for a payment link. A sharing event occurs when a merchant shares a payment link with a customer.
     * <p>To list the sharing events for a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for links sent to a specific customer.</p>
     * <p>Our gateway returns the following information for each sharing event in the list:</p>
     * <ul>
     * <li>Customer that the merchant sent the link to.</li>
     * <li>Date that the merchant sent the link.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkEmailShareEvent>> list(
            String paymentLinkId, ListSharingEventsRequest request) {
        return list(paymentLinkId, request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of sharing events for a payment link. A sharing event occurs when a merchant shares a payment link with a customer.
     * <p>To list the sharing events for a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for links sent to a specific customer.</p>
     * <p>Our gateway returns the following information for each sharing event in the list:</p>
     * <ul>
     * <li>Customer that the merchant sent the link to.</li>
     * <li>Date that the merchant sent the link.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<PayrocPager<PaymentLinkEmailShareEvent>> list(
            String paymentLinkId, ListSharingEventsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payment-links")
                .addPathSegment(paymentLinkId)
                .addPathSegments("sharing-events");
        if (request.getRecipientName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "recipientName", request.getRecipientName().get(), false);
        }
        if (request.getRecipientEmail().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "recipientEmail", request.getRecipientEmail().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                SharingEventPaginatedList parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SharingEventPaginatedList.class);
                return new PayrocApiHttpResponse<>(
                        PayrocPager.create(parsedResponse, clientOptions, requestOptions), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Use this method to email a payment link to a customer.
     * <p>To email a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>In the request, you must provide the recipient's name and email address.</p>
     * <p>In the response, our gateway returns a sharingEventId, which you can use to <a href="https://docs.payroc.com/api/schema/payment-links/sharing-events/list">List Payment Link Sharing Events</a>.</p>
     */
    public PayrocApiHttpResponse<PaymentLinkEmailShareEvent> share(
            String paymentLinkId, ShareSharingEventsRequest request) {
        return share(paymentLinkId, request, null);
    }

    /**
     * Use this method to email a payment link to a customer.
     * <p>To email a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the <a href="https://docs.payroc.com/api/schema/payment-links/create">Create Payment Link</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentLinkId, use our <a href="https://docs.payroc.com/api/schema/payment-links/list">List Payment Links</a> method to search for the payment link.</p>
     * <p>In the request, you must provide the recipient's name and email address.</p>
     * <p>In the response, our gateway returns a sharingEventId, which you can use to <a href="https://docs.payroc.com/api/schema/payment-links/sharing-events/list">List Payment Link Sharing Events</a>.</p>
     */
    public PayrocApiHttpResponse<PaymentLinkEmailShareEvent> share(
            String paymentLinkId, ShareSharingEventsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("payment-links")
                .addPathSegment(paymentLinkId)
                .addPathSegments("sharing-events");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentLinkEmailShareEvent.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                response);
                    case 415:
                        throw new UnsupportedMediaTypeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFifteen.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
