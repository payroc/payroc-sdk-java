/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.paymentlinks.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.MultiUsePaymentLink;
import com.payroc.api.types.SingleUsePaymentLink;
import java.util.Objects;
import java.util.Optional;

public final class CreatePaymentLinksResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CreatePaymentLinksResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CreatePaymentLinksResponse multiUse(MultiUsePaymentLink value) {
        return new CreatePaymentLinksResponse(new MultiUseValue(value));
    }

    public static CreatePaymentLinksResponse singleUse(SingleUsePaymentLink value) {
        return new CreatePaymentLinksResponse(new SingleUseValue(value));
    }

    public boolean isMultiUse() {
        return value instanceof MultiUseValue;
    }

    public boolean isSingleUse() {
        return value instanceof SingleUseValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<MultiUsePaymentLink> getMultiUse() {
        if (isMultiUse()) {
            return Optional.of(((MultiUseValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SingleUsePaymentLink> getSingleUse() {
        if (isSingleUse()) {
            return Optional.of(((SingleUseValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitMultiUse(MultiUsePaymentLink multiUse);

        T visitSingleUse(SingleUsePaymentLink singleUse);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(MultiUseValue.class), @JsonSubTypes.Type(SingleUseValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("multiUse")
    @JsonIgnoreProperties("type")
    private static final class MultiUseValue implements Value {
        @JsonUnwrapped
        private MultiUsePaymentLink value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MultiUseValue() {}

        private MultiUseValue(MultiUsePaymentLink value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMultiUse(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MultiUseValue && equalTo((MultiUseValue) other);
        }

        private boolean equalTo(MultiUseValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreatePaymentLinksResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("singleUse")
    @JsonIgnoreProperties("type")
    private static final class SingleUseValue implements Value {
        @JsonUnwrapped
        private SingleUsePaymentLink value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SingleUseValue() {}

        private SingleUseValue(SingleUsePaymentLink value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSingleUse(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SingleUseValue && equalTo((SingleUseValue) other);
        }

        private boolean equalTo(SingleUseValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreatePaymentLinksResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreatePaymentLinksResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
