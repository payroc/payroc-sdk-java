/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.payroccloud.closedloopreads;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.payroccloud.closedloopreads.requests.RetrieveClosedLoopReadsRequest;
import com.payroc.api.types.ClosedLoopResponse;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawClosedLoopReadsClient {
    protected final ClientOptions clientOptions;

    public RawClosedLoopReadsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to retrieve information that a payment device captured from a closed-loop card.
     * <p>A closed-loop card is a type of card that a customer can use only with a specific merchant. Each time a payment device captures information from a closed-loop card, we store the information as a closed-loop read.</p>
     * <p>Our gateway returns the following information from a closed-loop read:</p>
     * <ul>
     * <li>Date that the payment device captured the information.</li>
     * <li>Unstructured payload from the card.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<ClosedLoopResponse> retrieve(String closedLoopReadId) {
        return retrieve(
                closedLoopReadId, RetrieveClosedLoopReadsRequest.builder().build());
    }

    /**
     * Use this method to retrieve information that a payment device captured from a closed-loop card.
     * <p>A closed-loop card is a type of card that a customer can use only with a specific merchant. Each time a payment device captures information from a closed-loop card, we store the information as a closed-loop read.</p>
     * <p>Our gateway returns the following information from a closed-loop read:</p>
     * <ul>
     * <li>Date that the payment device captured the information.</li>
     * <li>Unstructured payload from the card.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<ClosedLoopResponse> retrieve(String closedLoopReadId, RequestOptions requestOptions) {
        return retrieve(
                closedLoopReadId, RetrieveClosedLoopReadsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information that a payment device captured from a closed-loop card.
     * <p>A closed-loop card is a type of card that a customer can use only with a specific merchant. Each time a payment device captures information from a closed-loop card, we store the information as a closed-loop read.</p>
     * <p>Our gateway returns the following information from a closed-loop read:</p>
     * <ul>
     * <li>Date that the payment device captured the information.</li>
     * <li>Unstructured payload from the card.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<ClosedLoopResponse> retrieve(
            String closedLoopReadId, RetrieveClosedLoopReadsRequest request) {
        return retrieve(closedLoopReadId, request, null);
    }

    /**
     * Use this method to retrieve information that a payment device captured from a closed-loop card.
     * <p>A closed-loop card is a type of card that a customer can use only with a specific merchant. Each time a payment device captures information from a closed-loop card, we store the information as a closed-loop read.</p>
     * <p>Our gateway returns the following information from a closed-loop read:</p>
     * <ul>
     * <li>Date that the payment device captured the information.</li>
     * <li>Unstructured payload from the card.</li>
     * </ul>
     */
    public PayrocApiHttpResponse<ClosedLoopResponse> retrieve(
            String closedLoopReadId, RetrieveClosedLoopReadsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("closed-loop-reads")
                .addPathSegment(closedLoopReadId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayrocApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClosedLoopResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                response);
                    case 406:
                        throw new NotAcceptableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayrocApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PayrocApiException("Network error executing HTTP request", e);
        }
    }
}
