/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.repeatpayments.paymentplans;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.repeatpayments.paymentplans.requests.CreatePaymentPlansRequest;
import com.payroc.api.resources.repeatpayments.paymentplans.requests.DeletePaymentPlansRequest;
import com.payroc.api.resources.repeatpayments.paymentplans.requests.ListPaymentPlansRequest;
import com.payroc.api.resources.repeatpayments.paymentplans.requests.PartiallyUpdatePaymentPlansRequest;
import com.payroc.api.resources.repeatpayments.paymentplans.requests.RetrievePaymentPlansRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.PaymentPlan;
import com.payroc.api.types.PaymentPlanPaginatedList;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawPaymentPlansClient {
    protected final ClientOptions clientOptions;

    public AsyncRawPaymentPlansClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment plans for a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment plan and you have its paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a> method.</p>
     * <p>Our gateway returns the following information about each payment plan in the list:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     * <p>For each payment plan, we return the paymentPlanId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<PaymentPlan>>>> list(
            String processingTerminalId) {
        return list(processingTerminalId, ListPaymentPlansRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment plans for a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment plan and you have its paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a> method.</p>
     * <p>Our gateway returns the following information about each payment plan in the list:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     * <p>For each payment plan, we return the paymentPlanId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<PaymentPlan>>>> list(
            String processingTerminalId, RequestOptions requestOptions) {
        return list(processingTerminalId, ListPaymentPlansRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment plans for a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment plan and you have its paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a> method.</p>
     * <p>Our gateway returns the following information about each payment plan in the list:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     * <p>For each payment plan, we return the paymentPlanId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<PaymentPlan>>>> list(
            String processingTerminalId, ListPaymentPlansRequest request) {
        return list(processingTerminalId, request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of payment plans for a processing terminal.
     * <p><strong>Note:</strong> If you want to view the details of a specific payment plan and you have its paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a> method.</p>
     * <p>Our gateway returns the following information about each payment plan in the list:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     * <p>For each payment plan, we return the paymentPlanId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<PaymentPlan>>>> list(
            String processingTerminalId, ListPaymentPlansRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-plans");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<PaymentPlan>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        PaymentPlanPaginatedList parsedResponse =
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentPlanPaginatedList.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create a payment schedule that you can assign customers to.
     * <p><strong>Note:</strong> This method is part of our Repeat Payments feature. To help you understand how this method works with our Subscriptions endpoints, go to <a href="https://docs.payroc.com/guides/take-payments/repeat-payments">Repeat Payments</a>.</p>
     * <p>When you create a payment plan you need to provide a unique paymentPlanId that you use to run follow-on actions:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a>  - View the details of the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/partially-update">Update Payment Plan</a>  - Update the details of the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/delete">Delete Payment Plan</a>  - Delete the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/create">Create Subscription</a>  - Subscribe a customer to the payment plan.</li>
     * </ul>
     * <p>The request includes the following settings:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates if our gateway or the merchant collects payments. If the merchant manually collects payments, integrate with the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/pay">Pay Manual Subscription</a> method.</li>
     * <li><strong>recurringOrder</strong> - Amount of each payment if the gateway automatically collect payments.</li>
     * <li><strong>setupOrder</strong> - Setup fee that our gateway immediately collects from the customer's payment method.</li>
     * <li><strong>onUpdate and onDelete</strong> - Indicates what happens to associated subscriptions if the merchant updates or deletes the payment plan.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> create(
            String processingTerminalId, CreatePaymentPlansRequest request) {
        return create(processingTerminalId, request, null);
    }

    /**
     * Use this method to create a payment schedule that you can assign customers to.
     * <p><strong>Note:</strong> This method is part of our Repeat Payments feature. To help you understand how this method works with our Subscriptions endpoints, go to <a href="https://docs.payroc.com/guides/take-payments/repeat-payments">Repeat Payments</a>.</p>
     * <p>When you create a payment plan you need to provide a unique paymentPlanId that you use to run follow-on actions:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/retrieve">Retrieve Payment Plan</a>  - View the details of the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/partially-update">Update Payment Plan</a>  - Update the details of the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/delete">Delete Payment Plan</a>  - Delete the payment plan.</li>
     * <li><a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/create">Create Subscription</a>  - Subscribe a customer to the payment plan.</li>
     * </ul>
     * <p>The request includes the following settings:</p>
     * <ul>
     * <li><strong>type</strong> - Indicates if our gateway or the merchant collects payments. If the merchant manually collects payments, integrate with the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/pay">Pay Manual Subscription</a> method.</li>
     * <li><strong>recurringOrder</strong> - Amount of each payment if the gateway automatically collect payments.</li>
     * <li><strong>setupOrder</strong> - Setup fee that our gateway immediately collects from the customer's payment method.</li>
     * <li><strong>onUpdate and onDelete</strong> - Indicates what happens to associated subscriptions if the merchant updates or deletes the payment plan.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> create(
            String processingTerminalId, CreatePaymentPlansRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-plans");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentPlan.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a payment plan.
     * <p>To retrieve a payment plan, you need its paymentPlanId. Our gateway returned the paymentPlanId in the response of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>Our gateway returns the following information about the payment plan:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> retrieve(
            String processingTerminalId, String paymentPlanId) {
        return retrieve(
                processingTerminalId,
                paymentPlanId,
                RetrievePaymentPlansRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a payment plan.
     * <p>To retrieve a payment plan, you need its paymentPlanId. Our gateway returned the paymentPlanId in the response of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>Our gateway returns the following information about the payment plan:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> retrieve(
            String processingTerminalId, String paymentPlanId, RequestOptions requestOptions) {
        return retrieve(
                processingTerminalId,
                paymentPlanId,
                RetrievePaymentPlansRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use this method to retrieve information about a payment plan.
     * <p>To retrieve a payment plan, you need its paymentPlanId. Our gateway returned the paymentPlanId in the response of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>Our gateway returns the following information about the payment plan:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> retrieve(
            String processingTerminalId, String paymentPlanId, RetrievePaymentPlansRequest request) {
        return retrieve(processingTerminalId, paymentPlanId, request, null);
    }

    /**
     * Use this method to retrieve information about a payment plan.
     * <p>To retrieve a payment plan, you need its paymentPlanId. Our gateway returned the paymentPlanId in the response of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>Our gateway returns the following information about the payment plan:</p>
     * <ul>
     * <li>Name, length, and currency of the plan</li>
     * <li>How often our gateway collects each payment</li>
     * <li>How much our gateway collects for each payment</li>
     * <li>What happens if the merchant updates or deletes the plan</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> retrieve(
            String processingTerminalId,
            String paymentPlanId,
            RetrievePaymentPlansRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-plans")
                .addPathSegment(paymentPlanId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentPlan.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to delete a payment plan.
     * <blockquote>
     * <p><strong>Important:</strong> When you delete a payment plan, you can’t recover it. You also won’t be able to add subscriptions to the payment plan.</p>
     * </blockquote>
     * <p>To delete a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>The value you sent for the onDelete parameter when you created the payment plan indicates what happens to associated subscriptions when you delete the plan:</p>
     * <ul>
     * <li><code>complete</code> - Our gateway stops taking payments for the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our gateway continues to take payments for the subscriptions associated with the payment plan. To stop a subscription for a cancelled payment plan, go to the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/deactivate">Deactivate Subscription</a> method.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(String processingTerminalId, String paymentPlanId) {
        return delete(
                processingTerminalId,
                paymentPlanId,
                DeletePaymentPlansRequest.builder().build());
    }

    /**
     * Use this method to delete a payment plan.
     * <blockquote>
     * <p><strong>Important:</strong> When you delete a payment plan, you can’t recover it. You also won’t be able to add subscriptions to the payment plan.</p>
     * </blockquote>
     * <p>To delete a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>The value you sent for the onDelete parameter when you created the payment plan indicates what happens to associated subscriptions when you delete the plan:</p>
     * <ul>
     * <li><code>complete</code> - Our gateway stops taking payments for the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our gateway continues to take payments for the subscriptions associated with the payment plan. To stop a subscription for a cancelled payment plan, go to the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/deactivate">Deactivate Subscription</a> method.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId, String paymentPlanId, RequestOptions requestOptions) {
        return delete(
                processingTerminalId,
                paymentPlanId,
                DeletePaymentPlansRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use this method to delete a payment plan.
     * <blockquote>
     * <p><strong>Important:</strong> When you delete a payment plan, you can’t recover it. You also won’t be able to add subscriptions to the payment plan.</p>
     * </blockquote>
     * <p>To delete a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>The value you sent for the onDelete parameter when you created the payment plan indicates what happens to associated subscriptions when you delete the plan:</p>
     * <ul>
     * <li><code>complete</code> - Our gateway stops taking payments for the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our gateway continues to take payments for the subscriptions associated with the payment plan. To stop a subscription for a cancelled payment plan, go to the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/deactivate">Deactivate Subscription</a> method.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId, String paymentPlanId, DeletePaymentPlansRequest request) {
        return delete(processingTerminalId, paymentPlanId, request, null);
    }

    /**
     * Use this method to delete a payment plan.
     * <blockquote>
     * <p><strong>Important:</strong> When you delete a payment plan, you can’t recover it. You also won’t be able to add subscriptions to the payment plan.</p>
     * </blockquote>
     * <p>To delete a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>The value you sent for the onDelete parameter when you created the payment plan indicates what happens to associated subscriptions when you delete the plan:</p>
     * <ul>
     * <li><code>complete</code> - Our gateway stops taking payments for the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our gateway continues to take payments for the subscriptions associated with the payment plan. To stop a subscription for a cancelled payment plan, go to the <a href="https://docs.payroc.com/api/schema/repeat-payments/subscriptions/deactivate">Deactivate Subscription</a> method.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId,
            String paymentPlanId,
            DeletePaymentPlansRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-plans")
                .addPathSegment(paymentPlanId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to partially update a payment plan. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>You can update all of the properties of the payment plan except for the paymentPlanId.</p>
     * <p>The value you sent for the onUpdate parameter when you created the payment plan indicates what happens to the associated subscriptions when you update the plan:</p>
     * <ul>
     * <li><code>update</code> - Our gateway updates the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our  gateway doesn't update the subscriptions associated with the payment plan.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> partiallyUpdate(
            String processingTerminalId, String paymentPlanId, PartiallyUpdatePaymentPlansRequest request) {
        return partiallyUpdate(processingTerminalId, paymentPlanId, request, null);
    }

    /**
     * Use this method to partially update a payment plan. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a payment plan, you need its paymentPlanId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/create">Create Payment Plan</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the paymentPlanId, use our <a href="https://docs.payroc.com/api/schema/repeat-payments/payment-plans/list">List Payment Plans</a> method to search for the payment plan.</p>
     * <p>You can update all of the properties of the payment plan except for the paymentPlanId.</p>
     * <p>The value you sent for the onUpdate parameter when you created the payment plan indicates what happens to the associated subscriptions when you update the plan:</p>
     * <ul>
     * <li><code>update</code> - Our gateway updates the subscriptions associated with the payment plan.</li>
     * <li><code>continue</code> - Our  gateway doesn't update the subscriptions associated with the payment plan.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> partiallyUpdate(
            String processingTerminalId,
            String paymentPlanId,
            PartiallyUpdatePaymentPlansRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("payment-plans")
                .addPathSegment(paymentPlanId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<PaymentPlan>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentPlan.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
