/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.reporting.settlement;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.resources.reporting.settlement.requests.ListDisputesStatusesSettlementRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementAchDepositFeesRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementAchDepositsRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementAuthorizationsRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementBatchesRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementDisputesRequest;
import com.payroc.api.resources.reporting.settlement.requests.ListReportingSettlementTransactionsRequest;
import com.payroc.api.resources.reporting.settlement.requests.RetrieveAchDepositSettlementRequest;
import com.payroc.api.resources.reporting.settlement.requests.RetrieveAuthorizationSettlementRequest;
import com.payroc.api.resources.reporting.settlement.requests.RetrieveBatchSettlementRequest;
import com.payroc.api.resources.reporting.settlement.requests.RetrieveTransactionSettlementRequest;
import com.payroc.api.resources.reporting.settlement.types.ListAchDepositFeesSettlementResponse;
import com.payroc.api.resources.reporting.settlement.types.ListAchDepositsSettlementResponse;
import com.payroc.api.resources.reporting.settlement.types.ListAuthorizationsSettlementResponse;
import com.payroc.api.resources.reporting.settlement.types.ListBatchesSettlementResponse;
import com.payroc.api.resources.reporting.settlement.types.ListDisputesSettlementResponse;
import com.payroc.api.resources.reporting.settlement.types.ListTransactionsSettlementResponse;
import com.payroc.api.types.AchDeposit;
import com.payroc.api.types.AchDepositFee;
import com.payroc.api.types.Authorization;
import com.payroc.api.types.Batch;
import com.payroc.api.types.Dispute;
import com.payroc.api.types.DisputeStatus;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.Transaction;
import java.io.IOException;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSettlementClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSettlementClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of batches that your merchants submitted to the processor on a specific date.
     * <p><strong>Note:</strong> If you want to view the details of a specific batch and you have its batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-batch">Retrieve Batch</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for batches that were submitted by a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each batch in the list:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Batch>>>> listBatches(
            ListReportingSettlementBatchesRequest request) {
        return listBatches(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of batches that your merchants submitted to the processor on a specific date.
     * <p><strong>Note:</strong> If you want to view the details of a specific batch and you have its batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-batch">Retrieve Batch</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for batches that were submitted by a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each batch in the list:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Batch>>>> listBatches(
            ListReportingSettlementBatchesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("batches");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "date", request.getDate(), false);
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Batch>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListBatchesSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListBatchesSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a batch.
     * <p><strong>Note:</strong> To retrieve a batch, you need its batchId. If you don't have the batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-batches">List Batches</a> method to search for the batch.</p>
     * <p>Our gateway returns the following information about the batch:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Batch>> retrieveBatch(int batchId) {
        return retrieveBatch(batchId, RetrieveBatchSettlementRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a batch.
     * <p><strong>Note:</strong> To retrieve a batch, you need its batchId. If you don't have the batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-batches">List Batches</a> method to search for the batch.</p>
     * <p>Our gateway returns the following information about the batch:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Batch>> retrieveBatch(int batchId, RequestOptions requestOptions) {
        return retrieveBatch(batchId, RetrieveBatchSettlementRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a batch.
     * <p><strong>Note:</strong> To retrieve a batch, you need its batchId. If you don't have the batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-batches">List Batches</a> method to search for the batch.</p>
     * <p>Our gateway returns the following information about the batch:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Batch>> retrieveBatch(
            int batchId, RetrieveBatchSettlementRequest request) {
        return retrieveBatch(batchId, request, null);
    }

    /**
     * Use this method to retrieve information about a batch.
     * <p><strong>Note:</strong> To retrieve a batch, you need its batchId. If you don't have the batchId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-batches">List Batches</a> method to search for the batch.</p>
     * <p>Our gateway returns the following information about the batch:</p>
     * <ul>
     * <li>Transaction information, including the number of transactions and total value of sales.</li>
     * <li>Merchant information, including the merchant ID (MID) and the processing account that the batch is associated with.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Batch>> retrieveBatch(
            int batchId, RetrieveBatchSettlementRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("batches")
                .addPathSegment(Integer.toString(batchId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Batch>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Batch.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return a paginated list of your merchants’ transactions.
     * <p><strong>Note:</strong> If you want to view the details of a specific transaction and you have its transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-transaction">Retrieve Transaction</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for transactions for a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each transaction in the list:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Transaction>>>>
            listTransactions() {
        return listTransactions(
                ListReportingSettlementTransactionsRequest.builder().build());
    }

    /**
     * Use this method to return a paginated list of your merchants’ transactions.
     * <p><strong>Note:</strong> If you want to view the details of a specific transaction and you have its transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-transaction">Retrieve Transaction</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for transactions for a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each transaction in the list:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Transaction>>>> listTransactions(
            RequestOptions requestOptions) {
        return listTransactions(
                ListReportingSettlementTransactionsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a paginated list of your merchants’ transactions.
     * <p><strong>Note:</strong> If you want to view the details of a specific transaction and you have its transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-transaction">Retrieve Transaction</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for transactions for a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each transaction in the list:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Transaction>>>> listTransactions(
            ListReportingSettlementTransactionsRequest request) {
        return listTransactions(request, null);
    }

    /**
     * Use this method to return a paginated list of your merchants’ transactions.
     * <p><strong>Note:</strong> If you want to view the details of a specific transaction and you have its transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-transaction">Retrieve Transaction</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for transactions for a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each transaction in the list:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Transaction>>>> listTransactions(
            ListReportingSettlementTransactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("transactions");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "date", request.getDate().get(), false);
        }
        if (request.getBatchId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "batchId", request.getBatchId().get(), false);
        }
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (request.getTransactionType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "transactionType", request.getTransactionType().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Transaction>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListTransactionsSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListTransactionsSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a transaction.
     * <p><strong>Note:</strong> To retrieve a transaction, you need its transactionId. If you don't have the transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-transactions">List Transactions</a> method to search for the transaction.</p>
     * <p>Our gateway returns the following information about the transaction:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Transaction>> retrieveTransaction(int transactionId) {
        return retrieveTransaction(
                transactionId, RetrieveTransactionSettlementRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a transaction.
     * <p><strong>Note:</strong> To retrieve a transaction, you need its transactionId. If you don't have the transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-transactions">List Transactions</a> method to search for the transaction.</p>
     * <p>Our gateway returns the following information about the transaction:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Transaction>> retrieveTransaction(
            int transactionId, RequestOptions requestOptions) {
        return retrieveTransaction(
                transactionId, RetrieveTransactionSettlementRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about a transaction.
     * <p><strong>Note:</strong> To retrieve a transaction, you need its transactionId. If you don't have the transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-transactions">List Transactions</a> method to search for the transaction.</p>
     * <p>Our gateway returns the following information about the transaction:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Transaction>> retrieveTransaction(
            int transactionId, RetrieveTransactionSettlementRequest request) {
        return retrieveTransaction(transactionId, request, null);
    }

    /**
     * Use this method to retrieve information about a transaction.
     * <p><strong>Note:</strong> To retrieve a transaction, you need its transactionId. If you don't have the transactionId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-transactions">List Transactions</a> method to search for the transaction.</p>
     * <p>Our gateway returns the following information about the transaction:</p>
     * <ul>
     * <li>Merchant and processing account that ran the transaction.</li>
     * <li>Transaction type, date, amount, and the payment method that the customer used.</li>
     * <li>Batch that contains the transaction, and authorization details for the transaction.</li>
     * <li>Processor that settled the transaction and the ACH deposit containing the transaction.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Transaction>> retrieveTransaction(
            int transactionId, RetrieveTransactionSettlementRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("transactions")
                .addPathSegment(Integer.toString(transactionId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Transaction>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Transaction.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of authorizations.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for authorizations linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each authorization in the list:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Authorization>>>>
            listAuthorizations() {
        return listAuthorizations(
                ListReportingSettlementAuthorizationsRequest.builder().build());
    }

    /**
     * Use this method to retrieve a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of authorizations.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for authorizations linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each authorization in the list:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Authorization>>>>
            listAuthorizations(RequestOptions requestOptions) {
        return listAuthorizations(
                ListReportingSettlementAuthorizationsRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of authorizations.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for authorizations linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each authorization in the list:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Authorization>>>>
            listAuthorizations(ListReportingSettlementAuthorizationsRequest request) {
        return listAuthorizations(request, null);
    }

    /**
     * Use this method to retrieve a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of authorizations.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for authorizations linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the date query parameter or the batchId query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each authorization in the list:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Authorization>>>>
            listAuthorizations(ListReportingSettlementAuthorizationsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("authorizations");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "date", request.getDate().get(), false);
        }
        if (request.getBatchId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "batchId", request.getBatchId().get(), false);
        }
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Authorization>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListAuthorizationsSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListAuthorizationsSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about an authorization.
     * <p><strong>Note:</strong> To retrieve an authorization, you need its authorizationId. If you don't have the authorizationId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-authorizations">List Authorizations</a> method to search for the authorization.</p>
     * <p>Our gateway returns the following information about the authorization:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Authorization>> retrieveAuthorization(int authorizationId) {
        return retrieveAuthorization(
                authorizationId,
                RetrieveAuthorizationSettlementRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about an authorization.
     * <p><strong>Note:</strong> To retrieve an authorization, you need its authorizationId. If you don't have the authorizationId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-authorizations">List Authorizations</a> method to search for the authorization.</p>
     * <p>Our gateway returns the following information about the authorization:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Authorization>> retrieveAuthorization(
            int authorizationId, RequestOptions requestOptions) {
        return retrieveAuthorization(
                authorizationId,
                RetrieveAuthorizationSettlementRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use this method to retrieve information about an authorization.
     * <p><strong>Note:</strong> To retrieve an authorization, you need its authorizationId. If you don't have the authorizationId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-authorizations">List Authorizations</a> method to search for the authorization.</p>
     * <p>Our gateway returns the following information about the authorization:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Authorization>> retrieveAuthorization(
            int authorizationId, RetrieveAuthorizationSettlementRequest request) {
        return retrieveAuthorization(authorizationId, request, null);
    }

    /**
     * Use this method to retrieve information about an authorization.
     * <p><strong>Note:</strong> To retrieve an authorization, you need its authorizationId. If you don't have the authorizationId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-authorizations">List Authorizations</a> method to search for the authorization.</p>
     * <p>Our gateway returns the following information about the authorization:</p>
     * <ul>
     * <li>Authorization response from the issuing bank.</li>
     * <li>Amount that the issuing bank authorized.</li>
     * <li>Merchant that ran the authorization.</li>
     * <li>Details about the customer's card, the transaction, and the batch.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<Authorization>> retrieveAuthorization(
            int authorizationId, RetrieveAuthorizationSettlementRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("authorizations")
                .addPathSegment(Integer.toString(authorizationId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Authorization>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Authorization.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of disputes.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for disputes linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each dispute in the list:</p>
     * <ul>
     * <li>Its status, type, and description.</li>
     * <li>Transaction that the dispute is linked to, including the transaction date, merchant who ran the transaction, and the payment method that the cardholder used.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Dispute>>>> listDisputes(
            ListReportingSettlementDisputesRequest request) {
        return listDisputes(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of disputes.
     * <p>Use query parameters to filter the list of results that we return, for example, to search for disputes linked to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each dispute in the list:</p>
     * <ul>
     * <li>Its status, type, and description.</li>
     * <li>Transaction that the dispute is linked to, including the transaction date, merchant who ran the transaction, and the payment method that the cardholder used.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Dispute>>>> listDisputes(
            ListReportingSettlementDisputesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("disputes");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "date", request.getDate(), false);
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<Dispute>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListDisputesSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListDisputesSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return the status history of a dispute.
     * <p>To view the status history of a dispute, you need its disputeId. If you don't have the disputeId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-disputes">List Disputes</a> method to search for the dispute.</p>
     * <p>Our gateway returns a list that contains each status change, the date it was changed, and its updated status.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<DisputeStatus>>> listDisputesStatuses(int disputeId) {
        return listDisputesStatuses(
                disputeId, ListDisputesStatusesSettlementRequest.builder().build());
    }

    /**
     * Use this method to return the status history of a dispute.
     * <p>To view the status history of a dispute, you need its disputeId. If you don't have the disputeId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-disputes">List Disputes</a> method to search for the dispute.</p>
     * <p>Our gateway returns a list that contains each status change, the date it was changed, and its updated status.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<DisputeStatus>>> listDisputesStatuses(
            int disputeId, RequestOptions requestOptions) {
        return listDisputesStatuses(
                disputeId, ListDisputesStatusesSettlementRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return the status history of a dispute.
     * <p>To view the status history of a dispute, you need its disputeId. If you don't have the disputeId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-disputes">List Disputes</a> method to search for the dispute.</p>
     * <p>Our gateway returns a list that contains each status change, the date it was changed, and its updated status.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<DisputeStatus>>> listDisputesStatuses(
            int disputeId, ListDisputesStatusesSettlementRequest request) {
        return listDisputesStatuses(disputeId, request, null);
    }

    /**
     * Use this method to return the status history of a dispute.
     * <p>To view the status history of a dispute, you need its disputeId. If you don't have the disputeId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-disputes">List Disputes</a> method to search for the dispute.</p>
     * <p>Our gateway returns a list that contains each status change, the date it was changed, and its updated status.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<List<DisputeStatus>>> listDisputesStatuses(
            int disputeId, ListDisputesStatusesSettlementRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("disputes")
                .addPathSegment(Integer.toString(disputeId))
                .addPathSegments("statuses");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<List<DisputeStatus>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<DisputeStatus>>() {}),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of ACH deposits that we paid to your merchants.
     * <p><strong>Note:</strong> If you want to view the details of a specific ACH deposit and you have its achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-ach-deposit">Retrieve ACH Deposit</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for ACH deposits that we paid to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each ACH deposit in the list:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDeposit>>>> listAchDeposits(
            ListReportingSettlementAchDepositsRequest request) {
        return listAchDeposits(request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of ACH deposits that we paid to your merchants.
     * <p><strong>Note:</strong> If you want to view the details of a specific ACH deposit and you have its achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/retrieve-ach-deposit">Retrieve ACH Deposit</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for ACH deposits that we paid to a specific merchant.</p>
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for the date query parameter.</p>
     * </blockquote>
     * <p>Our gateway returns the following information about each ACH deposit in the list:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDeposit>>>> listAchDeposits(
            ListReportingSettlementAchDepositsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("ach-deposits");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "date", request.getDate(), false);
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDeposit>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListAchDepositsSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListAchDepositsSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about an ACH deposit that we paid to a merchant.
     * <p><strong>Note:</strong> To retrieve an ACH deposit, you need its achDepositId. If you don't have the achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-ach-deposits">List ACH Deposits</a> method to search for the ACH deposit.</p>
     * <p>Our gateway returns the following information about the ACH deposit:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<AchDeposit>> retrieveAchDeposit(int achDepositId) {
        return retrieveAchDeposit(
                achDepositId, RetrieveAchDepositSettlementRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about an ACH deposit that we paid to a merchant.
     * <p><strong>Note:</strong> To retrieve an ACH deposit, you need its achDepositId. If you don't have the achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-ach-deposits">List ACH Deposits</a> method to search for the ACH deposit.</p>
     * <p>Our gateway returns the following information about the ACH deposit:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<AchDeposit>> retrieveAchDeposit(
            int achDepositId, RequestOptions requestOptions) {
        return retrieveAchDeposit(
                achDepositId, RetrieveAchDepositSettlementRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to retrieve information about an ACH deposit that we paid to a merchant.
     * <p><strong>Note:</strong> To retrieve an ACH deposit, you need its achDepositId. If you don't have the achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-ach-deposits">List ACH Deposits</a> method to search for the ACH deposit.</p>
     * <p>Our gateway returns the following information about the ACH deposit:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<AchDeposit>> retrieveAchDeposit(
            int achDepositId, RetrieveAchDepositSettlementRequest request) {
        return retrieveAchDeposit(achDepositId, request, null);
    }

    /**
     * Use this method to retrieve information about an ACH deposit that we paid to a merchant.
     * <p><strong>Note:</strong> To retrieve an ACH deposit, you need its achDepositId. If you don't have the achDepositId, use our <a href="https://docs.payroc.com/api/schema/reporting/settlement/list-ach-deposits">List ACH Deposits</a> method to search for the ACH deposit.</p>
     * <p>Our gateway returns the following information about the ACH deposit:</p>
     * <ul>
     * <li>Merchant that we sent the ACH deposit to.</li>
     * <li>Total amount that we paid the merchant.</li>
     * <li>Breakdown of sales, returns, and fees.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<AchDeposit>> retrieveAchDeposit(
            int achDepositId, RetrieveAchDepositSettlementRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("ach-deposits")
                .addPathSegment(Integer.toString(achDepositId));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<AchDeposit>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AchDeposit.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieve a list of ACH deposit fees.
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the 'date' query parameter or the 'achDepositId' query parameter.</p>
     * </blockquote>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDepositFee>>>>
            listAchDepositFees() {
        return listAchDepositFees(
                ListReportingSettlementAchDepositFeesRequest.builder().build());
    }

    /**
     * Retrieve a list of ACH deposit fees.
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the 'date' query parameter or the 'achDepositId' query parameter.</p>
     * </blockquote>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDepositFee>>>>
            listAchDepositFees(RequestOptions requestOptions) {
        return listAchDepositFees(
                ListReportingSettlementAchDepositFeesRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieve a list of ACH deposit fees.
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the 'date' query parameter or the 'achDepositId' query parameter.</p>
     * </blockquote>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDepositFee>>>>
            listAchDepositFees(ListReportingSettlementAchDepositFeesRequest request) {
        return listAchDepositFees(request, null);
    }

    /**
     * Retrieve a list of ACH deposit fees.
     * <blockquote>
     * <p><strong>Important:</strong> You must provide a value for either the 'date' query parameter or the 'achDepositId' query parameter.</p>
     * </blockquote>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDepositFee>>>>
            listAchDepositFees(ListReportingSettlementAchDepositFeesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("ach-deposit-fees");
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "date", request.getDate().get(), false);
        }
        if (request.getAchDepositId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "achDepositId", request.getAchDepositId().get(), false);
        }
        if (request.getMerchantId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merchantId", request.getMerchantId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<AchDepositFee>>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListAchDepositFeesSettlementResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListAchDepositFeesSettlementResponse.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
