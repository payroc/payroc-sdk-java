/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.tokenization.securetokens;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.payroc.api.core.ClientOptions;
import com.payroc.api.core.MediaTypes;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.core.PayrocApiApiException;
import com.payroc.api.core.PayrocApiException;
import com.payroc.api.core.PayrocApiHttpResponse;
import com.payroc.api.core.QueryStringMapper;
import com.payroc.api.core.RequestOptions;
import com.payroc.api.core.pagination.AsyncPayrocPager;
import com.payroc.api.errors.BadRequestError;
import com.payroc.api.errors.ConflictError;
import com.payroc.api.errors.ContentTooLargeError;
import com.payroc.api.errors.ForbiddenError;
import com.payroc.api.errors.InternalServerError;
import com.payroc.api.errors.NotAcceptableError;
import com.payroc.api.errors.NotFoundError;
import com.payroc.api.errors.UnauthorizedError;
import com.payroc.api.errors.UnsupportedMediaTypeError;
import com.payroc.api.resources.tokenization.securetokens.requests.DeleteSecureTokensRequest;
import com.payroc.api.resources.tokenization.securetokens.requests.ListSecureTokensRequest;
import com.payroc.api.resources.tokenization.securetokens.requests.PartiallyUpdateSecureTokensRequest;
import com.payroc.api.resources.tokenization.securetokens.requests.RetrieveSecureTokensRequest;
import com.payroc.api.resources.tokenization.securetokens.requests.TokenizationRequest;
import com.payroc.api.resources.tokenization.securetokens.requests.UpdateAccountSecureTokensRequest;
import com.payroc.api.types.FiveHundred;
import com.payroc.api.types.FourHundred;
import com.payroc.api.types.FourHundredFifteen;
import com.payroc.api.types.FourHundredFour;
import com.payroc.api.types.FourHundredNine;
import com.payroc.api.types.FourHundredOne;
import com.payroc.api.types.FourHundredSix;
import com.payroc.api.types.FourHundredThirteen;
import com.payroc.api.types.SecureToken;
import com.payroc.api.types.SecureTokenPaginatedListWithAccountType;
import com.payroc.api.types.SecureTokenWithAccountType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSecureTokensClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSecureTokensClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of secure tokens.
     * <p><strong>Note:</strong> If you want to view the details of a specific secure token and you have its secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for secure tokens by customer or by the first four digits of a card number.</p>
     * <p>Our gateway returns information about the following for each secure token in the list:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     * <p>For each secure token, we also return the secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<SecureTokenWithAccountType>>>>
            list(String processingTerminalId) {
        return list(processingTerminalId, ListSecureTokensRequest.builder().build());
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of secure tokens.
     * <p><strong>Note:</strong> If you want to view the details of a specific secure token and you have its secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for secure tokens by customer or by the first four digits of a card number.</p>
     * <p>Our gateway returns information about the following for each secure token in the list:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     * <p>For each secure token, we also return the secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<SecureTokenWithAccountType>>>>
            list(String processingTerminalId, RequestOptions requestOptions) {
        return list(processingTerminalId, ListSecureTokensRequest.builder().build(), requestOptions);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of secure tokens.
     * <p><strong>Note:</strong> If you want to view the details of a specific secure token and you have its secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for secure tokens by customer or by the first four digits of a card number.</p>
     * <p>Our gateway returns information about the following for each secure token in the list:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     * <p>For each secure token, we also return the secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<SecureTokenWithAccountType>>>>
            list(String processingTerminalId, ListSecureTokensRequest request) {
        return list(processingTerminalId, request, null);
    }

    /**
     * Use this method to return a <a href="https://docs.payroc.com/api/pagination">paginated</a> list of secure tokens.
     * <p><strong>Note:</strong> If you want to view the details of a specific secure token and you have its secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> method.</p>
     * <p>Use query parameters to filter the list of results that we return, for example, to search for secure tokens by customer or by the first four digits of a card number.</p>
     * <p>Our gateway returns information about the following for each secure token in the list:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     * <p>For each secure token, we also return the secureTokenId, which you can use to perform follow-on actions.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<SecureTokenWithAccountType>>>>
            list(String processingTerminalId, ListSecureTokensRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens");
        if (request.getSecureTokenId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "secureTokenId", request.getSecureTokenId().get(), false);
        }
        if (request.getCustomerName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "customerName", request.getCustomerName().get(), false);
        }
        if (request.getPhone().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "phone", request.getPhone().get(), false);
        }
        if (request.getEmail().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "email", request.getEmail().get(), false);
        }
        if (request.getToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "token", request.getToken().get(), false);
        }
        if (request.getFirst6().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first6", request.getFirst6().get(), false);
        }
        if (request.getLast4().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last4", request.getLast4().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<CompletableFuture<AsyncPayrocPager<SecureTokenWithAccountType>>>>
                future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        SecureTokenPaginatedListWithAccountType parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, SecureTokenPaginatedListWithAccountType.class);
                        future.complete(new PayrocApiHttpResponse<>(
                                AsyncPayrocPager.createAsync(parsedResponse, clientOptions, requestOptions), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to create a secure token that represents a customer's payment details.
     * <p>When you create a secure token, you need to generate and provide a secureTokenId that you use to run follow-on actions:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> – View the details of the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/delete">Delete Secure Token</a> – Delete the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/partially-update">Update Secure Token</a> – Update the details of the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/update-account">Update Account Details</a> – Update the secure token with the details from a single-use token.</li>
     * </ul>
     * <p><strong>Note:</strong> If you don't generate a secureTokenId to identify the token, our gateway generates a unique identifier and returns it in the response.</p>
     * <p>If the request is successful, our gateway returns a token that the merchant can use in transactions instead of the customer's sensitive payment details, for example, when they <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">run a sale</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> create(
            String processingTerminalId, TokenizationRequest request) {
        return create(processingTerminalId, request, null);
    }

    /**
     * Use this method to create a secure token that represents a customer's payment details.
     * <p>When you create a secure token, you need to generate and provide a secureTokenId that you use to run follow-on actions:</p>
     * <ul>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/retrieve">Retrieve Secure Token</a> – View the details of the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/delete">Delete Secure Token</a> – Delete the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/partially-update">Update Secure Token</a> – Update the details of the secure token.</li>
     * <li><a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/update-account">Update Account Details</a> – Update the secure token with the details from a single-use token.</li>
     * </ul>
     * <p><strong>Note:</strong> If you don't generate a secureTokenId to identify the token, our gateway generates a unique identifier and returns it in the response.</p>
     * <p>If the request is successful, our gateway returns a token that the merchant can use in transactions instead of the customer's sensitive payment details, for example, when they <a href="https://docs.payroc.com/api/schema/card-payments/payments/create">run a sale</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> create(
            String processingTerminalId, TokenizationRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<SecureToken>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SecureToken.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to retrieve information about a secure token.
     * <p>To retrieve a secure token, you need its secureTokenID, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>Our gateway returns the following information about the secure token:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureTokenWithAccountType>> retrieve(
            String processingTerminalId, String secureTokenId) {
        return retrieve(
                processingTerminalId,
                secureTokenId,
                RetrieveSecureTokensRequest.builder().build());
    }

    /**
     * Use this method to retrieve information about a secure token.
     * <p>To retrieve a secure token, you need its secureTokenID, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>Our gateway returns the following information about the secure token:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureTokenWithAccountType>> retrieve(
            String processingTerminalId, String secureTokenId, RequestOptions requestOptions) {
        return retrieve(
                processingTerminalId,
                secureTokenId,
                RetrieveSecureTokensRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use this method to retrieve information about a secure token.
     * <p>To retrieve a secure token, you need its secureTokenID, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>Our gateway returns the following information about the secure token:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureTokenWithAccountType>> retrieve(
            String processingTerminalId, String secureTokenId, RetrieveSecureTokensRequest request) {
        return retrieve(processingTerminalId, secureTokenId, request, null);
    }

    /**
     * Use this method to retrieve information about a secure token.
     * <p>To retrieve a secure token, you need its secureTokenID, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>Our gateway returns the following information about the secure token:</p>
     * <ul>
     * <li>Payment details that the secure token represents.</li>
     * <li>Customer details, including shipping and billing addresses.</li>
     * <li>Secure token that you can use to carry out transactions.</li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureTokenWithAccountType>> retrieve(
            String processingTerminalId,
            String secureTokenId,
            RetrieveSecureTokensRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens")
                .addPathSegment(secureTokenId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<SecureTokenWithAccountType>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, SecureTokenWithAccountType.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to delete a secure token and its related payment details from our vault.
     * <p>To delete a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>When you delete a secure token, you can’t recover it, and you can’t reuse its identifier for a new token.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(String processingTerminalId, String secureTokenId) {
        return delete(
                processingTerminalId,
                secureTokenId,
                DeleteSecureTokensRequest.builder().build());
    }

    /**
     * Use this method to delete a secure token and its related payment details from our vault.
     * <p>To delete a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>When you delete a secure token, you can’t recover it, and you can’t reuse its identifier for a new token.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId, String secureTokenId, RequestOptions requestOptions) {
        return delete(
                processingTerminalId,
                secureTokenId,
                DeleteSecureTokensRequest.builder().build(),
                requestOptions);
    }

    /**
     * Use this method to delete a secure token and its related payment details from our vault.
     * <p>To delete a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>When you delete a secure token, you can’t recover it, and you can’t reuse its identifier for a new token.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId, String secureTokenId, DeleteSecureTokensRequest request) {
        return delete(processingTerminalId, secureTokenId, request, null);
    }

    /**
     * Use this method to delete a secure token and its related payment details from our vault.
     * <p>To delete a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don’t have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search for the secure token.</p>
     * <p>When you delete a secure token, you can’t recover it, and you can’t reuse its identifier for a new token.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<Void>> delete(
            String processingTerminalId,
            String secureTokenId,
            DeleteSecureTokensRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens")
                .addPathSegment(secureTokenId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to partially update a secure token. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search  for the payment.</p>
     * <p>You can update all of the properties of the secure token, except the following:</p>
     * <ul>
     * <li>processingTerminalId</li>
     * <li>type</li>
     * <li>token</li>
     * <li>status</li>
     * <li>source/Card
     * <ul>
     * <li>type</li>
     * <li>cardNumber</li>
     * <li>cardType</li>
     * <li>currency</li>
     * <li>debit</li>
     * <li>surcharging</li>
     * </ul>
     * </li>
     * <li>source/ACH account
     * <ul>
     * <li>accountNumber</li>
     * <li>routingNumber</li>
     * </ul>
     * </li>
     * <li>source/PAD account
     * <ul>
     * <li>type</li>
     * <li>accountNumber</li>
     * <li>transitNumber</li>
     * </ul>
     * </li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> partiallyUpdate(
            String processingTerminalId, String secureTokenId, PartiallyUpdateSecureTokensRequest request) {
        return partiallyUpdate(processingTerminalId, secureTokenId, request, null);
    }

    /**
     * Use this method to partially update a secure token. Structure your request to follow the <a href="https://datatracker.ietf.org/doc/html/rfc6902">RFC 6902</a> standard.
     * <p>To update a secure token, you need its secureTokenId, which you sent in the request of the <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/create">Create Secure Token</a> method.</p>
     * <p><strong>Note:</strong> If you don't have the secureTokenId, use our <a href="https://docs.payroc.com/api/schema/tokenization/secure-tokens/list">List Secure Tokens</a> method to search  for the payment.</p>
     * <p>You can update all of the properties of the secure token, except the following:</p>
     * <ul>
     * <li>processingTerminalId</li>
     * <li>type</li>
     * <li>token</li>
     * <li>status</li>
     * <li>source/Card
     * <ul>
     * <li>type</li>
     * <li>cardNumber</li>
     * <li>cardType</li>
     * <li>currency</li>
     * <li>debit</li>
     * <li>surcharging</li>
     * </ul>
     * </li>
     * <li>source/ACH account
     * <ul>
     * <li>accountNumber</li>
     * <li>routingNumber</li>
     * </ul>
     * </li>
     * <li>source/PAD account
     * <ul>
     * <li>type</li>
     * <li>accountNumber</li>
     * <li>transitNumber</li>
     * </ul>
     * </li>
     * </ul>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> partiallyUpdate(
            String processingTerminalId,
            String secureTokenId,
            PartiallyUpdateSecureTokensRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens")
                .addPathSegment(secureTokenId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<SecureToken>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SecureToken.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Use this method to update a secure token if you have a single-use token from Hosted Fields.
     * <p><strong>Note:</strong> If you don't have a single-use token, you can update saved payment details with our <a href="https://docs.payroc.com/api/resources#updateSecureToken">Update Secure Token</a> method. For more information about our two options to update a secure token, go to <a href="https://docs.payroc.com/guides/take-payments/update-saved-payment-details">Update saved payment details</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> updateAccount(
            String processingTerminalId, String secureTokenId, UpdateAccountSecureTokensRequest request) {
        return updateAccount(processingTerminalId, secureTokenId, request, null);
    }

    /**
     * Use this method to update a secure token if you have a single-use token from Hosted Fields.
     * <p><strong>Note:</strong> If you don't have a single-use token, you can update saved payment details with our <a href="https://docs.payroc.com/api/resources#updateSecureToken">Update Secure Token</a> method. For more information about our two options to update a secure token, go to <a href="https://docs.payroc.com/guides/take-payments/update-saved-payment-details">Update saved payment details</a>.</p>
     */
    public CompletableFuture<PayrocApiHttpResponse<SecureToken>> updateAccount(
            String processingTerminalId,
            String secureTokenId,
            UpdateAccountSecureTokensRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getApiURL())
                .newBuilder()
                .addPathSegments("processing-terminals")
                .addPathSegment(processingTerminalId)
                .addPathSegments("secure-tokens")
                .addPathSegment(secureTokenId)
                .addPathSegments("update-account");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PayrocApiHttpResponse<SecureToken>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PayrocApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SecureToken.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundred.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredOne.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredFour.class),
                                        response));
                                return;
                            case 406:
                                future.completeExceptionally(new NotAcceptableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredSix.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FourHundredNine.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredThirteen.class),
                                        response));
                                return;
                            case 415:
                                future.completeExceptionally(new UnsupportedMediaTypeError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, FourHundredFifteen.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FiveHundred.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PayrocApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PayrocApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
