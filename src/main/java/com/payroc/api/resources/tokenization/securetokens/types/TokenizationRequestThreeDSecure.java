/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.tokenization.securetokens.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.payroc.api.types.GatewayThreeDSecure;
import com.payroc.api.types.ThirdPartyThreeDSecure;
import java.util.Objects;
import java.util.Optional;

public final class TokenizationRequestThreeDSecure {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private TokenizationRequestThreeDSecure(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static TokenizationRequestThreeDSecure gatewayThreeDSecure(GatewayThreeDSecure value) {
        return new TokenizationRequestThreeDSecure(new GatewayThreeDSecureValue(value));
    }

    public static TokenizationRequestThreeDSecure thirdPartyThreeDSecure(ThirdPartyThreeDSecure value) {
        return new TokenizationRequestThreeDSecure(new ThirdPartyThreeDSecureValue(value));
    }

    public boolean isGatewayThreeDSecure() {
        return value instanceof GatewayThreeDSecureValue;
    }

    public boolean isThirdPartyThreeDSecure() {
        return value instanceof ThirdPartyThreeDSecureValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<GatewayThreeDSecure> getGatewayThreeDSecure() {
        if (isGatewayThreeDSecure()) {
            return Optional.of(((GatewayThreeDSecureValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ThirdPartyThreeDSecure> getThirdPartyThreeDSecure() {
        if (isThirdPartyThreeDSecure()) {
            return Optional.of(((ThirdPartyThreeDSecureValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitGatewayThreeDSecure(GatewayThreeDSecure gatewayThreeDSecure);

        T visitThirdPartyThreeDSecure(ThirdPartyThreeDSecure thirdPartyThreeDSecure);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(GatewayThreeDSecureValue.class),
        @JsonSubTypes.Type(ThirdPartyThreeDSecureValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("gatewayThreeDSecure")
    @JsonIgnoreProperties("type")
    private static final class GatewayThreeDSecureValue implements Value {
        @JsonUnwrapped
        private GatewayThreeDSecure value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private GatewayThreeDSecureValue() {}

        private GatewayThreeDSecureValue(GatewayThreeDSecure value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitGatewayThreeDSecure(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof GatewayThreeDSecureValue && equalTo((GatewayThreeDSecureValue) other);
        }

        private boolean equalTo(GatewayThreeDSecureValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TokenizationRequestThreeDSecure{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("thirdPartyThreeDSecure")
    @JsonIgnoreProperties("type")
    private static final class ThirdPartyThreeDSecureValue implements Value {
        @JsonUnwrapped
        private ThirdPartyThreeDSecure value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ThirdPartyThreeDSecureValue() {}

        private ThirdPartyThreeDSecureValue(ThirdPartyThreeDSecure value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitThirdPartyThreeDSecure(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ThirdPartyThreeDSecureValue && equalTo((ThirdPartyThreeDSecureValue) other);
        }

        private boolean equalTo(ThirdPartyThreeDSecureValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TokenizationRequestThreeDSecure{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TokenizationRequestThreeDSecure{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
