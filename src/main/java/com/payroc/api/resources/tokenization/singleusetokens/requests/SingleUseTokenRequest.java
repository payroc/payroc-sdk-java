/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.resources.tokenization.singleusetokens.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import com.payroc.api.resources.tokenization.singleusetokens.types.SingleUseTokenRequestChannel;
import com.payroc.api.resources.tokenization.singleusetokens.types.SingleUseTokenRequestSource;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SingleUseTokenRequest.Builder.class)
public final class SingleUseTokenRequest {
    private final String idempotencyKey;

    private final SingleUseTokenRequestChannel channel;

    private final Optional<String> operator;

    private final SingleUseTokenRequestSource source;

    private final Map<String, Object> additionalProperties;

    private SingleUseTokenRequest(
            String idempotencyKey,
            SingleUseTokenRequestChannel channel,
            Optional<String> operator,
            SingleUseTokenRequestSource source,
            Map<String, Object> additionalProperties) {
        this.idempotencyKey = idempotencyKey;
        this.channel = channel;
        this.operator = operator;
        this.source = source;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Unique identifier that you generate for each request. You must use the <a href="https://www.rfc-editor.org/rfc/rfc4122">UUID v4 format</a> for the identifier. For more information about the idempotency key, go to <a href="https://docs.payroc.com/api/idempotency">Idempotency</a>.
     */
    @JsonIgnore
    public String getIdempotencyKey() {
        return idempotencyKey;
    }

    /**
     * @return Channel that the merchant used to receive the payment details.
     */
    @JsonProperty("channel")
    public SingleUseTokenRequestChannel getChannel() {
        return channel;
    }

    /**
     * @return Operator who initiated the request.
     */
    @JsonProperty("operator")
    public Optional<String> getOperator() {
        return operator;
    }

    /**
     * @return Polymorphic object that contains the payment method to tokenize.
     * <p>The value of the type parameter determines which variant you should use:</p>
     * <ul>
     * <li><code>ach</code> - Automated Clearing House (ACH) details</li>
     * <li><code>pad</code> - Pre-authorized debit (PAD) details</li>
     * <li><code>card</code> - Payment card details</li>
     * </ul>
     */
    @JsonProperty("source")
    public SingleUseTokenRequestSource getSource() {
        return source;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SingleUseTokenRequest && equalTo((SingleUseTokenRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SingleUseTokenRequest other) {
        return idempotencyKey.equals(other.idempotencyKey)
                && channel.equals(other.channel)
                && operator.equals(other.operator)
                && source.equals(other.source);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.idempotencyKey, this.channel, this.operator, this.source);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdempotencyKeyStage builder() {
        return new Builder();
    }

    public interface IdempotencyKeyStage {
        /**
         * <p>Unique identifier that you generate for each request. You must use the <a href="https://www.rfc-editor.org/rfc/rfc4122">UUID v4 format</a> for the identifier. For more information about the idempotency key, go to <a href="https://docs.payroc.com/api/idempotency">Idempotency</a>.</p>
         */
        ChannelStage idempotencyKey(@NotNull String idempotencyKey);

        Builder from(SingleUseTokenRequest other);
    }

    public interface ChannelStage {
        /**
         * <p>Channel that the merchant used to receive the payment details.</p>
         */
        SourceStage channel(@NotNull SingleUseTokenRequestChannel channel);
    }

    public interface SourceStage {
        /**
         * <p>Polymorphic object that contains the payment method to tokenize.</p>
         * <p>The value of the type parameter determines which variant you should use:</p>
         * <ul>
         * <li><code>ach</code> - Automated Clearing House (ACH) details</li>
         * <li><code>pad</code> - Pre-authorized debit (PAD) details</li>
         * <li><code>card</code> - Payment card details</li>
         * </ul>
         */
        _FinalStage source(@NotNull SingleUseTokenRequestSource source);
    }

    public interface _FinalStage {
        SingleUseTokenRequest build();

        /**
         * <p>Operator who initiated the request.</p>
         */
        _FinalStage operator(Optional<String> operator);

        _FinalStage operator(String operator);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdempotencyKeyStage, ChannelStage, SourceStage, _FinalStage {
        private String idempotencyKey;

        private SingleUseTokenRequestChannel channel;

        private SingleUseTokenRequestSource source;

        private Optional<String> operator = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SingleUseTokenRequest other) {
            idempotencyKey(other.getIdempotencyKey());
            channel(other.getChannel());
            operator(other.getOperator());
            source(other.getSource());
            return this;
        }

        /**
         * <p>Unique identifier that you generate for each request. You must use the <a href="https://www.rfc-editor.org/rfc/rfc4122">UUID v4 format</a> for the identifier. For more information about the idempotency key, go to <a href="https://docs.payroc.com/api/idempotency">Idempotency</a>.</p>
         * <p>Unique identifier that you generate for each request. You must use the <a href="https://www.rfc-editor.org/rfc/rfc4122">UUID v4 format</a> for the identifier. For more information about the idempotency key, go to <a href="https://docs.payroc.com/api/idempotency">Idempotency</a>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public ChannelStage idempotencyKey(@NotNull String idempotencyKey) {
            this.idempotencyKey = Objects.requireNonNull(idempotencyKey, "idempotencyKey must not be null");
            return this;
        }

        /**
         * <p>Channel that the merchant used to receive the payment details.</p>
         * <p>Channel that the merchant used to receive the payment details.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("channel")
        public SourceStage channel(@NotNull SingleUseTokenRequestChannel channel) {
            this.channel = Objects.requireNonNull(channel, "channel must not be null");
            return this;
        }

        /**
         * <p>Polymorphic object that contains the payment method to tokenize.</p>
         * <p>The value of the type parameter determines which variant you should use:</p>
         * <ul>
         * <li><code>ach</code> - Automated Clearing House (ACH) details</li>
         * <li><code>pad</code> - Pre-authorized debit (PAD) details</li>
         * <li><code>card</code> - Payment card details</li>
         * </ul>
         * <p>Polymorphic object that contains the payment method to tokenize.</p>
         * <p>The value of the type parameter determines which variant you should use:</p>
         * <ul>
         * <li><code>ach</code> - Automated Clearing House (ACH) details</li>
         * <li><code>pad</code> - Pre-authorized debit (PAD) details</li>
         * <li><code>card</code> - Payment card details</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("source")
        public _FinalStage source(@NotNull SingleUseTokenRequestSource source) {
            this.source = Objects.requireNonNull(source, "source must not be null");
            return this;
        }

        /**
         * <p>Operator who initiated the request.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage operator(String operator) {
            this.operator = Optional.ofNullable(operator);
            return this;
        }

        /**
         * <p>Operator who initiated the request.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "operator", nulls = Nulls.SKIP)
        public _FinalStage operator(Optional<String> operator) {
            this.operator = operator;
            return this;
        }

        @java.lang.Override
        public SingleUseTokenRequest build() {
            return new SingleUseTokenRequest(idempotencyKey, channel, operator, source, additionalProperties);
        }
    }
}
