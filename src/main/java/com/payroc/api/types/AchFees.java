/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AchFees.Builder.class)
public final class AchFees {
    private final int transaction;

    private final int batch;

    private final int returns;

    private final int unauthorizedReturn;

    private final int statement;

    private final int monthlyMinimum;

    private final int accountVerification;

    private final double discountRateUnder10000;

    private final double discountRateAbove10000;

    private final Map<String, Object> additionalProperties;

    private AchFees(
            int transaction,
            int batch,
            int returns,
            int unauthorizedReturn,
            int statement,
            int monthlyMinimum,
            int accountVerification,
            double discountRateUnder10000,
            double discountRateAbove10000,
            Map<String, Object> additionalProperties) {
        this.transaction = transaction;
        this.batch = batch;
        this.returns = returns;
        this.unauthorizedReturn = unauthorizedReturn;
        this.statement = statement;
        this.monthlyMinimum = monthlyMinimum;
        this.accountVerification = accountVerification;
        this.discountRateUnder10000 = discountRateUnder10000;
        this.discountRateAbove10000 = discountRateAbove10000;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Fee for each transaction. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("transaction")
    public int getTransaction() {
        return transaction;
    }

    /**
     * @return Fee for each batch. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("batch")
    public int getBatch() {
        return batch;
    }

    /**
     * @return Fee for each return. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("returns")
    public int getReturns() {
        return returns;
    }

    /**
     * @return Fee for each unauthorized return. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("unauthorizedReturn")
    public int getUnauthorizedReturn() {
        return unauthorizedReturn;
    }

    /**
     * @return Fee for each statement. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("statement")
    public int getStatement() {
        return statement;
    }

    /**
     * @return Minimum monthly value of transactions. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("monthlyMinimum")
    public int getMonthlyMinimum() {
        return monthlyMinimum;
    }

    /**
     * @return Fee for each account verification. The value is in the currency's lowest denomination, for example, cents.
     */
    @JsonProperty("accountVerification")
    public int getAccountVerification() {
        return accountVerification;
    }

    /**
     * @return Percentage discount for ACH transfers less than $10,000.
     */
    @JsonProperty("discountRateUnder10000")
    public double getDiscountRateUnder10000() {
        return discountRateUnder10000;
    }

    /**
     * @return Percentage discount for ACH transfers equal to or more than $10,000.
     */
    @JsonProperty("discountRateAbove10000")
    public double getDiscountRateAbove10000() {
        return discountRateAbove10000;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AchFees && equalTo((AchFees) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AchFees other) {
        return transaction == other.transaction
                && batch == other.batch
                && returns == other.returns
                && unauthorizedReturn == other.unauthorizedReturn
                && statement == other.statement
                && monthlyMinimum == other.monthlyMinimum
                && accountVerification == other.accountVerification
                && discountRateUnder10000 == other.discountRateUnder10000
                && discountRateAbove10000 == other.discountRateAbove10000;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.transaction,
                this.batch,
                this.returns,
                this.unauthorizedReturn,
                this.statement,
                this.monthlyMinimum,
                this.accountVerification,
                this.discountRateUnder10000,
                this.discountRateAbove10000);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TransactionStage builder() {
        return new Builder();
    }

    public interface TransactionStage {
        /**
         * <p>Fee for each transaction. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        BatchStage transaction(int transaction);

        Builder from(AchFees other);
    }

    public interface BatchStage {
        /**
         * <p>Fee for each batch. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        ReturnsStage batch(int batch);
    }

    public interface ReturnsStage {
        /**
         * <p>Fee for each return. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        UnauthorizedReturnStage returns(int returns);
    }

    public interface UnauthorizedReturnStage {
        /**
         * <p>Fee for each unauthorized return. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        StatementStage unauthorizedReturn(int unauthorizedReturn);
    }

    public interface StatementStage {
        /**
         * <p>Fee for each statement. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        MonthlyMinimumStage statement(int statement);
    }

    public interface MonthlyMinimumStage {
        /**
         * <p>Minimum monthly value of transactions. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        AccountVerificationStage monthlyMinimum(int monthlyMinimum);
    }

    public interface AccountVerificationStage {
        /**
         * <p>Fee for each account verification. The value is in the currency's lowest denomination, for example, cents.</p>
         */
        DiscountRateUnder10000Stage accountVerification(int accountVerification);
    }

    public interface DiscountRateUnder10000Stage {
        /**
         * <p>Percentage discount for ACH transfers less than $10,000.</p>
         */
        DiscountRateAbove10000Stage discountRateUnder10000(double discountRateUnder10000);
    }

    public interface DiscountRateAbove10000Stage {
        /**
         * <p>Percentage discount for ACH transfers equal to or more than $10,000.</p>
         */
        _FinalStage discountRateAbove10000(double discountRateAbove10000);
    }

    public interface _FinalStage {
        AchFees build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements TransactionStage,
                    BatchStage,
                    ReturnsStage,
                    UnauthorizedReturnStage,
                    StatementStage,
                    MonthlyMinimumStage,
                    AccountVerificationStage,
                    DiscountRateUnder10000Stage,
                    DiscountRateAbove10000Stage,
                    _FinalStage {
        private int transaction;

        private int batch;

        private int returns;

        private int unauthorizedReturn;

        private int statement;

        private int monthlyMinimum;

        private int accountVerification;

        private double discountRateUnder10000;

        private double discountRateAbove10000;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AchFees other) {
            transaction(other.getTransaction());
            batch(other.getBatch());
            returns(other.getReturns());
            unauthorizedReturn(other.getUnauthorizedReturn());
            statement(other.getStatement());
            monthlyMinimum(other.getMonthlyMinimum());
            accountVerification(other.getAccountVerification());
            discountRateUnder10000(other.getDiscountRateUnder10000());
            discountRateAbove10000(other.getDiscountRateAbove10000());
            return this;
        }

        /**
         * <p>Fee for each transaction. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each transaction. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("transaction")
        public BatchStage transaction(int transaction) {
            this.transaction = transaction;
            return this;
        }

        /**
         * <p>Fee for each batch. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each batch. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("batch")
        public ReturnsStage batch(int batch) {
            this.batch = batch;
            return this;
        }

        /**
         * <p>Fee for each return. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each return. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("returns")
        public UnauthorizedReturnStage returns(int returns) {
            this.returns = returns;
            return this;
        }

        /**
         * <p>Fee for each unauthorized return. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each unauthorized return. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("unauthorizedReturn")
        public StatementStage unauthorizedReturn(int unauthorizedReturn) {
            this.unauthorizedReturn = unauthorizedReturn;
            return this;
        }

        /**
         * <p>Fee for each statement. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each statement. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("statement")
        public MonthlyMinimumStage statement(int statement) {
            this.statement = statement;
            return this;
        }

        /**
         * <p>Minimum monthly value of transactions. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Minimum monthly value of transactions. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("monthlyMinimum")
        public AccountVerificationStage monthlyMinimum(int monthlyMinimum) {
            this.monthlyMinimum = monthlyMinimum;
            return this;
        }

        /**
         * <p>Fee for each account verification. The value is in the currency's lowest denomination, for example, cents.</p>
         * <p>Fee for each account verification. The value is in the currency's lowest denomination, for example, cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("accountVerification")
        public DiscountRateUnder10000Stage accountVerification(int accountVerification) {
            this.accountVerification = accountVerification;
            return this;
        }

        /**
         * <p>Percentage discount for ACH transfers less than $10,000.</p>
         * <p>Percentage discount for ACH transfers less than $10,000.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("discountRateUnder10000")
        public DiscountRateAbove10000Stage discountRateUnder10000(double discountRateUnder10000) {
            this.discountRateUnder10000 = discountRateUnder10000;
            return this;
        }

        /**
         * <p>Percentage discount for ACH transfers equal to or more than $10,000.</p>
         * <p>Percentage discount for ACH transfers equal to or more than $10,000.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("discountRateAbove10000")
        public _FinalStage discountRateAbove10000(double discountRateAbove10000) {
            this.discountRateAbove10000 = discountRateAbove10000;
            return this;
        }

        @java.lang.Override
        public AchFees build() {
            return new AchFees(
                    transaction,
                    batch,
                    returns,
                    unauthorizedReturn,
                    statement,
                    monthlyMinimum,
                    accountVerification,
                    discountRateUnder10000,
                    discountRateAbove10000,
                    additionalProperties);
        }
    }
}
