/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ActivityRecordType {
    public static final ActivityRecordType DEBIT = new ActivityRecordType(Value.DEBIT, "debit");

    public static final ActivityRecordType CREDIT = new ActivityRecordType(Value.CREDIT, "credit");

    private final Value value;

    private final String string;

    ActivityRecordType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ActivityRecordType && this.string.equals(((ActivityRecordType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DEBIT:
                return visitor.visitDebit();
            case CREDIT:
                return visitor.visitCredit();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ActivityRecordType valueOf(String value) {
        switch (value) {
            case "debit":
                return DEBIT;
            case "credit":
                return CREDIT;
            default:
                return new ActivityRecordType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        CREDIT,

        DEBIT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCredit();

        T visitDebit();

        T visitUnknown(String unknownType);
    }
}
