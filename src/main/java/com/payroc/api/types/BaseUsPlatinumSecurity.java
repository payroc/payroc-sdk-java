/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class BaseUsPlatinumSecurity {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private BaseUsPlatinumSecurity(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static BaseUsPlatinumSecurity monthly(BaseUsMonthly value) {
        return new BaseUsPlatinumSecurity(new MonthlyValue(value));
    }

    public static BaseUsPlatinumSecurity annual(BaseUsAnnual value) {
        return new BaseUsPlatinumSecurity(new AnnualValue(value));
    }

    public boolean isMonthly() {
        return value instanceof MonthlyValue;
    }

    public boolean isAnnual() {
        return value instanceof AnnualValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<BaseUsMonthly> getMonthly() {
        if (isMonthly()) {
            return Optional.of(((MonthlyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BaseUsAnnual> getAnnual() {
        if (isAnnual()) {
            return Optional.of(((AnnualValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitMonthly(BaseUsMonthly monthly);

        T visitAnnual(BaseUsAnnual annual);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "billingFrequency",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(MonthlyValue.class), @JsonSubTypes.Type(AnnualValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("monthly")
    @JsonIgnoreProperties("billingFrequency")
    private static final class MonthlyValue implements Value {
        @JsonUnwrapped
        private BaseUsMonthly value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MonthlyValue() {}

        private MonthlyValue(BaseUsMonthly value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMonthly(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MonthlyValue && equalTo((MonthlyValue) other);
        }

        private boolean equalTo(MonthlyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BaseUsPlatinumSecurity{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("annual")
    @JsonIgnoreProperties("billingFrequency")
    private static final class AnnualValue implements Value {
        @JsonUnwrapped
        private BaseUsAnnual value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AnnualValue() {}

        private AnnualValue(BaseUsAnnual value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAnnual(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AnnualValue && equalTo((AnnualValue) other);
        }

        private boolean equalTo(AnnualValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BaseUsPlatinumSecurity{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("billingFrequency")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "BaseUsPlatinumSecurity{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
