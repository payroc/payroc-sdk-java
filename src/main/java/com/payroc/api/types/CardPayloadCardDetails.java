/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CardPayloadCardDetails {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CardPayloadCardDetails(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CardPayloadCardDetails raw(RawCardDetails value) {
        return new CardPayloadCardDetails(new RawValue(value));
    }

    public static CardPayloadCardDetails icc(IccCardDetails value) {
        return new CardPayloadCardDetails(new IccValue(value));
    }

    public static CardPayloadCardDetails keyed(KeyedCardDetails value) {
        return new CardPayloadCardDetails(new KeyedValue(value));
    }

    public static CardPayloadCardDetails swiped(SwipedCardDetails value) {
        return new CardPayloadCardDetails(new SwipedValue(value));
    }

    public boolean isRaw() {
        return value instanceof RawValue;
    }

    public boolean isIcc() {
        return value instanceof IccValue;
    }

    public boolean isKeyed() {
        return value instanceof KeyedValue;
    }

    public boolean isSwiped() {
        return value instanceof SwipedValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<RawCardDetails> getRaw() {
        if (isRaw()) {
            return Optional.of(((RawValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<IccCardDetails> getIcc() {
        if (isIcc()) {
            return Optional.of(((IccValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<KeyedCardDetails> getKeyed() {
        if (isKeyed()) {
            return Optional.of(((KeyedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SwipedCardDetails> getSwiped() {
        if (isSwiped()) {
            return Optional.of(((SwipedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitRaw(RawCardDetails raw);

        T visitIcc(IccCardDetails icc);

        T visitKeyed(KeyedCardDetails keyed);

        T visitSwiped(SwipedCardDetails swiped);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "entryMethod",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(RawValue.class),
        @JsonSubTypes.Type(IccValue.class),
        @JsonSubTypes.Type(KeyedValue.class),
        @JsonSubTypes.Type(SwipedValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("raw")
    @JsonIgnoreProperties("entryMethod")
    private static final class RawValue implements Value {
        @JsonUnwrapped
        private RawCardDetails value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RawValue() {}

        private RawValue(RawCardDetails value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRaw(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RawValue && equalTo((RawValue) other);
        }

        private boolean equalTo(RawValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CardPayloadCardDetails{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("icc")
    @JsonIgnoreProperties("entryMethod")
    private static final class IccValue implements Value {
        @JsonUnwrapped
        private IccCardDetails value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private IccValue() {}

        private IccValue(IccCardDetails value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitIcc(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof IccValue && equalTo((IccValue) other);
        }

        private boolean equalTo(IccValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CardPayloadCardDetails{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("keyed")
    @JsonIgnoreProperties("entryMethod")
    private static final class KeyedValue implements Value {
        @JsonUnwrapped
        private KeyedCardDetails value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private KeyedValue() {}

        private KeyedValue(KeyedCardDetails value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitKeyed(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof KeyedValue && equalTo((KeyedValue) other);
        }

        private boolean equalTo(KeyedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CardPayloadCardDetails{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("swiped")
    @JsonIgnoreProperties("entryMethod")
    private static final class SwipedValue implements Value {
        @JsonUnwrapped
        private SwipedCardDetails value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SwipedValue() {}

        private SwipedValue(SwipedCardDetails value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSwiped(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SwipedValue && equalTo((SwipedValue) other);
        }

        private boolean equalTo(SwipedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CardPayloadCardDetails{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("entryMethod")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CardPayloadCardDetails{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
