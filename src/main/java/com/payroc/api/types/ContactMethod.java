/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class ContactMethod {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private ContactMethod(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static ContactMethod email(ContactMethodEmail value) {
        return new ContactMethod(new EmailValue(value));
    }

    public static ContactMethod phone(ContactMethodPhone value) {
        return new ContactMethod(new PhoneValue(value));
    }

    public static ContactMethod mobile(ContactMethodMobile value) {
        return new ContactMethod(new MobileValue(value));
    }

    public static ContactMethod fax(ContactMethodFax value) {
        return new ContactMethod(new FaxValue(value));
    }

    public boolean isEmail() {
        return value instanceof EmailValue;
    }

    public boolean isPhone() {
        return value instanceof PhoneValue;
    }

    public boolean isMobile() {
        return value instanceof MobileValue;
    }

    public boolean isFax() {
        return value instanceof FaxValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<ContactMethodEmail> getEmail() {
        if (isEmail()) {
            return Optional.of(((EmailValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ContactMethodPhone> getPhone() {
        if (isPhone()) {
            return Optional.of(((PhoneValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ContactMethodMobile> getMobile() {
        if (isMobile()) {
            return Optional.of(((MobileValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ContactMethodFax> getFax() {
        if (isFax()) {
            return Optional.of(((FaxValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitEmail(ContactMethodEmail email);

        T visitPhone(ContactMethodPhone phone);

        T visitMobile(ContactMethodMobile mobile);

        T visitFax(ContactMethodFax fax);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(EmailValue.class),
        @JsonSubTypes.Type(PhoneValue.class),
        @JsonSubTypes.Type(MobileValue.class),
        @JsonSubTypes.Type(FaxValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("email")
    @JsonIgnoreProperties("type")
    private static final class EmailValue implements Value {
        @JsonUnwrapped
        private ContactMethodEmail value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EmailValue() {}

        private EmailValue(ContactMethodEmail value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEmail(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EmailValue && equalTo((EmailValue) other);
        }

        private boolean equalTo(EmailValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ContactMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("phone")
    @JsonIgnoreProperties("type")
    private static final class PhoneValue implements Value {
        @JsonUnwrapped
        private ContactMethodPhone value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PhoneValue() {}

        private PhoneValue(ContactMethodPhone value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPhone(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PhoneValue && equalTo((PhoneValue) other);
        }

        private boolean equalTo(PhoneValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ContactMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("mobile")
    @JsonIgnoreProperties("type")
    private static final class MobileValue implements Value {
        @JsonUnwrapped
        private ContactMethodMobile value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MobileValue() {}

        private MobileValue(ContactMethodMobile value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMobile(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MobileValue && equalTo((MobileValue) other);
        }

        private boolean equalTo(MobileValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ContactMethod{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("fax")
    @JsonIgnoreProperties("type")
    private static final class FaxValue implements Value {
        @JsonUnwrapped
        private ContactMethodFax value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FaxValue() {}

        private FaxValue(ContactMethodFax value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFax(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FaxValue && equalTo((FaxValue) other);
        }

        private boolean equalTo(FaxValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ContactMethod{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ContactMethod{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
