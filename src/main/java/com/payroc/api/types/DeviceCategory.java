/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DeviceCategory {
    public static final DeviceCategory UNATTENDED = new DeviceCategory(Value.UNATTENDED, "unattended");

    public static final DeviceCategory ATTENDED = new DeviceCategory(Value.ATTENDED, "attended");

    private final Value value;

    private final String string;

    DeviceCategory(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof DeviceCategory && this.string.equals(((DeviceCategory) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case UNATTENDED:
                return visitor.visitUnattended();
            case ATTENDED:
                return visitor.visitAttended();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DeviceCategory valueOf(String value) {
        switch (value) {
            case "unattended":
                return UNATTENDED;
            case "attended":
                return ATTENDED;
            default:
                return new DeviceCategory(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ATTENDED,

        UNATTENDED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAttended();

        T visitUnattended();

        T visitUnknown(String unknownType);
    }
}
