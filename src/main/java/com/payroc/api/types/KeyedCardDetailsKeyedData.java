/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class KeyedCardDetailsKeyedData {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private KeyedCardDetailsKeyedData(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static KeyedCardDetailsKeyedData fullyEncrypted(FullyEncryptedKeyedDataFormat value) {
        return new KeyedCardDetailsKeyedData(new FullyEncryptedValue(value));
    }

    public static KeyedCardDetailsKeyedData partiallyEncrypted(PartiallyEncryptedKeyedDataFormat value) {
        return new KeyedCardDetailsKeyedData(new PartiallyEncryptedValue(value));
    }

    public static KeyedCardDetailsKeyedData plainText(PlainTextKeyedDataFormat value) {
        return new KeyedCardDetailsKeyedData(new PlainTextValue(value));
    }

    public boolean isFullyEncrypted() {
        return value instanceof FullyEncryptedValue;
    }

    public boolean isPartiallyEncrypted() {
        return value instanceof PartiallyEncryptedValue;
    }

    public boolean isPlainText() {
        return value instanceof PlainTextValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<FullyEncryptedKeyedDataFormat> getFullyEncrypted() {
        if (isFullyEncrypted()) {
            return Optional.of(((FullyEncryptedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PartiallyEncryptedKeyedDataFormat> getPartiallyEncrypted() {
        if (isPartiallyEncrypted()) {
            return Optional.of(((PartiallyEncryptedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PlainTextKeyedDataFormat> getPlainText() {
        if (isPlainText()) {
            return Optional.of(((PlainTextValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitFullyEncrypted(FullyEncryptedKeyedDataFormat fullyEncrypted);

        T visitPartiallyEncrypted(PartiallyEncryptedKeyedDataFormat partiallyEncrypted);

        T visitPlainText(PlainTextKeyedDataFormat plainText);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "dataFormat",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(FullyEncryptedValue.class),
        @JsonSubTypes.Type(PartiallyEncryptedValue.class),
        @JsonSubTypes.Type(PlainTextValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("fullyEncrypted")
    @JsonIgnoreProperties("dataFormat")
    private static final class FullyEncryptedValue implements Value {
        @JsonUnwrapped
        private FullyEncryptedKeyedDataFormat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FullyEncryptedValue() {}

        private FullyEncryptedValue(FullyEncryptedKeyedDataFormat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFullyEncrypted(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FullyEncryptedValue && equalTo((FullyEncryptedValue) other);
        }

        private boolean equalTo(FullyEncryptedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "KeyedCardDetailsKeyedData{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("partiallyEncrypted")
    @JsonIgnoreProperties("dataFormat")
    private static final class PartiallyEncryptedValue implements Value {
        @JsonUnwrapped
        private PartiallyEncryptedKeyedDataFormat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PartiallyEncryptedValue() {}

        private PartiallyEncryptedValue(PartiallyEncryptedKeyedDataFormat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPartiallyEncrypted(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PartiallyEncryptedValue && equalTo((PartiallyEncryptedValue) other);
        }

        private boolean equalTo(PartiallyEncryptedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "KeyedCardDetailsKeyedData{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("plainText")
    @JsonIgnoreProperties("dataFormat")
    private static final class PlainTextValue implements Value {
        @JsonUnwrapped
        private PlainTextKeyedDataFormat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PlainTextValue() {}

        private PlainTextValue(PlainTextKeyedDataFormat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPlainText(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PlainTextValue && equalTo((PlainTextValue) other);
        }

        private boolean equalTo(PlainTextValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "KeyedCardDetailsKeyedData{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("dataFormat")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "KeyedCardDetailsKeyedData{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
