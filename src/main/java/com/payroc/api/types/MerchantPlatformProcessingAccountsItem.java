/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = MerchantPlatformProcessingAccountsItem.Builder.class)
public final class MerchantPlatformProcessingAccountsItem {
    private final Optional<String> processingAccountId;

    private final String doingBusinessAs;

    private final Optional<MerchantPlatformProcessingAccountsItemStatus> status;

    private final Optional<MerchantPlatformProcessingAccountsItemLink> link;

    private final Optional<Signature> signature;

    private final Map<String, Object> additionalProperties;

    private MerchantPlatformProcessingAccountsItem(
            Optional<String> processingAccountId,
            String doingBusinessAs,
            Optional<MerchantPlatformProcessingAccountsItemStatus> status,
            Optional<MerchantPlatformProcessingAccountsItemLink> link,
            Optional<Signature> signature,
            Map<String, Object> additionalProperties) {
        this.processingAccountId = processingAccountId;
        this.doingBusinessAs = doingBusinessAs;
        this.status = status;
        this.link = link;
        this.signature = signature;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Unique identifier that we assigned to the processing account.
     */
    @JsonProperty("processingAccountId")
    public Optional<String> getProcessingAccountId() {
        return processingAccountId;
    }

    /**
     * @return Trading name of the business.
     */
    @JsonProperty("doingBusinessAs")
    public String getDoingBusinessAs() {
        return doingBusinessAs;
    }

    /**
     * @return Status of the processing account.
     * <ul>
     * <li><code>entered</code> - We have received information about the account, but we have not yet reviewed it.</li>
     * <li><code>pending</code> - We have reviewed the information about the account, but we have not yet approved it.</li>
     * <li><code>approved</code> - We have approved the account for processing transactions and funding.</li>
     * <li><code>subjectTo</code> - We have approved the account, but we are waiting on further information.</li>
     * <li><code>dormant</code> - Account is closed for a period.</li>
     * <li><code>nonProcessing</code> - We have approved the account, but the merchant has not yet run a transaction.</li>
     * <li><code>rejected</code> - We rejected the application for the processing account.</li>
     * <li><code>terminated</code> - Processing account is closed.</li>
     * <li><code>cancelled</code> - Merchant withdrew the application for the processing account.</li>
     * <li><code>failed</code> - An error occurred while we were setting up the processing account.</li>
     * </ul>
     * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we change the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/guides/integrate/event-subscriptions">Event Subscriptions</a>.</p>
     */
    @JsonProperty("status")
    public Optional<MerchantPlatformProcessingAccountsItemStatus> getStatus() {
        return status;
    }

    /**
     * @return Object that contains HATEOAS links for the processing account.
     */
    @JsonProperty("link")
    public Optional<MerchantPlatformProcessingAccountsItemLink> getLink() {
        return link;
    }

    @JsonProperty("signature")
    public Optional<Signature> getSignature() {
        return signature;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof MerchantPlatformProcessingAccountsItem
                && equalTo((MerchantPlatformProcessingAccountsItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(MerchantPlatformProcessingAccountsItem other) {
        return processingAccountId.equals(other.processingAccountId)
                && doingBusinessAs.equals(other.doingBusinessAs)
                && status.equals(other.status)
                && link.equals(other.link)
                && signature.equals(other.signature);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.processingAccountId, this.doingBusinessAs, this.status, this.link, this.signature);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static DoingBusinessAsStage builder() {
        return new Builder();
    }

    public interface DoingBusinessAsStage {
        /**
         * <p>Trading name of the business.</p>
         */
        _FinalStage doingBusinessAs(@NotNull String doingBusinessAs);

        Builder from(MerchantPlatformProcessingAccountsItem other);
    }

    public interface _FinalStage {
        MerchantPlatformProcessingAccountsItem build();

        /**
         * <p>Unique identifier that we assigned to the processing account.</p>
         */
        _FinalStage processingAccountId(Optional<String> processingAccountId);

        _FinalStage processingAccountId(String processingAccountId);

        /**
         * <p>Status of the processing account.</p>
         * <ul>
         * <li><code>entered</code> - We have received information about the account, but we have not yet reviewed it.</li>
         * <li><code>pending</code> - We have reviewed the information about the account, but we have not yet approved it.</li>
         * <li><code>approved</code> - We have approved the account for processing transactions and funding.</li>
         * <li><code>subjectTo</code> - We have approved the account, but we are waiting on further information.</li>
         * <li><code>dormant</code> - Account is closed for a period.</li>
         * <li><code>nonProcessing</code> - We have approved the account, but the merchant has not yet run a transaction.</li>
         * <li><code>rejected</code> - We rejected the application for the processing account.</li>
         * <li><code>terminated</code> - Processing account is closed.</li>
         * <li><code>cancelled</code> - Merchant withdrew the application for the processing account.</li>
         * <li><code>failed</code> - An error occurred while we were setting up the processing account.</li>
         * </ul>
         * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we change the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/guides/integrate/event-subscriptions">Event Subscriptions</a>.</p>
         */
        _FinalStage status(Optional<MerchantPlatformProcessingAccountsItemStatus> status);

        _FinalStage status(MerchantPlatformProcessingAccountsItemStatus status);

        /**
         * <p>Object that contains HATEOAS links for the processing account.</p>
         */
        _FinalStage link(Optional<MerchantPlatformProcessingAccountsItemLink> link);

        _FinalStage link(MerchantPlatformProcessingAccountsItemLink link);

        _FinalStage signature(Optional<Signature> signature);

        _FinalStage signature(Signature signature);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements DoingBusinessAsStage, _FinalStage {
        private String doingBusinessAs;

        private Optional<Signature> signature = Optional.empty();

        private Optional<MerchantPlatformProcessingAccountsItemLink> link = Optional.empty();

        private Optional<MerchantPlatformProcessingAccountsItemStatus> status = Optional.empty();

        private Optional<String> processingAccountId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(MerchantPlatformProcessingAccountsItem other) {
            processingAccountId(other.getProcessingAccountId());
            doingBusinessAs(other.getDoingBusinessAs());
            status(other.getStatus());
            link(other.getLink());
            signature(other.getSignature());
            return this;
        }

        /**
         * <p>Trading name of the business.</p>
         * <p>Trading name of the business.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("doingBusinessAs")
        public _FinalStage doingBusinessAs(@NotNull String doingBusinessAs) {
            this.doingBusinessAs = Objects.requireNonNull(doingBusinessAs, "doingBusinessAs must not be null");
            return this;
        }

        @java.lang.Override
        public _FinalStage signature(Signature signature) {
            this.signature = Optional.ofNullable(signature);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "signature", nulls = Nulls.SKIP)
        public _FinalStage signature(Optional<Signature> signature) {
            this.signature = signature;
            return this;
        }

        /**
         * <p>Object that contains HATEOAS links for the processing account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage link(MerchantPlatformProcessingAccountsItemLink link) {
            this.link = Optional.ofNullable(link);
            return this;
        }

        /**
         * <p>Object that contains HATEOAS links for the processing account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "link", nulls = Nulls.SKIP)
        public _FinalStage link(Optional<MerchantPlatformProcessingAccountsItemLink> link) {
            this.link = link;
            return this;
        }

        /**
         * <p>Status of the processing account.</p>
         * <ul>
         * <li><code>entered</code> - We have received information about the account, but we have not yet reviewed it.</li>
         * <li><code>pending</code> - We have reviewed the information about the account, but we have not yet approved it.</li>
         * <li><code>approved</code> - We have approved the account for processing transactions and funding.</li>
         * <li><code>subjectTo</code> - We have approved the account, but we are waiting on further information.</li>
         * <li><code>dormant</code> - Account is closed for a period.</li>
         * <li><code>nonProcessing</code> - We have approved the account, but the merchant has not yet run a transaction.</li>
         * <li><code>rejected</code> - We rejected the application for the processing account.</li>
         * <li><code>terminated</code> - Processing account is closed.</li>
         * <li><code>cancelled</code> - Merchant withdrew the application for the processing account.</li>
         * <li><code>failed</code> - An error occurred while we were setting up the processing account.</li>
         * </ul>
         * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we change the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/guides/integrate/event-subscriptions">Event Subscriptions</a>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(MerchantPlatformProcessingAccountsItemStatus status) {
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * <p>Status of the processing account.</p>
         * <ul>
         * <li><code>entered</code> - We have received information about the account, but we have not yet reviewed it.</li>
         * <li><code>pending</code> - We have reviewed the information about the account, but we have not yet approved it.</li>
         * <li><code>approved</code> - We have approved the account for processing transactions and funding.</li>
         * <li><code>subjectTo</code> - We have approved the account, but we are waiting on further information.</li>
         * <li><code>dormant</code> - Account is closed for a period.</li>
         * <li><code>nonProcessing</code> - We have approved the account, but the merchant has not yet run a transaction.</li>
         * <li><code>rejected</code> - We rejected the application for the processing account.</li>
         * <li><code>terminated</code> - Processing account is closed.</li>
         * <li><code>cancelled</code> - Merchant withdrew the application for the processing account.</li>
         * <li><code>failed</code> - An error occurred while we were setting up the processing account.</li>
         * </ul>
         * <p><strong>Note</strong>: You can subscribe to our processingAccount.status.changed event to get notifications when we change the status of a processing account. For more information about how to subscribe to events, go to <a href="https://docs.payroc.com/guides/integrate/event-subscriptions">Event Subscriptions</a>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status", nulls = Nulls.SKIP)
        public _FinalStage status(Optional<MerchantPlatformProcessingAccountsItemStatus> status) {
            this.status = status;
            return this;
        }

        /**
         * <p>Unique identifier that we assigned to the processing account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage processingAccountId(String processingAccountId) {
            this.processingAccountId = Optional.ofNullable(processingAccountId);
            return this;
        }

        /**
         * <p>Unique identifier that we assigned to the processing account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "processingAccountId", nulls = Nulls.SKIP)
        public _FinalStage processingAccountId(Optional<String> processingAccountId) {
            this.processingAccountId = processingAccountId;
            return this;
        }

        @java.lang.Override
        public MerchantPlatformProcessingAccountsItem build() {
            return new MerchantPlatformProcessingAccountsItem(
                    processingAccountId, doingBusinessAs, status, link, signature, additionalProperties);
        }
    }
}
