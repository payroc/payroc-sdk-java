/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class MultiUsePaymentLinkOrderCharge {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private MultiUsePaymentLinkOrderCharge(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static MultiUsePaymentLinkOrderCharge prompt(PromptPaymentLinkCharge value) {
        return new MultiUsePaymentLinkOrderCharge(new PromptValue(value));
    }

    public static MultiUsePaymentLinkOrderCharge preset(PresetPaymentLinkCharge value) {
        return new MultiUsePaymentLinkOrderCharge(new PresetValue(value));
    }

    public boolean isPrompt() {
        return value instanceof PromptValue;
    }

    public boolean isPreset() {
        return value instanceof PresetValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PromptPaymentLinkCharge> getPrompt() {
        if (isPrompt()) {
            return Optional.of(((PromptValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PresetPaymentLinkCharge> getPreset() {
        if (isPreset()) {
            return Optional.of(((PresetValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitPrompt(PromptPaymentLinkCharge prompt);

        T visitPreset(PresetPaymentLinkCharge preset);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(PromptValue.class), @JsonSubTypes.Type(PresetValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("prompt")
    @JsonIgnoreProperties("type")
    private static final class PromptValue implements Value {
        @JsonUnwrapped
        private PromptPaymentLinkCharge value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PromptValue() {}

        private PromptValue(PromptPaymentLinkCharge value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPrompt(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PromptValue && equalTo((PromptValue) other);
        }

        private boolean equalTo(PromptValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MultiUsePaymentLinkOrderCharge{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("preset")
    @JsonIgnoreProperties("type")
    private static final class PresetValue implements Value {
        @JsonUnwrapped
        private PresetPaymentLinkCharge value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PresetValue() {}

        private PresetValue(PresetPaymentLinkCharge value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPreset(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PresetValue && equalTo((PresetValue) other);
        }

        private boolean equalTo(PresetValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MultiUsePaymentLinkOrderCharge{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MultiUsePaymentLinkOrderCharge{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
