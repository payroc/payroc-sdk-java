/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PatchDocument {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PatchDocument(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PatchDocument add(PatchAdd value) {
        return new PatchDocument(new AddValue(value));
    }

    public static PatchDocument remove(PatchRemove value) {
        return new PatchDocument(new RemoveValue(value));
    }

    public static PatchDocument replace(PatchReplace value) {
        return new PatchDocument(new ReplaceValue(value));
    }

    public static PatchDocument move(PatchMove value) {
        return new PatchDocument(new MoveValue(value));
    }

    public static PatchDocument copy(PatchCopy value) {
        return new PatchDocument(new CopyValue(value));
    }

    public static PatchDocument test(PatchTest value) {
        return new PatchDocument(new TestValue(value));
    }

    public boolean isAdd() {
        return value instanceof AddValue;
    }

    public boolean isRemove() {
        return value instanceof RemoveValue;
    }

    public boolean isReplace() {
        return value instanceof ReplaceValue;
    }

    public boolean isMove() {
        return value instanceof MoveValue;
    }

    public boolean isCopy() {
        return value instanceof CopyValue;
    }

    public boolean isTest() {
        return value instanceof TestValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PatchAdd> getAdd() {
        if (isAdd()) {
            return Optional.of(((AddValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PatchRemove> getRemove() {
        if (isRemove()) {
            return Optional.of(((RemoveValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PatchReplace> getReplace() {
        if (isReplace()) {
            return Optional.of(((ReplaceValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PatchMove> getMove() {
        if (isMove()) {
            return Optional.of(((MoveValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PatchCopy> getCopy() {
        if (isCopy()) {
            return Optional.of(((CopyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PatchTest> getTest() {
        if (isTest()) {
            return Optional.of(((TestValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAdd(PatchAdd add);

        T visitRemove(PatchRemove remove);

        T visitReplace(PatchReplace replace);

        T visitMove(PatchMove move);

        T visitCopy(PatchCopy copy);

        T visitTest(PatchTest test);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "op", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(AddValue.class),
        @JsonSubTypes.Type(RemoveValue.class),
        @JsonSubTypes.Type(ReplaceValue.class),
        @JsonSubTypes.Type(MoveValue.class),
        @JsonSubTypes.Type(CopyValue.class),
        @JsonSubTypes.Type(TestValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("add")
    @JsonIgnoreProperties("op")
    private static final class AddValue implements Value {
        @JsonUnwrapped
        private PatchAdd value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AddValue() {}

        private AddValue(PatchAdd value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAdd(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AddValue && equalTo((AddValue) other);
        }

        private boolean equalTo(AddValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("remove")
    @JsonIgnoreProperties("op")
    private static final class RemoveValue implements Value {
        @JsonUnwrapped
        private PatchRemove value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RemoveValue() {}

        private RemoveValue(PatchRemove value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRemove(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RemoveValue && equalTo((RemoveValue) other);
        }

        private boolean equalTo(RemoveValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("replace")
    @JsonIgnoreProperties("op")
    private static final class ReplaceValue implements Value {
        @JsonUnwrapped
        private PatchReplace value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ReplaceValue() {}

        private ReplaceValue(PatchReplace value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitReplace(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ReplaceValue && equalTo((ReplaceValue) other);
        }

        private boolean equalTo(ReplaceValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("move")
    @JsonIgnoreProperties("op")
    private static final class MoveValue implements Value {
        @JsonUnwrapped
        private PatchMove value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MoveValue() {}

        private MoveValue(PatchMove value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMove(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MoveValue && equalTo((MoveValue) other);
        }

        private boolean equalTo(MoveValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("copy")
    @JsonIgnoreProperties("op")
    private static final class CopyValue implements Value {
        @JsonUnwrapped
        private PatchCopy value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CopyValue() {}

        private CopyValue(PatchCopy value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCopy(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CopyValue && equalTo((CopyValue) other);
        }

        private boolean equalTo(CopyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("test")
    @JsonIgnoreProperties("op")
    private static final class TestValue implements Value {
        @JsonUnwrapped
        private PatchTest value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TestValue() {}

        private TestValue(PatchTest value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTest(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TestValue && equalTo((TestValue) other);
        }

        private boolean equalTo(TestValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("op")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PatchDocument{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
