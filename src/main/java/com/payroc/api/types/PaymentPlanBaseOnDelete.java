/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class PaymentPlanBaseOnDelete {
    public static final PaymentPlanBaseOnDelete COMPLETE = new PaymentPlanBaseOnDelete(Value.COMPLETE, "complete");

    public static final PaymentPlanBaseOnDelete CONTINUE = new PaymentPlanBaseOnDelete(Value.CONTINUE, "continue");

    private final Value value;

    private final String string;

    PaymentPlanBaseOnDelete(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof PaymentPlanBaseOnDelete
                        && this.string.equals(((PaymentPlanBaseOnDelete) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case COMPLETE:
                return visitor.visitComplete();
            case CONTINUE:
                return visitor.visitContinue();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static PaymentPlanBaseOnDelete valueOf(String value) {
        switch (value) {
            case "complete":
                return COMPLETE;
            case "continue":
                return CONTINUE;
            default:
                return new PaymentPlanBaseOnDelete(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        COMPLETE,

        CONTINUE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitComplete();

        T visitContinue();

        T visitUnknown(String unknownType);
    }
}
