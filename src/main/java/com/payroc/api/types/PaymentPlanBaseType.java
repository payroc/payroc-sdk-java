/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class PaymentPlanBaseType {
    public static final PaymentPlanBaseType MANUAL = new PaymentPlanBaseType(Value.MANUAL, "manual");

    public static final PaymentPlanBaseType AUTOMATIC = new PaymentPlanBaseType(Value.AUTOMATIC, "automatic");

    private final Value value;

    private final String string;

    PaymentPlanBaseType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof PaymentPlanBaseType && this.string.equals(((PaymentPlanBaseType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case MANUAL:
                return visitor.visitManual();
            case AUTOMATIC:
                return visitor.visitAutomatic();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static PaymentPlanBaseType valueOf(String value) {
        switch (value) {
            case "manual":
                return MANUAL;
            case "automatic":
                return AUTOMATIC;
            default:
                return new PaymentPlanBaseType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        MANUAL,

        AUTOMATIC,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitManual();

        T visitAutomatic();

        T visitUnknown(String unknownType);
    }
}
