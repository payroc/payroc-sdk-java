/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PricingAgreementUs50ProcessorCard {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PricingAgreementUs50ProcessorCard(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PricingAgreementUs50ProcessorCard interchangePlus(InterchangePlus value) {
        return new PricingAgreementUs50ProcessorCard(new InterchangePlusValue(value));
    }

    public static PricingAgreementUs50ProcessorCard interchangePlusPlus(InterchangePlusPlus value) {
        return new PricingAgreementUs50ProcessorCard(new InterchangePlusPlusValue(value));
    }

    public static PricingAgreementUs50ProcessorCard tiered3(Tiered3 value) {
        return new PricingAgreementUs50ProcessorCard(new Tiered3Value(value));
    }

    public static PricingAgreementUs50ProcessorCard tiered4(Tiered4 value) {
        return new PricingAgreementUs50ProcessorCard(new Tiered4Value(value));
    }

    public static PricingAgreementUs50ProcessorCard tiered6(Tiered6 value) {
        return new PricingAgreementUs50ProcessorCard(new Tiered6Value(value));
    }

    public static PricingAgreementUs50ProcessorCard flatRate(FlatRate value) {
        return new PricingAgreementUs50ProcessorCard(new FlatRateValue(value));
    }

    public static PricingAgreementUs50ProcessorCard consumerChoice(ConsumerChoice value) {
        return new PricingAgreementUs50ProcessorCard(new ConsumerChoiceValue(value));
    }

    public static PricingAgreementUs50ProcessorCard rewardPayChoice(RewardPayChoice value) {
        return new PricingAgreementUs50ProcessorCard(new RewardPayChoiceValue(value));
    }

    public boolean isInterchangePlus() {
        return value instanceof InterchangePlusValue;
    }

    public boolean isInterchangePlusPlus() {
        return value instanceof InterchangePlusPlusValue;
    }

    public boolean isTiered3() {
        return value instanceof Tiered3Value;
    }

    public boolean isTiered4() {
        return value instanceof Tiered4Value;
    }

    public boolean isTiered6() {
        return value instanceof Tiered6Value;
    }

    public boolean isFlatRate() {
        return value instanceof FlatRateValue;
    }

    public boolean isConsumerChoice() {
        return value instanceof ConsumerChoiceValue;
    }

    public boolean isRewardPayChoice() {
        return value instanceof RewardPayChoiceValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<InterchangePlus> getInterchangePlus() {
        if (isInterchangePlus()) {
            return Optional.of(((InterchangePlusValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<InterchangePlusPlus> getInterchangePlusPlus() {
        if (isInterchangePlusPlus()) {
            return Optional.of(((InterchangePlusPlusValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Tiered3> getTiered3() {
        if (isTiered3()) {
            return Optional.of(((Tiered3Value) value).value);
        }
        return Optional.empty();
    }

    public Optional<Tiered4> getTiered4() {
        if (isTiered4()) {
            return Optional.of(((Tiered4Value) value).value);
        }
        return Optional.empty();
    }

    public Optional<Tiered6> getTiered6() {
        if (isTiered6()) {
            return Optional.of(((Tiered6Value) value).value);
        }
        return Optional.empty();
    }

    public Optional<FlatRate> getFlatRate() {
        if (isFlatRate()) {
            return Optional.of(((FlatRateValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ConsumerChoice> getConsumerChoice() {
        if (isConsumerChoice()) {
            return Optional.of(((ConsumerChoiceValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<RewardPayChoice> getRewardPayChoice() {
        if (isRewardPayChoice()) {
            return Optional.of(((RewardPayChoiceValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitInterchangePlus(InterchangePlus interchangePlus);

        T visitInterchangePlusPlus(InterchangePlusPlus interchangePlusPlus);

        T visitTiered3(Tiered3 tiered3);

        T visitTiered4(Tiered4 tiered4);

        T visitTiered6(Tiered6 tiered6);

        T visitFlatRate(FlatRate flatRate);

        T visitConsumerChoice(ConsumerChoice consumerChoice);

        T visitRewardPayChoice(RewardPayChoice rewardPayChoice);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "planType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(InterchangePlusValue.class),
        @JsonSubTypes.Type(InterchangePlusPlusValue.class),
        @JsonSubTypes.Type(Tiered3Value.class),
        @JsonSubTypes.Type(Tiered4Value.class),
        @JsonSubTypes.Type(Tiered6Value.class),
        @JsonSubTypes.Type(FlatRateValue.class),
        @JsonSubTypes.Type(ConsumerChoiceValue.class),
        @JsonSubTypes.Type(RewardPayChoiceValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("interchangePlus")
    @JsonIgnoreProperties("planType")
    private static final class InterchangePlusValue implements Value {
        @JsonUnwrapped
        private InterchangePlus value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private InterchangePlusValue() {}

        private InterchangePlusValue(InterchangePlus value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitInterchangePlus(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof InterchangePlusValue && equalTo((InterchangePlusValue) other);
        }

        private boolean equalTo(InterchangePlusValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("interchangePlusPlus")
    @JsonIgnoreProperties("planType")
    private static final class InterchangePlusPlusValue implements Value {
        @JsonUnwrapped
        private InterchangePlusPlus value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private InterchangePlusPlusValue() {}

        private InterchangePlusPlusValue(InterchangePlusPlus value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitInterchangePlusPlus(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof InterchangePlusPlusValue && equalTo((InterchangePlusPlusValue) other);
        }

        private boolean equalTo(InterchangePlusPlusValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("tiered3")
    @JsonIgnoreProperties("planType")
    private static final class Tiered3Value implements Value {
        @JsonUnwrapped
        private Tiered3 value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private Tiered3Value() {}

        private Tiered3Value(Tiered3 value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTiered3(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof Tiered3Value && equalTo((Tiered3Value) other);
        }

        private boolean equalTo(Tiered3Value other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("tiered4")
    @JsonIgnoreProperties("planType")
    private static final class Tiered4Value implements Value {
        @JsonUnwrapped
        private Tiered4 value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private Tiered4Value() {}

        private Tiered4Value(Tiered4 value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTiered4(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof Tiered4Value && equalTo((Tiered4Value) other);
        }

        private boolean equalTo(Tiered4Value other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("tiered6")
    @JsonIgnoreProperties("planType")
    private static final class Tiered6Value implements Value {
        @JsonUnwrapped
        private Tiered6 value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private Tiered6Value() {}

        private Tiered6Value(Tiered6 value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTiered6(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof Tiered6Value && equalTo((Tiered6Value) other);
        }

        private boolean equalTo(Tiered6Value other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("flatRate")
    @JsonIgnoreProperties("planType")
    private static final class FlatRateValue implements Value {
        @JsonUnwrapped
        private FlatRate value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FlatRateValue() {}

        private FlatRateValue(FlatRate value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFlatRate(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FlatRateValue && equalTo((FlatRateValue) other);
        }

        private boolean equalTo(FlatRateValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("consumerChoice")
    @JsonIgnoreProperties("planType")
    private static final class ConsumerChoiceValue implements Value {
        @JsonUnwrapped
        private ConsumerChoice value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ConsumerChoiceValue() {}

        private ConsumerChoiceValue(ConsumerChoice value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitConsumerChoice(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ConsumerChoiceValue && equalTo((ConsumerChoiceValue) other);
        }

        private boolean equalTo(ConsumerChoiceValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("rewardPayChoice")
    @JsonIgnoreProperties("planType")
    private static final class RewardPayChoiceValue implements Value {
        @JsonUnwrapped
        private RewardPayChoice value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RewardPayChoiceValue() {}

        private RewardPayChoiceValue(RewardPayChoice value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRewardPayChoice(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RewardPayChoiceValue && equalTo((RewardPayChoiceValue) other);
        }

        private boolean equalTo(RewardPayChoiceValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("planType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PricingAgreementUs50ProcessorCard{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
