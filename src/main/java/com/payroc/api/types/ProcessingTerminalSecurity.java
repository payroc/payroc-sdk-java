/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ProcessingTerminalSecurity.Builder.class)
public final class ProcessingTerminalSecurity {
    private final boolean tokenization;

    private final boolean avsPrompt;

    private final Optional<ProcessingTerminalSecurityAvsLevel> avsLevel;

    private final boolean cvvPrompt;

    private final Map<String, Object> additionalProperties;

    private ProcessingTerminalSecurity(
            boolean tokenization,
            boolean avsPrompt,
            Optional<ProcessingTerminalSecurityAvsLevel> avsLevel,
            boolean cvvPrompt,
            Map<String, Object> additionalProperties) {
        this.tokenization = tokenization;
        this.avsPrompt = avsPrompt;
        this.avsLevel = avsLevel;
        this.cvvPrompt = cvvPrompt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Indicates if the terminal can tokenize customer's payment details. For more information about tokenization, go to <a href="https://docs.payroc.com/knowledge/basic-concepts/tokenization">Tokenization</a>.
     */
    @JsonProperty("tokenization")
    public boolean getTokenization() {
        return tokenization;
    }

    /**
     * @return Indicates if the terminal should prompt for Address Verification Service (AVS) details when running a transaction.
     */
    @JsonProperty("avsPrompt")
    public boolean getAvsPrompt() {
        return avsPrompt;
    }

    /**
     * @return Indicates the level of AVS details that the terminal should prompt for.
     */
    @JsonProperty("avsLevel")
    public Optional<ProcessingTerminalSecurityAvsLevel> getAvsLevel() {
        return avsLevel;
    }

    /**
     * @return Indicates if the terminal should prompt for a Card Verfication Value (CVV) when running a transaction.
     */
    @JsonProperty("cvvPrompt")
    public boolean getCvvPrompt() {
        return cvvPrompt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ProcessingTerminalSecurity && equalTo((ProcessingTerminalSecurity) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ProcessingTerminalSecurity other) {
        return tokenization == other.tokenization
                && avsPrompt == other.avsPrompt
                && avsLevel.equals(other.avsLevel)
                && cvvPrompt == other.cvvPrompt;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.tokenization, this.avsPrompt, this.avsLevel, this.cvvPrompt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TokenizationStage builder() {
        return new Builder();
    }

    public interface TokenizationStage {
        /**
         * <p>Indicates if the terminal can tokenize customer's payment details. For more information about tokenization, go to <a href="https://docs.payroc.com/knowledge/basic-concepts/tokenization">Tokenization</a>.</p>
         */
        AvsPromptStage tokenization(boolean tokenization);

        Builder from(ProcessingTerminalSecurity other);
    }

    public interface AvsPromptStage {
        /**
         * <p>Indicates if the terminal should prompt for Address Verification Service (AVS) details when running a transaction.</p>
         */
        CvvPromptStage avsPrompt(boolean avsPrompt);
    }

    public interface CvvPromptStage {
        /**
         * <p>Indicates if the terminal should prompt for a Card Verfication Value (CVV) when running a transaction.</p>
         */
        _FinalStage cvvPrompt(boolean cvvPrompt);
    }

    public interface _FinalStage {
        ProcessingTerminalSecurity build();

        /**
         * <p>Indicates the level of AVS details that the terminal should prompt for.</p>
         */
        _FinalStage avsLevel(Optional<ProcessingTerminalSecurityAvsLevel> avsLevel);

        _FinalStage avsLevel(ProcessingTerminalSecurityAvsLevel avsLevel);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements TokenizationStage, AvsPromptStage, CvvPromptStage, _FinalStage {
        private boolean tokenization;

        private boolean avsPrompt;

        private boolean cvvPrompt;

        private Optional<ProcessingTerminalSecurityAvsLevel> avsLevel = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ProcessingTerminalSecurity other) {
            tokenization(other.getTokenization());
            avsPrompt(other.getAvsPrompt());
            avsLevel(other.getAvsLevel());
            cvvPrompt(other.getCvvPrompt());
            return this;
        }

        /**
         * <p>Indicates if the terminal can tokenize customer's payment details. For more information about tokenization, go to <a href="https://docs.payroc.com/knowledge/basic-concepts/tokenization">Tokenization</a>.</p>
         * <p>Indicates if the terminal can tokenize customer's payment details. For more information about tokenization, go to <a href="https://docs.payroc.com/knowledge/basic-concepts/tokenization">Tokenization</a>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("tokenization")
        public AvsPromptStage tokenization(boolean tokenization) {
            this.tokenization = tokenization;
            return this;
        }

        /**
         * <p>Indicates if the terminal should prompt for Address Verification Service (AVS) details when running a transaction.</p>
         * <p>Indicates if the terminal should prompt for Address Verification Service (AVS) details when running a transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("avsPrompt")
        public CvvPromptStage avsPrompt(boolean avsPrompt) {
            this.avsPrompt = avsPrompt;
            return this;
        }

        /**
         * <p>Indicates if the terminal should prompt for a Card Verfication Value (CVV) when running a transaction.</p>
         * <p>Indicates if the terminal should prompt for a Card Verfication Value (CVV) when running a transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cvvPrompt")
        public _FinalStage cvvPrompt(boolean cvvPrompt) {
            this.cvvPrompt = cvvPrompt;
            return this;
        }

        /**
         * <p>Indicates the level of AVS details that the terminal should prompt for.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage avsLevel(ProcessingTerminalSecurityAvsLevel avsLevel) {
            this.avsLevel = Optional.ofNullable(avsLevel);
            return this;
        }

        /**
         * <p>Indicates the level of AVS details that the terminal should prompt for.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "avsLevel", nulls = Nulls.SKIP)
        public _FinalStage avsLevel(Optional<ProcessingTerminalSecurityAvsLevel> avsLevel) {
            this.avsLevel = avsLevel;
            return this;
        }

        @java.lang.Override
        public ProcessingTerminalSecurity build() {
            return new ProcessingTerminalSecurity(tokenization, avsPrompt, avsLevel, cvvPrompt, additionalProperties);
        }
    }
}
