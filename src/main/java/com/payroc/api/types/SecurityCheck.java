/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SecurityCheck.Builder.class)
public final class SecurityCheck {
    private final Optional<SecurityCheckCvvResult> cvvResult;

    private final Optional<SecurityCheckAvsResult> avsResult;

    private final Map<String, Object> additionalProperties;

    private SecurityCheck(
            Optional<SecurityCheckCvvResult> cvvResult,
            Optional<SecurityCheckAvsResult> avsResult,
            Map<String, Object> additionalProperties) {
        this.cvvResult = cvvResult;
        this.avsResult = avsResult;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Indicates if the card verification value (CVV) that the customer provided in the request matches the CVV on the card.
     * <ul>
     * <li><code>M</code> – The CVV matches the card’s CVV.</li>
     * <li><code>N</code> – The CVV doesn’t match the card’s CVV.</li>
     * <li><code>P</code> – The CVV wasn’t processed.</li>
     * <li><code>U</code> – The CVV isn’t registered.</li>
     * </ul>
     * <p><strong>Note:</strong> Our gateway doesn’t automatically decline transactions when the CVV doesn’t match the card’s CVV, unless the merchant selects this setting in their account.</p>
     */
    @JsonProperty("cvvResult")
    public Optional<SecurityCheckCvvResult> getCvvResult() {
        return cvvResult;
    }

    /**
     * @return Indicates if the address that the customer provided in the request matches the address linked to the card.
     * <ul>
     * <li><code>Y</code> – The address in the request matches the address linked to the card.</li>
     * <li><code>N</code> – The address in the request doesn’t match the address linked to the card.</li>
     * <li><code>A</code> – The street address matches, but ZIP code or postal code doesn’t match.</li>
     * <li><code>Z</code> - The ZIP code or postal code matches, but street address doesn’t match.</li>
     * <li><code>U</code> – The address information is unavailable.</li>
     * <li><code>G</code> – The issuer or card brand doesn’t support the Address Verification Service (AVS).</li>
     * <li><code>R</code> – The AVS is currently unavailable. Try again later.</li>
     * <li><code>S</code> – There was no AVS data in the request, or it was sent in the wrong format.</li>
     * <li><code>F</code> - For UK addresses, the address in the request matches the address linked to the card.</li>
     * <li><code>W</code> – For US addresses, the nine-digit ZIP code or postal code in the request matches the address linked to the card but the street address doesn’t.</li>
     * <li><code>X</code> – For US addresses, the nine-digit ZIP code or postal code and the street address matches the address linked to the card.</li>
     * </ul>
     * <p><strong>Note:</strong> Our gateway doesn’t automatically decline transactions when the address doesn’t match the address linked to the card,
     * unless the merchant selects this setting in their account.</p>
     */
    @JsonProperty("avsResult")
    public Optional<SecurityCheckAvsResult> getAvsResult() {
        return avsResult;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SecurityCheck && equalTo((SecurityCheck) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SecurityCheck other) {
        return cvvResult.equals(other.cvvResult) && avsResult.equals(other.avsResult);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.cvvResult, this.avsResult);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<SecurityCheckCvvResult> cvvResult = Optional.empty();

        private Optional<SecurityCheckAvsResult> avsResult = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(SecurityCheck other) {
            cvvResult(other.getCvvResult());
            avsResult(other.getAvsResult());
            return this;
        }

        /**
         * <p>Indicates if the card verification value (CVV) that the customer provided in the request matches the CVV on the card.</p>
         * <ul>
         * <li><code>M</code> – The CVV matches the card’s CVV.</li>
         * <li><code>N</code> – The CVV doesn’t match the card’s CVV.</li>
         * <li><code>P</code> – The CVV wasn’t processed.</li>
         * <li><code>U</code> – The CVV isn’t registered.</li>
         * </ul>
         * <p><strong>Note:</strong> Our gateway doesn’t automatically decline transactions when the CVV doesn’t match the card’s CVV, unless the merchant selects this setting in their account.</p>
         */
        @JsonSetter(value = "cvvResult", nulls = Nulls.SKIP)
        public Builder cvvResult(Optional<SecurityCheckCvvResult> cvvResult) {
            this.cvvResult = cvvResult;
            return this;
        }

        public Builder cvvResult(SecurityCheckCvvResult cvvResult) {
            this.cvvResult = Optional.ofNullable(cvvResult);
            return this;
        }

        /**
         * <p>Indicates if the address that the customer provided in the request matches the address linked to the card.</p>
         * <ul>
         * <li><code>Y</code> – The address in the request matches the address linked to the card.</li>
         * <li><code>N</code> – The address in the request doesn’t match the address linked to the card.</li>
         * <li><code>A</code> – The street address matches, but ZIP code or postal code doesn’t match.</li>
         * <li><code>Z</code> - The ZIP code or postal code matches, but street address doesn’t match.</li>
         * <li><code>U</code> – The address information is unavailable.</li>
         * <li><code>G</code> – The issuer or card brand doesn’t support the Address Verification Service (AVS).</li>
         * <li><code>R</code> – The AVS is currently unavailable. Try again later.</li>
         * <li><code>S</code> – There was no AVS data in the request, or it was sent in the wrong format.</li>
         * <li><code>F</code> - For UK addresses, the address in the request matches the address linked to the card.</li>
         * <li><code>W</code> – For US addresses, the nine-digit ZIP code or postal code in the request matches the address linked to the card but the street address doesn’t.</li>
         * <li><code>X</code> – For US addresses, the nine-digit ZIP code or postal code and the street address matches the address linked to the card.</li>
         * </ul>
         * <p><strong>Note:</strong> Our gateway doesn’t automatically decline transactions when the address doesn’t match the address linked to the card,
         * unless the merchant selects this setting in their account.</p>
         */
        @JsonSetter(value = "avsResult", nulls = Nulls.SKIP)
        public Builder avsResult(Optional<SecurityCheckAvsResult> avsResult) {
            this.avsResult = avsResult;
            return this;
        }

        public Builder avsResult(SecurityCheckAvsResult avsResult) {
            this.avsResult = Optional.ofNullable(avsResult);
            return this;
        }

        public SecurityCheck build() {
            return new SecurityCheck(cvvResult, avsResult, additionalProperties);
        }
    }
}
