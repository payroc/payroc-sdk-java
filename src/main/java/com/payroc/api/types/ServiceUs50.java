/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class ServiceUs50 {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private ServiceUs50(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static ServiceUs50 hardwareAdvantagePlan(HardwareAdvantagePlan value) {
        return new ServiceUs50(new HardwareAdvantagePlanValue(value));
    }

    public boolean isHardwareAdvantagePlan() {
        return value instanceof HardwareAdvantagePlanValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<HardwareAdvantagePlan> getHardwareAdvantagePlan() {
        if (isHardwareAdvantagePlan()) {
            return Optional.of(((HardwareAdvantagePlanValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitHardwareAdvantagePlan(HardwareAdvantagePlan hardwareAdvantagePlan);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "name", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes(@JsonSubTypes.Type(HardwareAdvantagePlanValue.class))
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("hardwareAdvantagePlan")
    @JsonIgnoreProperties("name")
    private static final class HardwareAdvantagePlanValue implements Value {
        @JsonUnwrapped
        private HardwareAdvantagePlan value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private HardwareAdvantagePlanValue() {}

        private HardwareAdvantagePlanValue(HardwareAdvantagePlan value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitHardwareAdvantagePlan(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof HardwareAdvantagePlanValue && equalTo((HardwareAdvantagePlanValue) other);
        }

        private boolean equalTo(HardwareAdvantagePlanValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ServiceUs50{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("name")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ServiceUs50{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
