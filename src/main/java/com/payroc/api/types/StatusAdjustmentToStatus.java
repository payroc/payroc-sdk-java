/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class StatusAdjustmentToStatus {
    public static final StatusAdjustmentToStatus PENDING = new StatusAdjustmentToStatus(Value.PENDING, "pending");

    public static final StatusAdjustmentToStatus READY = new StatusAdjustmentToStatus(Value.READY, "ready");

    private final Value value;

    private final String string;

    StatusAdjustmentToStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof StatusAdjustmentToStatus
                        && this.string.equals(((StatusAdjustmentToStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case READY:
                return visitor.visitReady();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static StatusAdjustmentToStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "ready":
                return READY;
            default:
                return new StatusAdjustmentToStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        READY,

        PENDING,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitReady();

        T visitPending();

        T visitUnknown(String unknownType);
    }
}
