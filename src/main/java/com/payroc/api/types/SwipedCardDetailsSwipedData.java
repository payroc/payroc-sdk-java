/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class SwipedCardDetailsSwipedData {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private SwipedCardDetailsSwipedData(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static SwipedCardDetailsSwipedData encrypted(EncryptedSwipedDataFormat value) {
        return new SwipedCardDetailsSwipedData(new EncryptedValue(value));
    }

    public static SwipedCardDetailsSwipedData plainText(PlainTextSwipedDataFormat value) {
        return new SwipedCardDetailsSwipedData(new PlainTextValue(value));
    }

    public boolean isEncrypted() {
        return value instanceof EncryptedValue;
    }

    public boolean isPlainText() {
        return value instanceof PlainTextValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<EncryptedSwipedDataFormat> getEncrypted() {
        if (isEncrypted()) {
            return Optional.of(((EncryptedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PlainTextSwipedDataFormat> getPlainText() {
        if (isPlainText()) {
            return Optional.of(((PlainTextValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitEncrypted(EncryptedSwipedDataFormat encrypted);

        T visitPlainText(PlainTextSwipedDataFormat plainText);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "dataFormat",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(EncryptedValue.class), @JsonSubTypes.Type(PlainTextValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("encrypted")
    @JsonIgnoreProperties("dataFormat")
    private static final class EncryptedValue implements Value {
        @JsonUnwrapped
        private EncryptedSwipedDataFormat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EncryptedValue() {}

        private EncryptedValue(EncryptedSwipedDataFormat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEncrypted(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EncryptedValue && equalTo((EncryptedValue) other);
        }

        private boolean equalTo(EncryptedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SwipedCardDetailsSwipedData{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("plainText")
    @JsonIgnoreProperties("dataFormat")
    private static final class PlainTextValue implements Value {
        @JsonUnwrapped
        private PlainTextSwipedDataFormat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PlainTextValue() {}

        private PlainTextValue(PlainTextSwipedDataFormat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPlainText(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PlainTextValue && equalTo((PlainTextValue) other);
        }

        private boolean equalTo(PlainTextValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SwipedCardDetailsSwipedData{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("dataFormat")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SwipedCardDetailsSwipedData{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
