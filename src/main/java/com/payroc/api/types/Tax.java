/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class Tax {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private Tax(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static Tax amount(TaxAmount value) {
        return new Tax(new AmountValue(value));
    }

    public static Tax rate(TaxRate value) {
        return new Tax(new RateValue(value));
    }

    public boolean isAmount() {
        return value instanceof AmountValue;
    }

    public boolean isRate() {
        return value instanceof RateValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TaxAmount> getAmount() {
        if (isAmount()) {
            return Optional.of(((AmountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TaxRate> getRate() {
        if (isRate()) {
            return Optional.of(((RateValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAmount(TaxAmount amount);

        T visitRate(TaxRate rate);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(AmountValue.class), @JsonSubTypes.Type(RateValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("amount")
    @JsonIgnoreProperties("type")
    private static final class AmountValue implements Value {
        @JsonUnwrapped
        private TaxAmount value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AmountValue() {}

        private AmountValue(TaxAmount value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAmount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AmountValue && equalTo((AmountValue) other);
        }

        private boolean equalTo(AmountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Tax{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("rate")
    @JsonIgnoreProperties("type")
    private static final class RateValue implements Value {
        @JsonUnwrapped
        private TaxRate value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RateValue() {}

        private RateValue(TaxRate value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRate(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RateValue && equalTo((RateValue) other);
        }

        private boolean equalTo(RateValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Tax{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Tax{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
