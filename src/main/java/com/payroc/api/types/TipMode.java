/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class TipMode {
    public static final TipMode PROMPTED = new TipMode(Value.PROMPTED, "prompted");

    public static final TipMode ADJUSTED = new TipMode(Value.ADJUSTED, "adjusted");

    private final Value value;

    private final String string;

    TipMode(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof TipMode && this.string.equals(((TipMode) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PROMPTED:
                return visitor.visitPrompted();
            case ADJUSTED:
                return visitor.visitAdjusted();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static TipMode valueOf(String value) {
        switch (value) {
            case "prompted":
                return PROMPTED;
            case "adjusted":
                return ADJUSTED;
            default:
                return new TipMode(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PROMPTED,

        ADJUSTED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPrompted();

        T visitAdjusted();

        T visitUnknown(String unknownType);
    }
}
