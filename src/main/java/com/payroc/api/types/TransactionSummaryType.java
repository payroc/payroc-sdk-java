/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class TransactionSummaryType {
    public static final TransactionSummaryType RETURN = new TransactionSummaryType(Value.RETURN, "return");

    public static final TransactionSummaryType CAPTURE = new TransactionSummaryType(Value.CAPTURE, "capture");

    private final Value value;

    private final String string;

    TransactionSummaryType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof TransactionSummaryType
                        && this.string.equals(((TransactionSummaryType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case RETURN:
                return visitor.visitReturn();
            case CAPTURE:
                return visitor.visitCapture();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static TransactionSummaryType valueOf(String value) {
        switch (value) {
            case "return":
                return RETURN;
            case "capture":
                return CAPTURE;
            default:
                return new TransactionSummaryType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        CAPTURE,

        RETURN,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCapture();

        T visitReturn();

        T visitUnknown(String unknownType);
    }
}
