/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.payroc.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.payroc.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Webhook.Builder.class)
public final class Webhook {
    private final String uri;

    private final String secret;

    private final String supportEmailAddress;

    private final Map<String, Object> additionalProperties;

    private Webhook(String uri, String secret, String supportEmailAddress, Map<String, Object> additionalProperties) {
        this.uri = uri;
        this.secret = secret;
        this.supportEmailAddress = supportEmailAddress;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Public endpoint that we send notifications to.
     */
    @JsonProperty("uri")
    public String getUri() {
        return uri;
    }

    /**
     * @return String that we send with a notification so that you can ensure it is a valid notification from our gateway. We include the value in the Payroc-Secret header parameter in the webhook call.
     * <strong>Note:</strong> In the response, we truncate the secret to the last 16 characters and mask the first 10 characters.
     */
    @JsonProperty("secret")
    public String getSecret() {
        return secret;
    }

    /**
     * @return Email address of the person or team that we contact if we can't deliver notifications.
     */
    @JsonProperty("supportEmailAddress")
    public String getSupportEmailAddress() {
        return supportEmailAddress;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Webhook && equalTo((Webhook) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Webhook other) {
        return uri.equals(other.uri)
                && secret.equals(other.secret)
                && supportEmailAddress.equals(other.supportEmailAddress);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.uri, this.secret, this.supportEmailAddress);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static UriStage builder() {
        return new Builder();
    }

    public interface UriStage {
        /**
         * <p>Public endpoint that we send notifications to.</p>
         */
        SecretStage uri(@NotNull String uri);

        Builder from(Webhook other);
    }

    public interface SecretStage {
        /**
         * <p>String that we send with a notification so that you can ensure it is a valid notification from our gateway. We include the value in the Payroc-Secret header parameter in the webhook call.
         * <strong>Note:</strong> In the response, we truncate the secret to the last 16 characters and mask the first 10 characters.</p>
         */
        SupportEmailAddressStage secret(@NotNull String secret);
    }

    public interface SupportEmailAddressStage {
        /**
         * <p>Email address of the person or team that we contact if we can't deliver notifications.</p>
         */
        _FinalStage supportEmailAddress(@NotNull String supportEmailAddress);
    }

    public interface _FinalStage {
        Webhook build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements UriStage, SecretStage, SupportEmailAddressStage, _FinalStage {
        private String uri;

        private String secret;

        private String supportEmailAddress;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Webhook other) {
            uri(other.getUri());
            secret(other.getSecret());
            supportEmailAddress(other.getSupportEmailAddress());
            return this;
        }

        /**
         * <p>Public endpoint that we send notifications to.</p>
         * <p>Public endpoint that we send notifications to.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("uri")
        public SecretStage uri(@NotNull String uri) {
            this.uri = Objects.requireNonNull(uri, "uri must not be null");
            return this;
        }

        /**
         * <p>String that we send with a notification so that you can ensure it is a valid notification from our gateway. We include the value in the Payroc-Secret header parameter in the webhook call.
         * <strong>Note:</strong> In the response, we truncate the secret to the last 16 characters and mask the first 10 characters.</p>
         * <p>String that we send with a notification so that you can ensure it is a valid notification from our gateway. We include the value in the Payroc-Secret header parameter in the webhook call.
         * <strong>Note:</strong> In the response, we truncate the secret to the last 16 characters and mask the first 10 characters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("secret")
        public SupportEmailAddressStage secret(@NotNull String secret) {
            this.secret = Objects.requireNonNull(secret, "secret must not be null");
            return this;
        }

        /**
         * <p>Email address of the person or team that we contact if we can't deliver notifications.</p>
         * <p>Email address of the person or team that we contact if we can't deliver notifications.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("supportEmailAddress")
        public _FinalStage supportEmailAddress(@NotNull String supportEmailAddress) {
            this.supportEmailAddress =
                    Objects.requireNonNull(supportEmailAddress, "supportEmailAddress must not be null");
            return this;
        }

        @java.lang.Override
        public Webhook build() {
            return new Webhook(uri, secret, supportEmailAddress, additionalProperties);
        }
    }
}
